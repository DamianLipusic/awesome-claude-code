[
  {
    "id": "T001",
    "title": "scaffold project structure",
    "description": "Create index.html, main.css skeleton, and all src/ module stubs so imports resolve.",
    "priority": 1,
    "status": "done",
    "phase": 1,
    "notes": "Created full project scaffold: index.html, styles/main.css, src/main.js, src/core/{state,events,tick,actions}.js, src/systems/{resources,research}.js, src/ui/{hud,buildingPanel,messageLog,researchPanel,tabs}.js, src/data/{buildings,units,techs}.js, src/utils/{logger,fmt}.js. Also created CLAUDE.md, roadmap.md, tasks.json, execution.log."
  },
  {
    "id": "T002",
    "title": "implement state, events, and tick loop",
    "description": "state.js (initial state), events.js (pub/sub), tick.js (setInterval 250ms emitting tick).",
    "priority": 2,
    "status": "done",
    "phase": 1,
    "notes": "state.js: full initial state with resources/rates/caps/buildings/units/techs/queues/messages. events.js: tiny pub/sub (on/off/emit) with Events constants. tick.js: 250ms interval loop with registerSystem(), startLoop(), stopLoop(). All systems registered in main.js."
  },
  {
    "id": "T003",
    "title": "implement resource system with production/consumption rates",
    "description": "resources.js system: on each tick, apply rates to state.resources. Rates derived from buildings and techs.",
    "priority": 3,
    "status": "done",
    "phase": 1,
    "notes": "Implemented as part of T001/T002 bootstrap. resources.js: recalcRates() sums building production/consumption with tech multipliers and unit upkeep; resourceTick() applies per-tick delta capped at caps; advances training queue. Integrated into tick loop via registerSystem()."
  },
  {
    "id": "T004",
    "title": "implement building system (build, upgrade, demolish)",
    "description": "buildings data file, actions for build/upgrade/demolish, cost deduction, rate recalculation.",
    "priority": 4,
    "status": "done",
    "phase": 1,
    "notes": "data/buildings.js: 12 buildings with icon/cost/production/consumption/capBonus/requires. actions.js: buildBuilding() with 15%-per-level cost scaling, demolishBuilding(), trainUnit(). All trigger recalcRates() and emit events."
  },
  {
    "id": "T005",
    "title": "render resource HUD and building panel UI",
    "description": "UI components: resource bar at top, building cards panel, message log at bottom.",
    "priority": 5,
    "status": "done",
    "phase": 1,
    "notes": "hud.js: 6-resource HUD bar with icon/value/cap/rate, updates throttled to 1s. buildingPanel.js: grid of building cards with locked state, cost display, build/demolish buttons, click delegation. messageLog.js: scrolling event log, 15 visible entries. tabs.js: tab switcher for Buildings/Research/Log panels. researchPanel.js: tech tree with progress bar, prerequisite gating."
  },
  {
    "id": "T006",
    "title": "add unit data and training queue",
    "description": "units.js data, training queue system, UI panel for military.",
    "priority": 6,
    "status": "done",
    "phase": 2,
    "notes": "units.js: 4 units (soldier, archer, knight, mage) with cost/trainTicks/attack/defense/upkeep/requires. Training queue processed in resourceTick()._advanceTrainingQueue(). trainUnit() action in actions.js. Military panel (T007 session): militaryPanel.js added with army summary, training queue progress bar, and unit cards with cost/stat display and Train buttons."
  },
  {
    "id": "T007",
    "title": "implement map grid (20x20 tiles, fog of war)",
    "description": "Canvas-based 20x20 tile map with fog of war reveal on territory capture.",
    "priority": 7,
    "status": "done",
    "phase": 2,
    "notes": "systems/map.js: initMap() generates 20x20 tile grid with 5 terrain types (grass/forest/hills/river/mountain), player 3x3 capital at (10,10), ~20 enemy settlements. revealAround() expands fog on capture. territoryRateBonus() returns per-second rate bonuses for captured terrain (forest +wood, hills +stone, river +food, mountain +iron, grass +gold). ui/mapPanel.js: 480x480 canvas with terrain colours, player/enemy tints, fog-of-war, hover highlight for attackable tiles, click-to-attack. events.js: MAP_CHANGED, COMBAT added. state.js: map field added. index.html: Map and Military tabs added. main.js: initMap() called on boot/new-game, map saved/loaded."
  },
  {
    "id": "T008",
    "title": "basic combat resolution engine",
    "description": "Attack action, power calculation, outcome resolution, resource loot.",
    "priority": 8,
    "status": "done",
    "phase": 2,
    "notes": "systems/combat.js: attackTile(x,y) validates adjacency/fog/owner, sums unit attack values (with tech multipliers for tactics/steel/engineering), resolves combat probabilistically (win chance = power/(power+defense)), victory captures tile + grants loot + reveals neighbors, defeat loses 1 random unit. Defense scales with distance from capital (15 + dist*6). Log messages use combat-win/combat-loss types with matching CSS border colours."
  },
  {
    "id": "T009",
    "title": "territory capture and expansion mechanics",
    "description": "Capture adjacent tiles, apply territory bonuses to resource rates.",
    "priority": 9,
    "status": "done",
    "phase": 2,
    "notes": "Implemented as part of T007/T008. territoryRateBonus() in map.js returns rate bonuses integrated into recalcRates() in resources.js. Capturing tiles via attackTile() immediately triggers recalcRates() so bonuses apply. Map stats bar shows live territory/enemy/explored tile counts."
  },
  {
    "id": "T010",
    "title": "tech tree data and research system",
    "description": "techs.js data with prerequisites, research action, tick-based progress.",
    "priority": 10,
    "status": "done",
    "phase": 3,
    "notes": "Implemented as part of bootstrap. data/techs.js: 6 techs with cost/researchTicks/effectDesc/requires. systems/research.js: startResearch() validates prereqs and deducts cost; researchTick() advances queue and applies tech. ui/researchPanel.js: full tech tree UI with progress bar and gating."
  },
  {
    "id": "T016",
    "title": "save/load via localStorage (auto-save every 60s)",
    "description": "Serialize state to localStorage on interval, load on page open, manual save/load buttons.",
    "priority": 16,
    "status": "done",
    "phase": 5,
    "notes": "Implemented in main.js: _save() serializes state to localStorage key 'empireos-save'; _loadSave()/_applySave() restore on boot. Auto-save every 60s via setInterval. Manual Save button in title bar. New Game button clears save and reinits state. Version bumped to 2 to include map state."
  },
  {
    "id": "T011",
    "title": "tech effects applied to rates and unlock gates",
    "description": "Apply tech research bonuses to building multipliers, unit attack, and unlock requirements.",
    "priority": 11,
    "status": "done",
    "phase": 3,
    "notes": "Added 3 combat techs (tactics +25% attack, steel +50% attack, engineering +10% attack + unlocks Watchtower) to techs.js. Added tech requirements: knight requires metalworking, mage requires arcane, watchtower requires engineering. Wired warcraft -25% training speed in trainUnit() with totalTicks stored in queue entry for correct progress bar. buildingPanel now re-renders on TECH_CHANGED. militaryPanel reqStr shows tech icons/names via TECHS lookup. Army display shows tech-boosted combat power."
  },
  {
    "id": "T012",
    "title": "prestige / age advancement mechanic",
    "description": "Age system (Stone/Bronze/Iron/Medieval), unlock new buildings/units per age, prestige button.",
    "priority": 12,
    "status": "done",
    "phase": 3,
    "notes": "Created data/ages.js with 4 ages (Stone/Bronze/Iron/Medieval) each with productionMult (1.0/1.25/1.5/2.0), resource cost, and requirements (totalBuildings, totalUnits, territory, tech). Added state.age=0 to state.js. Added AGE_CHANGED event. Added advanceAge() to actions.js with full requirement checking + cost deduction. Applied age productionMult to building production in resources.js recalcRates(). Added age requirements to archeryRange (Bronze) and knight unit (Bronze). Building and military panels handle type:'age' in unlock logic and re-render on AGE_CHANGED. Research panel shows age section at top with current age, next age requirements checklist, cost, and Advance button. Age badge in title bar. Save/load includes age field (version 3)."
  },
  {
    "id": "T013",
    "title": "random event system (raids, windfalls, disasters)",
    "description": "Time-triggered random events that affect resources, units, or territory.",
    "priority": 13,
    "status": "done",
    "phase": 4,
    "notes": "Created src/systems/randomEvents.js with 10 events (3 raids: bandit_raid/goblin_attack/pirates; 4 windfalls: merchant_caravan/bountiful_harvest/ore_vein/mana_surge; 3 disasters: drought/mine_collapse/plague). Disasters push temporary rate modifiers into state.randomEvents.activeModifiers, consumed by recalcRates() in resources.js. Events fire on a weighted-random 120–240s interval. initRandomEvents() wired in main.js boot; randomEventTick() registered as a tick system. state.randomEvents saved/loaded (save version 4). Log colours: raid=red, windfall=green, disaster=orange."
  },
  {
    "id": "T014",
    "title": "quest/objective system with rewards",
    "description": "Milestone objectives (first building, first unit, first capture, etc.) with resource rewards.",
    "priority": 14,
    "status": "done",
    "phase": 4,
    "notes": "Created src/systems/quests.js with 11 milestone quests (first_building/first_unit/five_buildings/first_research/first_capture/army_ten/ten_buildings/twenty_territory/age_bronze/age_iron/age_medieval). Each quest has a check() predicate, icon, and resource reward. initQuests() subscribes to BUILDING_CHANGED/UNIT_CHANGED/TECH_CHANGED/AGE_CHANGED/MAP_CHANGED. Created src/ui/questPanel.js rendering a card grid with completion progress bar; added 🏆 Quests tab in index.html. Added QUEST_COMPLETED event constant. state.quests saved/loaded. CSS: quest-card, quest-header, progress bar gold fill, log-entry--quest."
  },
  {
    "id": "T015",
    "title": "empire log / story panel",
    "description": "Narrative story panel showing empire history milestones.",
    "priority": 15,
    "status": "done",
    "phase": 4,
    "notes": "Created src/systems/story.js: initStory() registers dedup-safe milestone entries on GAME_STARTED/BUILDING_CHANGED/UNIT_CHANGED/TECH_CHANGED/AGE_CHANGED/MAP_CHANGED/QUEST_COMPLETED events. 16 milestone checks (founding, building x4, military x3, research x3, age x3, territory x4, quest per-completion). Created src/ui/storyPanel.js: vertical timeline with icon, title, desc, tick-to-time footer; type-based left border accent colours. Added state.story=[] to state.js (reset in initState). Included in save/load (version 5). Added '📖 History' tab in index.html. Wired initStory+initStoryPanel in main.js. Added full CSS: .story-header, .story-timeline (::before vertical line), .story-entry (icon on line, body), type-accent border colours."
  },
  {
    "id": "T017",
    "title": "settings panel (speed, sound toggle)",
    "description": "Settings tab with game speed slider (0.5×/1×/2×/4×) and placeholder sound toggle.",
    "priority": 17,
    "status": "done",
    "phase": 5,
    "notes": "Extended src/core/tick.js: added _speedMult module var; setTickSpeed(mult) restarts interval at TICK_MS/mult; getTickSpeed() returns current mult; startLoop uses _speedMult so loops started after a speed change use the right interval. Created src/ui/settingsPanel.js: speed button row (½×/1×/2×/4×) with active highlight, disabled sound checkbox with 'Coming soon' badge, About section. Added '⚙️ Settings' tab in index.html. Wired initSettingsPanel in main.js. Added full CSS in main.css: .settings-section, .speed-buttons, .btn--speed, .btn--speed-active, .settings-toggle."
  },
  {
    "id": "T018",
    "title": "visual polish — CSS animations, transitions",
    "description": "Resource gain/loss flash, building card pop-in, combat flash on map tiles.",
    "priority": 18,
    "status": "done",
    "phase": 5,
    "notes": "Added @keyframes flash-gain/flash-loss/pop-in/slide-in to main.css. hud.js: tracks _prevValues per resource; flashes .hud__value--gain/.hud__value--loss when |delta| > 40 (filters per-tick noise). buildingPanel.js: added data-building-id attr to all cards; BUILDING_CHANGED handler calls _popCard(id) which applies .building-card--popin animation. mapPanel.js: _combatFlash module var + rAF-driven _animateFlash() fades an rgba overlay on the attacked tile over ~315ms (19 frames); MAP_CHANGED listener branches on data.outcome to trigger flash vs plain re-render. CSS: .log-entry:first-child gets slide-in so newest log entries animate in."
  },
  {
    "id": "T019",
    "title": "responsive layout for mobile",
    "description": "Map canvas scales down, HUD wraps gracefully, panels usable on 375px width.",
    "priority": 19,
    "status": "done",
    "phase": 5,
    "notes": "Added @media (max-width: 480px) block to main.css: title bar condensed (font-size, padding); HUD resources shrink to flex: 1 1 30% with 10px font; tab bar uses overflow-x:auto + scrollbar-width:none (hidden scrollbar on all platforms); all card grids (buildings, units, techs, quests) collapse to single column; map canvas uses width:100%/height:auto with touch-action:none; story timeline icon offset reduced; research-active stacks vertically; age-panel tighter padding. Also enhanced existing 600px breakpoint."
  },
  {
    "id": "T020",
    "title": "diplomacy system (alliances, trade routes)",
    "description": "AI empires, alliance/war status, trade route resource exchange per tick.",
    "priority": 20,
    "status": "done",
    "phase": 6,
    "notes": "Created data/empires.js with 3 AI empires (Iron Horde/Mage Council/Sea Wolves), each with tradeGift rates and AI probability params. Created systems/diplomacy.js: initDiplomacy(), diplomacyTick() with AI stance changes (neutral↔allied↔war) and war raids; player actions proposeAlliance/openTradeRoute/closeTradeRoute/declareWar/proposePeace with gold costs (200/100/—/—/300). Created ui/diplomacyPanel.js: empire cards showing relation badge, specialty, live trade income, context-sensitive action buttons. Trade income integrated into resources.js recalcRates() by reading state.diplomacy directly (no circular import). Added DIPLOMACY_CHANGED event. Added Diplomacy tab to index.html. Save version bumped to 7."
  },
  {
    "id": "T021",
    "title": "seasons cycle affecting resource rates",
    "description": "4-season cycle (90s each), rate modifiers per season per resource.",
    "priority": 21,
    "status": "done",
    "phase": 6,
    "notes": "Created data/seasons.js with 4 seasons (Spring/Summer/Autumn/Winter, 90s each). Spring: food +25%, wood +10%. Summer: wood +20%, food +10%, gold +10%. Autumn: food +15%, stone +20%, gold +20%. Winter: food -30%, wood -20%. Created systems/seasons.js: initSeasons(), seasonTick() advances season and emits SEASON_CHANGED; currentSeason()/seasonTicksRemaining() helpers. Season multipliers applied in resources.js recalcRates() to positive rates only (unit upkeep unaffected). Added season badge in title bar with live countdown tooltip. Added SEASON_CHANGED event. Save version 7."
  },
  {
    "id": "T022",
    "title": "hero unit with special abilities",
    "description": "Hero unit with unique abilities (battle cry, inspire, siege), recruit once per game.",
    "priority": 22,
    "status": "done",
    "phase": 6,
    "notes": "Created data/hero.js with HERO_DEF (Champion, +60 attack, 3 abilities). recruitHero()/useHeroAbility() in actions.js. Hero upkeep in recalcRates(); Inspire doubles training speed in _advanceTrainingQueue(). Combat: hero adds flat +60 power; Battle Cry ×2 attack on next strike (consumed); Siege Master guarantees win (winChance=1.0, consumed). militaryPanel.js: hero section shows recruit card (age-gated, cost-gated) or active hero with ability buttons + cooldown countdowns (16-tick refresh). army section shows hero badge. HERO_CHANGED event. state.hero saved/loaded (version 8). CSS: .hero-card, .hero-card--active, .btn--hero, .hero-ability, .btn--ability, .hero-effect--active/--cd, .mil-unit-badge--hero, .log-entry--hero."
  },
  {
    "id": "T023",
    "title": "leaderboard (localStorage high scores)",
    "description": "Track best territory count and gold earned per session, display top 5.",
    "priority": 23,
    "status": "done",
    "phase": 6,
    "notes": "state.stats = {goldEarned, peakTerritory} added to state.js+initState(). goldEarned tracked in resourceTick() (adds gold rate/tick when positive). peakTerritory updated on MAP_CHANGED in main.js._updatePeakTerritory(). _saveToLeaderboard() called on New Game before wipe; saves {name, territory, goldEarned, age, quests, tick, date} to 'empireos-leaderboard' localStorage key; keeps top 10 sorted by territory then gold. settingsPanel.js: leaderboard section renders top-5 rows in a grid table with clear button. state.stats saved/loaded (save version 8). CSS: .lb-table, .lb-header, .lb-row (--gold for #1), .lb-rank/name/territory/gold/age/date."
  },
  {
    "id": "T024",
    "title": "export/import save as JSON string",
    "description": "Encode state as base64 JSON, provide copy/paste import modal.",
    "priority": 24,
    "status": "done",
    "phase": 6,
    "notes": "Created src/ui/saveModal.js: initSaveModal(onImport) injects modal DOM + close handlers (X button, Cancel, Esc, click-outside); exportSave() builds payload, base64-encodes with Unicode-safe btoa(unescape(encodeURIComponent(json))), shows modal with clipboard copy (with execCommand fallback); importSave() shows paste textarea, decodes with decodeURIComponent(escape(atob())), validates save.state field, calls onImport callback. Updated settingsPanel.js: imports exportSave/importSave; added '💾 Save Portability' section with Export and Import buttons; binds them in _render(). Updated main.js: imports initSaveModal; calls initSaveModal(_applySave) after UI init. CSS: .modal-overlay (fixed inset, backdrop-filter blur), .modal--hidden (display:none), .modal-box, .modal-header/.title/.close, .modal-body/.desc/.textarea/.error, .modal-footer, .btn--ghost, .save-port-buttons; responsive mobile rules."
  },
  {
    "id": "T025",
    "title": "victory and defeat conditions with endgame screen",
    "description": "Win by reaching Medieval Age + 80 territory + 10 quests. Lose by starvation (food=0 for 30s). Game-over overlay shows stats and Play Again button.",
    "priority": 25,
    "status": "done",
    "phase": 7,
    "notes": "Created systems/victory.js: initVictory() resets module-level counters; victoryTick() checks win (age≥3 + tiles≥80 + quests≥10), lose (food=0 + negative rate for 120 ticks / 30s); warns at 60 starvation ticks; calls stopLoop() + emits GAME_OVER on trigger. Created ui/gameOverPanel.js: appends overlay to body; subscribes to GAME_OVER; renders win/lose box with stats (age, territory, quests, gold earned, time played), Play Again button which calls _newGame(). Added GAME_OVER event to events.js. Added state.gameOver field to state.js initState(). main.js: extracted _newGame() function (startLoop() + initVictory() + all system resets); old New Game button handler calls _newGame(); initGameOverPanel(_newGame) wired after UI init; victoryTick registered as 6th tick system; initVictory() called on boot. CSS: .game-over-overlay (fixed, z-2000, blur backdrop, fade-in animation), .game-over-box (win=gold border, lose=red border, pop-in animation), .game-over-stats (3-col grid), .go-stat-* cells, .game-over-btn."
  },
  {
    "id": "T027",
    "title": "resource trading market (buy/sell for gold)",
    "description": "Market system with fluctuating prices (±10% random walk every 15s). Buy/sell food/wood/stone/iron/mana for gold. Requires Market building. Market tab with price table, trend arrows, and buy/sell buttons (×10/×50/×100).",
    "priority": 27,
    "status": "done",
    "phase": 7,
    "notes": "Created src/systems/market.js: initMarket() seeds state.market with per-resource price multipliers (1.0), trend indicators, and totalTrades counter; marketTick() applies ±7.5% random drift every 15s (bounded 0.4–2.5×); buyPrice()/sellPrice() apply 20% spread over base gold values (food=1.5g, wood=2g, stone=3g, iron=5g, mana=10g); sellResources()/buyResources() validate Market building requirement, deduct/add correctly capped resources, increment totalTrades, emit MARKET_CHANGED. Created src/ui/marketPanel.js: locked-state card when no Market building; price table with resource name/stock, sell price (green)+trend arrow, buy price (red), ×10/×50/×100 sell (green) and buy (red) buttons; click delegation with btn--shake feedback on error. Added MARKET_CHANGED + ACHIEVEMENT_UNLOCKED events to events.js. Added state.market null field to state.js. index.html: 🏪 Market tab + panel. main.js: marketTick registered as 7th system; initMarket() called on boot + newGame; market saved/loaded (save version 9). CSS: market-locked, market-header, market-table (5-col grid), market-row, market-price--sell/--buy, trend--up/down/flat, btn--xs/--sell/--buy, btn--shake keyframes, mobile collapses to single column."
  },
  {
    "id": "T028",
    "title": "achievements system (persistent cross-game badges)",
    "description": "15 achievements stored in empireos-achievements localStorage key (persists across New Game wipes). Check on game events. Show in Settings panel with icon, title, desc, unlock date. Achievements section with locked/unlocked states. Toast message on unlock.",
    "priority": 28,
    "status": "done",
    "phase": 7,
    "notes": "Created src/systems/achievements.js: 15 achievement definitions (founder/builder/warlord/scholar/bronze_dawn/medieval/conqueror/diplomat/champion/wonder_built/quest_master/millionaire/trader/emperor/comeback). loadAchievements() reads empireos-achievements localStorage key; initAchievements() rebuilds in-memory _unlocked Set and registers event listeners on BUILDING_CHANGED/UNIT_CHANGED/TECH_CHANGED/AGE_CHANGED/MAP_CHANGED/DIPLOMACY_CHANGED/HERO_CHANGED/QUEST_COMPLETED/MARKET_CHANGED/RESOURCE_CHANGED/GAME_OVER/MESSAGE. _checkAndUnlock(id) calls predicate then _unlock() which persists to localStorage, adds achievement log message (type='achievement'), emits ACHIEVEMENT_UNLOCKED, calls _renderer callback. setAchievementRenderer() allows settingsPanel to register a re-render hook without circular imports. Updated settingsPanel.js: imports ACHIEVEMENTS/loadAchievements/setAchievementRenderer; _achievementsSection() builds ach-card grid with unlocked (gold border, unlock date) and locked (greyed, lock icon) states; 'X / 15 unlocked' progress count; registered _renderer in initSettingsPanel. main.js: imports+calls initAchievements() on boot and newGame. CSS: ach-grid (auto-fill 220px), ach-card (unlocked=gold border+tint, locked=55% opacity), ach-icon/body/title/desc/date/locked-label; log-entry--achievement (gold border + faint gold bg)."
  },
  {
    "id": "T029",
    "title": "toast notification system",
    "description": "Slide-in toasts for key game events (quest complete, achievement, age advance, raid, windfall, disaster). Show for 3.5s then fade out. Fixed bottom-right position, event-type colour coding.",
    "priority": 29,
    "status": "done",
    "phase": 8,
    "notes": "Created src/ui/toastManager.js: initToasts() creates #toast-container and subscribes to MESSAGE events; shows toast only for types in TOAST_TYPES set (quest/achievement/age/raid/windfall/disaster); showToast() creates .toast div with type modifier class, auto-dismisses after 3.5s via setTimeout; click dismisses immediately; _dismissToast() adds .toast--exiting class for exit animation then removes from DOM on animationend (with 400ms fallback). CSS: #toast-container fixed bottom-right z-900; @keyframes toast-in (translateX slide from right) / toast-out (reverse); 8 type colour variants via border-left-color; mobile shrinks to calc(100vw-24px). Wired in main.js boot sequence."
  },
  {
    "id": "T030",
    "title": "empire summary dashboard panel",
    "description": "New '📊 Empire' tab with at-a-glance overview: resource rates, territory, army power, diplomacy status, season, quest/achievement progress, lifetime stats.",
    "priority": 30,
    "status": "done",
    "phase": 8,
    "notes": "Created src/ui/summaryPanel.js with 6 summary cards in a responsive auto-fill grid. Empire header shows name/age/season badges. Resources card: all 6 with icon/name/value:cap/rate (positive=green, negative=red). Military card: unit rows with count+attack, hero row, training queue count, total attack power (with tech multipliers applied). Territory card: player tiles/%, enemy tiles, explored count. Diplomacy card: 3 empire rows with relation badge (neutral/allied/war colour coded) and trade route count. Progression card: quest progress bar (X/N + % fill), achievements bar (reads empireos-achievements localStorage, X/15), victory conditions checklist (Medieval Age + 80 territory + 10 quests) with green/red check icons. Lifetime Stats card: gold earned, peak territory, market trades, time played. Panel re-renders on 12 event types; RESOURCE_CHANGED throttled 4-tick; TICK drives a visibility-gated countdown re-render for time + season freshness. Added '📊 Empire' as first tab in tab bar (auto-activated by tabs.js). CSS: summary-grid auto-fill 230px; summary-card; sum-res-row/sum-stat-row/sum-dipl-row; sum-progress with quest/ach/victory fill variants; summary-empire-header/name/meta/badge. Mobile: single column at ≤480px."
  },
  {
    "id": "T026",
    "title": "wonder buildings (3 unique late-game structures)",
    "description": "Great Library (-25% research time, +1 mana/s), Colosseum (-33% training time, +3 food/s), Grand Cathedral (+8 gold/s, -50% disaster severity). Each unique (max 1 per game).",
    "priority": 26,
    "status": "done",
    "phase": 7,
    "notes": "Added 3 wonder buildings to data/buildings.js with unique:true, wonder:true flags: greatLibrary (🏛️, Iron Age, arcane+manaWell×2, cost 500g/300s/200i/100m, +1 mana/s, +300 mana cap), colosseum (🏟️, Bronze Age, barracks×2, cost 400g/350s/120i, +3 food/s), grandCathedral (⛪, Medieval Age, manaWell×2+wall×2, cost 600g/400s/200i/150m, +8 gold/s, +500 gold cap). actions.js: buildBuilding() rejects unique buildings when count≥1; trainUnit() chains Colosseum -33% speed after warcraft -25%. research.js: startResearch() applies -25% totalTicks when greatLibrary built; totalTicks stored in queue entry. resources.js: grandCathedral halves disaster penalty in activeModifiers loop (1-(1-rateMult)*0.5). buildingPanel.js: rewritten to for-loop; injects '🏛️ Wonders' section header before first wonder; shows 'Built ✓' badge + description for already-built unique buildings (no actions); wonder cards get .building-card--wonder gold border class; Construct button uses .btn--wonder. researchPanel.js: progress bar uses active.totalTicks ?? def.researchTicks for Library-adjusted display. CSS: .wonder-section-header (grid-column:1/-1, gold text), .building-card--wonder (gold border tint), .building-card--wonder-built (solid gold border), .btn--wonder (gold gradient button)."
  },
  {
    "id": "T031",
    "title": "keyboard shortcuts (tab switching, pause, save)",
    "description": "Global keydown handler: 1-0 switch tabs, Space/P pause/resume, S quick-save, Escape close modal. Guard against input fields. Show shortcut legend in Settings panel.",
    "priority": 31,
    "status": "done",
    "phase": 8,
    "notes": "tabs.js: exported switchTab(panelId) that simulates a tab-button click programmatically. main.js: imported stopLoop and switchTab; added _TAB_KEYS map (1→summary…0→settings, -→log); _bindKeyboard() registers a global keydown handler with input-field guard and modifier-key guard; tab keys call switchTab(); Space/P toggles pause/resume (stopLoop/startLoop) with addMessage feedback; S quick-saves; Escape closes the save modal. _bindKeyboard() called from _bindControls(). settingsPanel.js: added _shortcutsSection() rendering a .kbd-grid table with <kbd> elements for each binding; section injected into _render() between leaderboard and save-portability sections. CSS: kbd element style (inline-block, monospace, bg-card, border-bottom-2px, border-radius 3px); .kbd-grid flex column; .kbd-row 2-col grid (150px keys + 1fr desc); .kbd-keys flex row with gap."
  },
  {
    "id": "T032",
    "title": "resource sparklines in HUD (60-second rolling SVG trends)",
    "description": "Keep a 60-sample ring buffer (one per second) for each resource. Render inline SVG polyline sparklines in each HUD resource cell showing the last 60 seconds of history. Green for positive-rate resources, red for negative. Hidden on mobile ≤600px.",
    "priority": 32,
    "status": "done",
    "phase": 8,
    "notes": "hud.js: added HISTORY_LEN=60, SPARK_W=60, SPARK_H=18 constants; _history object (one array per resource); _sampleHistory() pushes current value and shifts when >60 entries; _sparkSVG(values, positiveRate) generates inline SVG polyline with min/max normalisation (1px padding on all edges), flat line for zero-range data, green stroke for positive-rate resources, red for negative; initHUD() adds <span class=hud__spark-wrap id=hud-spark-{id}> to each resource div; renderHUD() updates sparkEl innerHTML; _throttledRender() now calls _sampleHistory() before renderHUD() so history is always fresh at the render boundary. CSS: .hud__spark-wrap flex align-center margin-left:2px opacity:0.75 flex-shrink:0; .hud__spark display:block overflow:visible; @media (max-width:600px) hides spark wraps."
  },
  {
    "id": "T033",
    "title": "enemy AI territory expansion and counterattacks",
    "description": "Enemies periodically expand into neutral tiles (every 30–60s) and occasionally counterattack player border tiles (every 90–150s). Enemy power scales with game tick. Creates defensive pressure on the player.",
    "priority": 33,
    "status": "done",
    "phase": 9,
    "notes": "Created src/systems/enemyAI.js with two behaviours: _expandEnemies() picks a random enemy tile, finds an adjacent neutral tile, and captures it (interval 30–60s / 120–240 ticks); _counterattack() finds player border tiles adjacent to enemy tiles and probabilistically captures one (win chance = min(0.5, enemyPower/(enemyPower+playerDefense)); interval 90–150s / 360–600 ticks). Enemy power = 30 + tick/100 so pressure scales with game age. Capital tile is immune to counterattack. On enemy capture: one player unit is lost, recalcRates() called, MAP_CHANGED + UNIT_CHANGED emitted, 'raid' log message shown. initEnemyAI() and enemyAITick() wired into main.js (registered as 8th tick system, called in boot and _newGame). state.enemyAI = {nextExpansionTick, nextAttackTick} added to state.js and initState(); saved/loaded in main.js (save version 10)."
  },
  {
    "id": "T035",
    "title": "difficulty presets (Easy / Normal / Hard)",
    "description": "Three difficulty modes selectable in Settings. Easy: +50% starting resources, raids/disasters 40% weaker, enemy AI 50% slower. Normal: unchanged. Hard: -25% starting resources, raids/disasters 50% stronger, enemy AI 30% faster. Difficulty persists across new games. Starting-resource bonus/penalty applied only at New Game. All other effects apply immediately on change.",
    "priority": 35,
    "status": "done",
    "phase": 9,
    "notes": "Added DIFFICULTY_CHANGED event to events.js. Added state.difficulty='normal' field to state.js (not reset by initState — persists across new games). randomEvents.js: added _difficultyRaidMult() (0.6/1.0/1.5) and _disasterDuration() (40s/60s/90s in ticks); all raid events multiply losses by raidMult; all disaster events use _disasterDuration() and report the actual seconds in the log message. enemyAI.js: added _intervalMult() (1.5/1.0/0.7); _nextExpand() and _nextAttack() multiply their interval by _intervalMult() so Easy enemies expand/attack slower, Hard enemies act faster. settingsPanel.js: imported state and emit/Events; added _difficultySection() showing 3 buttons (🌿 Easy / ⚔️ Normal / 💀 Hard) with active highlight and descriptive text; wired click handlers that set state.difficulty and emit DIFFICULTY_CHANGED. main.js: added _applyDifficultyStart() (×1.5 or ×0.75 on gold/food/wood with floor 10); called in both the first-boot new-game path and _newGame(); difficulty saved in payload and restored in _applySave(). CSS: .difficulty-buttons flex wrapper; .btn--difficulty / .btn--difficulty-active (mirrors speed-button styling)."
  },
  {
    "id": "T036",
    "title": "extended tech tree (6 new technologies)",
    "description": "Add Navigation (+50% trade route income, requires tradeRoutes), Alchemy (+75% mana well, +25% quarry, requires arcane), Siege Craft (+75% attack power in combat, requires steel), Fortification (-40% enemy counterattack success, requires engineering), Economics (+50% market gold, +500 gold cap, requires tradeRoutes), Divine Favor (+30% farm food, +30% mana well, requires alchemy). Wire all effects into resources.js, combat.js, and enemyAI.js.",
    "priority": 36,
    "status": "done",
    "phase": 9,
    "notes": "Added 6 new tech definitions to data/techs.js: navigation (⚓, requires tradeRoutes, +50% trade route income), alchemy (⚗️, requires arcane, +75% mana well +25% quarry), siege_craft (🏹, requires steel, +75% attack power), fortification (🛡️, requires engineering, -40% enemy counterattack success), economics (📈, requires tradeRoutes, +50% market gold +500 gold cap), divine_favor (✨, requires alchemy, +30% farm food +30% mana well). systems/resources.js: _buildingProdMultiplier() extended to apply divine_favor ×1.3 to farm, alchemy ×1.25 to quarry, economics ×1.5 to market (stacks on tradeRoutes ×1.75), alchemy ×1.75 to manaWell (stacks on arcane ×2.0), divine_favor ×1.3 to manaWell; economics cap bonus (caps.gold += 500) added before territory bonus; trade route loop updated to multiply by navMult (1.5 if navigation researched, 1.0 otherwise); getBreakdown() trade route section updated to match. systems/combat.js: added if (state.techs.siege_craft) attackPower *= 1.75 after existing tech multipliers. systems/enemyAI.js: changed winChance to let; added if (state.techs?.fortification) winChance *= 0.6 after initial calculation. All 6 techs display correctly in the existing researchPanel UI with no changes needed (auto-renders from TECHS data)."
  },
  {
    "id": "T034",
    "title": "resource rate breakdown tooltip on HUD hover",
    "description": "Hover over any HUD resource cell to see a floating popup showing the full rate breakdown: buildings, territory, trade routes, season modifier, unit upkeep, and active disasters.",
    "priority": 34,
    "status": "done",
    "phase": 9,
    "notes": "Added getBreakdown(resId) export to systems/resources.js: mirrors recalcRates() logic to compute per-source contributions (baseline, buildings with age+tech mults, territory, trade routes, unit upkeep, hero upkeep), season multiplier as a modifier, and active disaster modifiers; returns {lines, seasonMult, seasonName, disasters, total}. Updated ui/hud.js: imported getBreakdown; added _tooltipEl module var + _createTooltip() (appends fixed #hud-tooltip div to body); _showTooltip(resId, anchorEl) calls getBreakdown, builds innerHTML with header, per-line rows (filtered <0.005), season modifier badge, disaster badges, divider, and net-rate total row; uses rAF to measure tooltip width and clamp position within viewport below the hovered cell; _hideTooltip() adds hud-tooltip--hidden class. initHUD() attaches mouseenter/mouseleave listeners to each resource div. Removed title= attr from resource divs (superseded by tooltip). CSS: .hud-tooltip (fixed, z-800, bg-card, border-h, shadow, pointer-events:none), .hud-tooltip--hidden (display:none), .hud-tt__header/row/label/val/val--pos/val--neg/modifier/modifier--disaster/divider/total; .hud__resource cursor:help."
  },
  {
    "id": "T037",
    "title": "map resource overlay mode",
    "description": "Add a toggle button on the map panel to switch between normal terrain view and a resource overlay that colors tiles by their primary resource contribution (food/wood/stone/iron/gold). Updates the map legend to match the active mode.",
    "priority": 37,
    "status": "done",
    "phase": 10,
    "notes": "Added _overlayMode boolean to mapPanel.js. Added OVERLAY_COLOR map (grass→amber gold, forest→deep green, hills→slate, river→dark teal, mountain→rust) and OVERLAY_LEGEND array. _buildHTML() now emits a .map-controls div with a '🌾 Resources' toggle button (#map-overlay-toggle). Toggle click flips _overlayMode, swaps button label to '🗺️ Terrain', adds .btn--map-overlay-active class, calls _updateLegend() which replaces #map-legend innerHTML with either terrain or resource swatches+labels. _drawTile() selects OVERLAY_COLOR vs TERRAIN_COLOR based on _overlayMode — no other logic changes needed. CSS: .map-controls flex wrapper + margin; .btn--map-overlay default (dim border/text); .btn--map-overlay-active (green border/text + faint green bg)."
  },
  {
    "id": "T038",
    "title": "map tile hover tooltip",
    "description": "Show a floating HTML tooltip when hovering over revealed tiles on the map. Tooltip displays terrain type, ownership (your territory / enemy / neutral), resource bonus/s, defense rating, and 'click to attack' hint for attackable tiles.",
    "priority": 38,
    "status": "done",
    "phase": 10,
    "notes": "Added _tileTipEl module variable. _createTileTip() creates a #map-tile-tip div and appends to body (idempotent: reuses existing if already present — handles panel re-init). _showTileTip(tile, x, y, mouseX, mouseY) builds tooltip HTML: terrain name title, owner badge (blue=player / red=enemy / neutral=dim), resource bonus row (amber accent), defense value row, 'click to attack' action row when tile is attackable. Positions the tooltip below-right of cursor clamped to viewport (150×110 estimate). _hideTileTip() adds .map-tile-tip--hidden. _onMousemove now calls _showTileTip for revealed tiles and _hideTileTip otherwise; _onMouseleave calls _hideTileTip. CSS: .map-tile-tip (fixed, z-820, bg-card, border-h, shadow, pointer-events:none, min-width 130px); .map-tile-tip--hidden (display:none); .map-tt-title/.map-tt-row/.map-tt-bonus/.map-tt-owner (--player blue / --enemy red)/.map-tt-action (accent-h, bold)."
  },
  {
    "id": "T039",
    "title": "unit experience and promotion system",
    "description": "Units gain 1 XP per combat victory. At 3 XP promote to Veteran (×1.5 attack), at 6 XP promote to Elite (×2.0 attack). XP is per unit type. Rank badges shown in military panel and army summary.",
    "priority": 39,
    "status": "done",
    "phase": 10,
    "notes": "Added VETERAN_XP=3/ELITE_XP=6 constants and _rankMult()/helpers to combat.js. _grantCombatXP() awards 1 XP to all participating unit types on victory; promotes rank when threshold crossed and emits UNIT_CHANGED. attackTile() applies _rankMult() when summing attack power so veteran/elite units deal more damage in combat. militaryPanel.js: added _rankMult()/_rankBadge() helpers; _armySection() shows ★/★★ badges and rank-adjusted power; _unitCard() shows rank badge + XP progress line (XP: n/threshold → NextRank). summaryPanel.js _militaryCard(): applied _rankMult() to per-unit attack power display; added _rankBadge() inline. state.js: added unitXP: {} and unitRanks: {} fields; both reset in initState(). main.js: unitXP+unitRanks saved/loaded; save version bumped to 11. CSS: .rank-badge (base), .rank-badge--veteran (blue tint), .rank-badge--elite (gold tint), .unit-card__xp (dim info line)."
  },
  {
    "id": "T040",
    "title": "empire advisor tips panel",
    "description": "Smart advisor card in the Empire Summary tab that analyses the current game state and shows up to 4 contextual tips (warnings, info, positive). Tips cover: food/gold draining, multiple wars, age advance ready, no military, available research, resource near cap, small territory.",
    "priority": 40,
    "status": "done",
    "phase": 10,
    "notes": "Added TECHS import to summaryPanel.js. Added _generateTips() which checks: food rate <-0.3 (warn), gold rate <-0.5 and <200 (warn), ≥2 wars (warn), next age requirements met (ok/green), zero units after 30s (info), idle affordable research (info), any resource ≥90% cap (info), territory <6 after 2min (info). Returns up to 4 tips sorted warn-first. Added _advisorCard() which renders tips as .sum-advisor-tip divs with level modifier classes; shows '✅ flourishing' message when no tips. Advisor card rendered first in summary-grid so it appears top-left. CSS: .sum-advisor-tip (flex, border-left 3px, rounded), --warn (red tint), --info (blue tint), --ok (green tint)."
  },
  {
    "id": "T041",
    "title": "new game wizard modal",
    "description": "Replace native browser confirm()/prompt() dialogs for New Game with a styled modal. Modal has: empire name text input, difficulty button group (Easy/Normal/Hard with descriptions), 'Found Empire' submit button, Cancel button. Also fix the duplicate id bug on the empire-name element that prevents rename from working.",
    "priority": 41,
    "status": "done",
    "phase": 11,
    "notes": "Created src/ui/newGameModal.js: showNewGameWizard(currentDifficulty, onConfirm) builds a #new-game-modal overlay reusing existing .modal-overlay/.modal-box/.modal-close/.btn--difficulty classes; empire name <input> (max 40 chars, placeholder 'My Empire'); difficulty button group (Easy/Normal/Hard) with live description text; '⚔️ Found Empire' gold submit button; Cancel + Esc + click-outside close; Enter in name field submits. index.html: fixed duplicate id bug on empire-name span (removed duplicate id='btn-empire-name', kept id='empire-name', added class='empire-name-btn' for cursor/hover styling). main.js: imported showNewGameWizard; btn-new-game handler calls showNewGameWizard(state.difficulty, opts => _newGame(opts)); _newGame() extended to accept optional {name, difficulty} — applies difficulty to state before initState(); updates #empire-name text after init; empire rename binding fixed to use getElementById('empire-name'). CSS: .ng-modal-box width; .ng-field/.ng-label/.ng-input/.ng-diff-desc/.ng-warning layout; .btn--ng-start (gold); .empire-name-btn cursor+dashed-underline hint."
  },
  {
    "id": "T043",
    "title": "multi-line resource trend chart in Empire Summary",
    "description": "Add a full-width 'Resource Trends' card at the bottom of the Empire Summary tab. Render a multi-line SVG chart with 6 independently-normalised polylines (one per resource, 60-second rolling history reused from hud.js sparklines). Include a colour-coded legend and a caption. Export getResourceHistory() from hud.js so summaryPanel.js can access the ring buffers without circular imports.",
    "priority": 43,
    "status": "done",
    "phase": 12,
    "notes": "Exported getResourceHistory() from ui/hud.js (returns live _history ring-buffer reference). Added RES_CHART_COLORS constant and _chartCard() function to ui/summaryPanel.js: builds a 300×80 SVG with 3 subtle grid lines and 6 independently-normalised polylines (one per resource, flat line at 50% when constant); appended to summary-grid via _render(). Added getResourceHistory import in summaryPanel.js. CSS: .summary-card--wide (grid-column:1/-1), .sum-chart__svg (bg-card border block), .sum-chart__legend (flex-wrap 11px colour-dot + label), .sum-chart__item/.sum-chart__dot (8px circle), .sum-chart__caption (10px right-aligned)."
  },
  {
    "id": "T044",
    "title": "resource shortage alerts — HUD threshold warnings",
    "description": "Let players set a per-resource numeric threshold in the Settings panel. When a resource value drops to or below its threshold, its HUD cell pulses with a red glow. Thresholds persist across new games (user preference). Config UI lives in a new 'Resource Alerts' section in the Settings tab. Save version bumped to 12.",
    "priority": 44,
    "status": "done",
    "phase": 12,
    "notes": "state.js: added alerts:{} (not in initState — persists across new games). ui/hud.js renderHUD(): added per-resource alert check — reads state.alerts[r.id]; toggles .hud__resource--alert class when resource value ≤ threshold. ui/settingsPanel.js: added _ALERT_RESOURCES constant and _alertsSection() rendering a 6-cell .alerts-grid with per-resource number inputs (placeholder='off', step=10); injected into _render() between Shortcuts and Save Portability sections; bound .alert-input change handlers that write/delete entries in state.alerts. main.js: added alerts to _save() payload and state.alerts = s.alerts ?? {} in _applySave(); save version bumped to 12. CSS: .hud__resource--alert (red border + alert-pulse keyframe glow 1.4s), .alerts-grid (2-col grid), .alert-row (flex bg-card), .alert-label, .alert-input (64px number field)."
  },
  {
    "id": "T045",
    "title": "combat preview modal",
    "description": "Before attacking a tile, show a confirmation modal with attack power, enemy defense, win probability (%), and expected loot on victory. Confirm/Cancel buttons; Enter confirms, Escape cancels. Prevents misclicks and adds tactical transparency.",
    "priority": 45,
    "status": "done",
    "phase": 13,
    "notes": "Added getAttackPreview(x,y) to systems/combat.js — mirrors attackTile() power calculation with no side effects; returns {valid, attackPower, defense, winChance, loot, terrain, owner, siegeActive}. ui/mapPanel.js: imported getAttackPreview; added _previewEl/_previewTarget module vars; _onClick now calls _showCombatPreview(x,y) instead of attackTile directly; _createCombatPreview() creates persistent #combat-preview overlay (idempotent); _showCombatPreview() builds modal HTML with terrain/owner subtitle, 3-stat row (attack/defense/win%), win% colour-coded green/amber/red, Siege Master and Battle Cry active-effect notices, loot-on-victory row; ⚔️ Attack and ✕ Cancel buttons; Enter/Escape keyboard shortcuts (stopPropagation to avoid double-firing); _hideCombatPreview() clears _previewTarget and hides overlay. CSS: .combat-preview (fixed overlay, backdrop-filter blur), .combat-preview--hidden, .cp-box (pop-in animation, border-h, shadow), .cp-header/.cp-sub/.cp-stats/.cp-stat/.cp-stat__label/.cp-stat__value, .cp-siege-notice (gold badge), .cp-loot-row/.cp-loot-item/.cp-loot-none, .cp-actions, .btn--cp-attack (red gradient), .cp-hint."
  },
  {
    "id": "T046",
    "title": "empire score system",
    "description": "Running empire score badge in the title bar, updated on all state changes. Formula: territory×10 + buildings×3 + units×2 + techs×50 + quests×100 + goldEarned÷100 + age×200. Score breakdown card in Empire Summary tab. Score column added to the leaderboard.",
    "priority": 46,
    "status": "done",
    "phase": 13,
    "notes": "Created src/utils/score.js: calcScore() returns total score integer derived from state; getScoreBreakdown() returns [{label, value, detail}] array for display. Formula: territory×10 + buildings×3 + units×2 + techs×50 + quests×100 + floor(goldEarned÷100) + age×200. index.html: added <span id='score-badge'> in title bar between season-badge and controls. main.js: imports calcScore; _updateScoreBadge() updates badge text and title tooltip; subscribed to RESOURCE_CHANGED/BUILDING_CHANGED/UNIT_CHANGED/TECH_CHANGED/AGE_CHANGED/MAP_CHANGED/QUEST_COMPLETED; _updateScoreBadge() called in _newGame(); score added to leaderboard entry as s.score field. summaryPanel.js: imports calcScore+getScoreBreakdown; added _scoreCard() rendering a .sum-score-rows grid with per-component rows (label, detail, value) filtered to non-zero, total row; injected after _advisorCard() in _render(). settingsPanel.js: leaderboard row replaced Gold Earned column with Score (⭐) column; lb-header updated; score formatted as s.score.toLocaleString() with '—' fallback for legacy entries. CSS: #score-badge (gold accent, pill-shape, cursor:help); .sum-score-row/.sum-score-label/.sum-score-detail/.sum-score-value/.sum-score-total/.sum-score-total-value; lb-table grid-template-columns updated for new 6-col layout (rank/name/score/territory/age/date); .lb-score gold colour."
  },
  {
    "id": "T047",
    "title": "research queue UI with multi-item queueing and cancel",
    "description": "Cap research queue at 3 items. Show all queued items below the active progress bar with position numbers and ✕ Cancel buttons. cancelResearch(techId) refunds cost. Queue-full feedback on startResearch() when limit reached.",
    "priority": 47,
    "status": "done",
    "phase": 14,
    "notes": "Added MAX_RESEARCH_QUEUE=3 export to systems/research.js; startResearch() rejects when queue length ≥ 3. Added cancelResearch(techId): removes entry from queue by index, refunds full cost regardless of progress, emits TECH_CHANGED+RESOURCE_CHANGED, logs cancellation. ui/researchPanel.js: imported cancelResearch+MAX_RESEARCH_QUEUE; replaced single-item active bar with _queueSection() helper that renders a .rq-section card with queue header (count / max), then one .rq-item per queued entry — active item shows live progress bar + seconds-left, pending items show ⏳ Pending; all items have a ✕ Cancel button (data-cancel-tech attr); click handler delegates to cancelResearch(); Research button calls startResearch() as before and now shows 'Queue full' toast on failure. CSS: .rq-section (purple border, padding), .rq-header (flex justify-between), .rq-count (pill), .rq-item / --active / --pending, .rq-pos, .rq-icon, .rq-body/.rq-name/.rq-progress-row, .rq-progress/.rq-time/.rq-pending, .rq-cancel (icon button with red hover)."
  },
  {
    "id": "T048",
    "title": "combat history log in military panel",
    "description": "Track last 20 battles in state.combatHistory. Each entry: tick, outcome (win/loss), terrain, coords, attackPower, defense, loot (wins) or unit lost (losses). Render a Combat History section at bottom of military panel showing most recent 15 entries with outcome badges, stats, and relative tick.",
    "priority": 48,
    "status": "done",
    "phase": 14,
    "notes": "state.js: added combatHistory:[] to static object + initState() reset. systems/combat.js: added MAX_HISTORY=20 constant and _recordHistory({outcome,terrain,x,y,power,defense,loot?,lost?}) helper that unshifts into state.combatHistory and trims to MAX_HISTORY; called in _victory() (with lootGained object built alongside lootParts) and _defeat() (with lost unit name). ui/militaryPanel.js: subscribed to MAP_CHANGED for history re-renders; added _combatHistorySection() rendering a .mil-history card — empty state if no battles; otherwise up to 15 .mil-hist-entry rows (--win green / --loss red left border) showing ⚔️/💀 icon, outcome label, terrain+coords, loot detail or 'Lost 1 X', attack/defense stat, and relative time (Xs/Xm ago). _combatHistorySection() injected at bottom of _render(). main.js: combatHistory added to _save() payload and _applySave() (state.combatHistory = s.combatHistory ?? []); save version bumped to 13. CSS: .mil-history (bg-panel, border), .mil-hist-count, .mil-hist-entry/--win/--loss (coloured left border), .mil-hist-icon/.mil-hist-body/.mil-hist-main/.mil-hist-outcome/--win/--loss/.mil-hist-terrain/.mil-hist-detail/.mil-hist-meta/.mil-hist-power/.mil-hist-time."
  },
  {
    "id": "T049",
    "title": "offline progress system",
    "description": "When the player returns after ≥30 seconds of real-world absence, calculate resource gains/losses for up to 8 hours offline using recalculated rates. Apply them to resources, then show a welcome-back modal with time away and per-resource delta. No interaction required — a single 'Continue →' button dismisses it.",
    "priority": 49,
    "status": "done",
    "phase": 15,
    "notes": "Created src/ui/offlineModal.js: calcOfflineProgress(saveTs, rates, resources, caps) computes elapsed = min(realElapsed, 8h), applies rate×elapsed deltas to each resource (capped at caps, floored at 0), returns {elapsed, gains} or null if <30s. showOfflineModal(elapsed, gains) renders a .modal-overlay welcome-back card with time away, per-resource ±delta rows (filtered to |delta|≥1), and a gold 'Continue →' button (reuses .btn--ng-start); Enter/Escape/click-outside all dismiss. main.js: added _pendingOffline module var; _applySave() calls calcOfflineProgress after recalcRates() and stores result; boot() calls showOfflineModal after all UI panels are ready. CSS: .offline-modal-box (narrow 340px modal), .offline-header (icon + title/sub), .offline-moon, .offline-title/.offline-sub, .offline-gains flex column, .offline-gain-row, .offline-gain-icon/.name/.val, .offline-gain--pos (green)/.offline-gain--neg (red), .offline-no-change."
  },
  {
    "id": "T050",
    "title": "mini-map thumbnail in title bar",
    "description": "Add a 60×60 px canvas thumbnail to the title bar showing a bird's-eye view of the 20×20 map. Player tiles = blue, enemy = red, explored neutral = gray, unexplored = very dark, capital = gold. Updates on MAP_CHANGED. Hidden on narrow screens (≤600 px).",
    "priority": 50,
    "status": "done",
    "phase": 15,
    "notes": "Created src/ui/minimap.js: TILE_PX=3, canvas 60×60 px; drawMinimap() iterates tiles, fills each 3×3 block with CLR_UNEXPLORED/CLR_NEUTRAL/CLR_PLAYER/CLR_ENEMY; capital tile gets CLR_CAPITAL fill + 1px strokeRect border; initMinimap() attaches to #minimap canvas, registers on(MAP_CHANGED/GAME_LOADED/GAME_STARTED, drawMinimap). index.html: added <canvas id='minimap'> between season-badge and score-badge in title bar. main.js: imported initMinimap/drawMinimap; initMinimap() called in boot sequence; drawMinimap() called explicitly in _newGame() (MAP_CHANGED listener handles subsequent redraws). CSS: #minimap border, border-radius:3px, image-rendering:pixelated/crisp-edges, box-shadow; hidden on ≤600px via @media."
  },
  {
    "id": "T051",
    "title": "map tile improvements (farms, sawmills, quarries, mines, docks)",
    "description": "Let players build one improvement per captured terrain tile. Grass→Farm (+2 food/s, cost 40 wood+20 stone), Forest→Sawmill (+2 wood/s, cost 40 stone+30 gold), Hills→Quarry (+1 stone/s, cost 50 gold+30 wood), Mountain→Mine (+1 iron/s, cost 60 gold+40 wood), River→Dock (+1 gold/s+1 food/s, cost 50 wood+30 stone). Improvements shown as canvas icons. Picker modal on player-tile click. Tile tooltip updated. Enemy capturing tile destroys improvement.",
    "priority": 51,
    "status": "done",
    "phase": 16,
    "notes": "Created src/data/improvements.js: IMPROVEMENTS keyed by terrain type (grass/forest/hills/mountain/river) with id, icon, name, desc, cost, production fields. systems/map.js: imported IMPROVEMENTS; territoryRateBonus() extended to sum tile.improvement production rates after base terrain bonus (looks up IMPROVEMENTS[tile.type], matches by impDef.id === tile.improvement). core/actions.js: imported IMPROVEMENTS; added buildTileImprovement(x,y) — validates player ownership, not capital, no existing improvement, affordability; deducts cost, sets tile.improvement, calls recalcRates(), emits MAP_CHANGED+RESOURCE_CHANGED, logs build message. systems/enemyAI.js: _counterattack() clears tile.improvement on successful enemy capture. ui/mapPanel.js: imported buildTileImprovement+addMessage from actions.js, IMPROVEMENTS; added _impPickerEl module var; _showTileTip() now renders improvement icon+desc when tile has improvement, or '🏗️ Click to build X' hint when improvable; _drawTile() renders improvement icon via _drawIcon() on player-owned improved tiles (else-if chain after capital/enemy); _onClick() shows _showImprovementPicker(x,y,tile) when player-owned non-capital tile clicked; added _createImprovementPicker()/_showImprovementPicker()/_hideImprovementPicker() — idempotent modal creation, delegated click (data-imp-build attr), Escape key handler; shows info-only card when already improved, build card with cost colour-coding (green=can afford, red=cannot) and disabled Build button when insufficient resources. main.js: save version bumped to 14; formation and tile.improvement field persisted (improvement is auto-saved as part of state.map.tiles). CSS: .imp-picker (fixed overlay, z-810), .imp-picker--hidden, .imp-box (pop-in, border-h), .imp-header/.imp-sub, .imp-card (flex card), .imp-icon/.imp-body/.imp-name/.imp-effect/.imp-costs, .imp-cost--ok (green)/.imp-cost--bad (red), .imp-built/.imp-note, .imp-actions, .imp-hint, .btn--imp-build (green gradient)."
  },
  {
    "id": "T052",
    "title": "battle formations (Defensive / Balanced / Aggressive)",
    "description": "Add a formation selector to the military panel with three stances: Defensive (-15% attack, -30% enemy counterattack success), Balanced (no modifier), Aggressive (+25% attack, +25% enemy counterattack success). Formation persists across new games. Applied in combat.js attackTile()/getAttackPreview() and enemyAI.js counterattack(). Save/load formation field.",
    "priority": 52,
    "status": "done",
    "phase": 16,
    "notes": "core/state.js: added formation:'balanced' field (not reset by initState — persists across new games). core/actions.js: added setFormation(type) — validates against VALID_FORMATIONS array, sets state.formation, emits UNIT_CHANGED (triggers mil panel re-render), logs message. systems/combat.js: added FORMATION_ATTACK={defensive:0.85,balanced:1.0,aggressive:1.25} and _formationAttackMult() helper; applied attackPower*=_formationAttackMult() in both attackTile() and getAttackPreview() (after tech multipliers, before hero bonus); getAttackPreview() returns formation field in result. systems/enemyAI.js: in _counterattack(), reads state.formation; if defensive winChance*=0.70; if aggressive winChance*=1.25; winChance capped at 0.9. ui/militaryPanel.js: imported setFormation; added FORMATIONS array constant (3 objects with id/icon/label/desc); added _formationSection() rendering .formation-section card with section title, 3 .btn--formation buttons (active=.btn--formation-active), .formation-desc showing active stance description; _formationSection() injected first in _render(); _handleClick() checks data-formation attr before other handlers. ui/mapPanel.js: _showCombatPreview() renders formation notice badge when formation is non-balanced (dim text, shows modifier label). main.js: save version 14; added formation:state.formation??'balanced' to _save() payload; state.formation=s.formation??'balanced' in _applySave(). CSS: .formation-section (bg-card, border, radius, padding), .formation-title (uppercase dim label), .formation-buttons flex wrap, .btn--formation/--formation-active (active=gold border+tint+weight), .formation-desc (dim 11px), @media ≤480px collapses to column."
  },
  {
    "id": "T053",
    "title": "per-faction enemy tile coloring on the map",
    "description": "Assign each initial enemy settlement to one of the 3 AI empires (Iron Horde / Mage Council / Sea Wolves) based on angular sector from the player capital. Tag each enemy tile with a faction property. Propagate faction when enemies expand. Clear faction on player capture. Render each empire's tiles in a distinct color on the canvas (Iron Horde=orange, Mage Council=purple, Sea Wolves=teal). Update map legend with faction swatches. Show faction name in tile hover tooltip. Update minimap to use faction colors.",
    "priority": 53,
    "status": "done",
    "phase": 17,
    "notes": "Added _getFaction(x,y) to systems/map.js: divides map into 3 angular sectors (~120° each) from the capital; assigns tiles[y][x].faction='ironHorde'|'mageCouncil'|'seaWolves' in _placeEnemies(). systems/enemyAI.js: imported EMPIRES; _expandEnemies() propagates tiles[ny][nx].faction = tiles[y][x].faction ?? null to newly captured neutral tiles; _counterattack() identifies attackingFaction from adjacent enemy tile and assigns it to captured tile; log message now includes empire icon+name ('⚔️ Iron Horde seized...' etc). systems/combat.js: _victory() sets tile.faction = null on player capture. ui/mapPanel.js: imported EMPIRES; added FACTION_TINT/FACTION_BORDER/FACTION_LEGEND constants (iron=orange, mage=purple, wolf=teal); _drawTile() uses faction tint+border for enemy tiles when faction known; _showTileTip() builds factionLabel from EMPIRES for enemy tooltip owner row; _buildHTML()/_updateLegend() now append faction swatches (circular .map-legend__swatch--faction) after terrain items. ui/minimap.js: added CLR_FACTION map; enemy tile color selects faction color over generic CLR_ENEMY when available. styles/main.css: added .map-legend__swatch--faction (border-radius:50% to distinguish from terrain squares). No save version bump needed — faction is part of tile data already persisted in state.map.tiles."
  },
  {
    "id": "T054",
    "title": "diplomatic history log in diplomacy panel",
    "description": "Track all diplomatic events in state.diplomacy.history (max 25 newest-first). Events: alliance formed/broken, trade route opened/closed, war declared, peace signed, AI-initiated stance changes, war raids. Show a scrollable history section at the bottom of the Diplomacy panel with type-coloured icons, event text, and relative time. Save/load history as part of diplomacy state.",
    "priority": 54,
    "status": "done",
    "phase": 17,
    "notes": "systems/diplomacy.js: added _logDiplomacy(empireId, type, text) helper (unshifts into state.diplomacy.history, caps at 25 entries); called in proposeAlliance (type='alliance'), openTradeRoute (type='trade'), closeTradeRoute (type='trade'), declareWar (type='war'), proposePeace (type='peace'), _warRaid (type='raid'), and all three _aiAction branches (type='war'|'alliance'|'peace'|'ai'). initDiplomacy() initialises history:[] on new game and adds migration guard (if (!state.diplomacy.history) state.diplomacy.history = []) for saves without it. history is persisted automatically since diplomacy:state.diplomacy is already in the save payload — no version bump needed. ui/diplomacyPanel.js: added HIST_ICON constant map; _historySection() renders .dipl-history card with up to 15 entries; each entry shows type icon, optional empire icon+name prefix, event text, and relative time (_relativeTime helper converts tick delta to Xs/Xm/Xh ago); injected into _render() after .dipl-empire-list. styles/main.css: .dipl-history (card wrapper, overflow hidden), .dipl-history__header (uppercase label bar), .dipl-hist-entry/--alliance/--trade/--war/--peace/--raid/--ai (flex row, coloured left border per type), .dipl-hist-icon/.dipl-hist-body/.dipl-hist-text/.dipl-hist-time; mobile: compact padding override."
  },
  {
    "id": "T055",
    "title": "mana spells system",
    "description": "4 castable spells that consume mana: Vision (🔭, 15 mana, always) reveals 6-tile radius around capital; Blessing (🌟, 40 mana, requires arcane) +60% food+gold for 90s; Aegis Ward (🛡️, 55 mana, requires engineering) −40% enemy counterattack for 120s; Mana Bolt (⚡, 80 mana, requires divine_favor) primed guaranteed-win on next attack. Spells section in Military panel. Effects wired into resources.js (blessing), combat.js (manaBolt), enemyAI.js (aegis). spellTick() handles expiration. SPELL_CAST event. Save version 15.",
    "priority": 55,
    "status": "done",
    "phase": 18,
    "notes": "Created src/systems/spells.js: SPELLS catalogue (4 entries), SPELL_ORDER array, initSpells() (idempotent struct guard), castSpell(id) validates unlock/cooldown/mana/duplicate-active then applies effect; _castVision() reveals 6-tile radius around capital via MAP_CHANGED; spellTick() expires blessing/aegis at their expiresAt tick. Added SPELL_CAST event to events.js. state.js: added spells:null and barbarians:null fields; initState() resets both. systems/resources.js: recalcRates() applies ×1.6 to food+gold when blessing active (after disaster modifiers). systems/combat.js: getAttackPreview() returns manaBoltActive flag; attackTile() checks manaBolt after Siege Master — consumes it for guaranteed win (defence=0, siegeActive=true), emits SPELL_CAST. systems/enemyAI.js: winChance *= 0.6 when aegis active. ui/militaryPanel.js: imported castSpell+SPELLS+SPELL_ORDER; added _spellsSection() rendering .spells-section card with 4 .spell-card entries (locked/cost/cd/active states); injected second in _render() after formation; _handleClick() intercepts [data-cast-spell]; SPELL_CAST event triggers re-render; TICK handler extended to also detect spell CD/active activity. ui/mapPanel.js: combat preview now shows manaBoltHtml notice; ownerStr shows '💀 Barbarian Camp' for barbarian owner (T055 side). main.js: imports initSpells+spellTick; registerSystem(spellTick); initSpells() in boot+_newGame; spells:state.spells in _save(); state.spells=s.spells??null in _applySave(); save version bumped to 15. CSS: .spells-section (purple left-border accent), .spells-title, .spell-grid (auto-fill 175px), .spell-card/.spell-card--locked, .spell-card__header/icon/name/cost, .spell-cost--ok/--bad, .spell-card__desc, .spell-status/--locked/--active/--cd, .btn--spell (blue gradient), .log-entry--barbarian (for T056)."
  },
  {
    "id": "T056",
    "title": "barbarian encampments on neutral tiles",
    "description": "Neutral revealed tiles periodically (every 45–90s) become Barbarian Camps (tile.owner='barbarian', max 5 at once). Camps get +20–35 defense and 2× loot. Visual: dark maroon tint + skull icon on canvas, distinct minimap color. Tile tooltip shows '⚔️ Barbarian Camp'. Combat preview shows 'Barbarian' owner. Capturing a camp gives the player territory + doubled loot. Enemy AI expansion can clear camps. barbarianTick() system registered in main.js. state.barbarians saved/loaded.",
    "priority": 56,
    "status": "done",
    "phase": 18,
    "notes": "Created src/systems/barbarianCamps.js: MAX_CAMPS=5, SPAWN_MIN=180 ticks (45s)/SPAWN_MAX=360 ticks (90s); initBarbarians() seeds state.barbarians.nextSpawnTick; barbarianTick() fires when tick≥nextSpawnTick, counts existing camps (tile.owner==='barbarian'), calls _spawnCamp() if under max; _spawnCamp() gathers neutral+revealed tiles ≥4 tiles from capital, picks from top-40% farthest, sets tile.owner='barbarian', boosts defense +20–35, stores barbDefenseBase, doubles loot (seeds from BASE_LOOT if tile.loot empty), logs raid-type message. clearBarbarianCamp(tile) restores defence and removes barbDefenseBase (used by enemy AI). systems/combat.js: _victory() checks wasBarbarian before setting tile.owner='player'; clears barbDefenseBase metadata; uses different log message '💀 Barbarian camp cleared'. systems/enemyAI.js: imported clearBarbarianCamp; _expandEnemies() now allows expanding into 'barbarian' tiles (as well as null); calls clearBarbarianCamp before claiming. ui/mapPanel.js: added BARBARIAN_TINT/BARBARIAN_BORDER constants; _drawTile() applies maroon tint+border; barbarian icon '💀' drawn before enemy sword; _showTileTip() shows '💀 Barbarian Camp' owner and loot-on-capture row; improvement hint suppressed for barbarian tiles; combat preview ownerStr shows '💀 Barbarian Camp'; _updateStats() counts barbarian camps and appends '💀 Camps: N' to stats bar. ui/minimap.js: CLR_BARBARIAN='#7a1c0a'; drawMinimap() branch for barbarian. main.js: imports initBarbarians+barbarianTick; registerSystem(barbarianTick); initBarbarians() in boot+_newGame; barbarians:state.barbarians in _save(); state.barbarians=s.barbarians??null in _applySave() (version 15 covers both T055+T056)."
  },
  {
    "id": "T057",
    "title": "army morale system",
    "description": "Morale gauge (0–100) that drifts based on wars (-0.012/tick/war), alliances (+0.006/tick/ally), season (Winter -0.010/tick, Spring +0.006/tick), and barbarian camps (-0.003/tick/camp). Flat changes: +5 on combat win, -8 on loss, -3 on enemy capturing player tile, +10 on age advance. Combat effects: morale ≥80 → +15% attack (Inspired), morale <25 → -20% attack (Demoralized). Desertion: if morale <15, 0.05%/tick chance to lose 1 unit. Morale gauge in military panel with color-coded fill bar, tier labels, active effect line, and modifier list. MORALE_CHANGED event. save version 16.",
    "priority": 57,
    "status": "done",
    "phase": 19,
    "notes": "Created src/systems/morale.js: MORALE_MAX/MIN constants; MORALE_COMBAT_WIN(+5)/MORALE_COMBAT_LOSS(-8)/MORALE_TILE_LOST(-3)/MORALE_AGE_ADVANCE(+10) exports; initMorale() seeds state.morale=50 + subscribes AGE_CHANGED once (module-level guard); changeMorale(delta) clamps+emits MORALE_CHANGED; getMoraleEffect() returns 1.15/1.0/0.80 based on thresholds; getMoraleLabel() returns tier string; moraleTick() applies per-tick drift from wars/alliances/season/camps, emits on 5-point bucket crossings, checks desertion. src/core/state.js: added morale:50 field + reset in initState(). src/core/events.js: added MORALE_CHANGED:'moraleChanged'. src/systems/combat.js: imported getMoraleEffect/changeMorale/constants; applied getMoraleEffect() multiplier in both attackTile() (after formation) and getAttackPreview() (same spot); _victory() calls changeMorale(MORALE_COMBAT_WIN); _defeat() calls changeMorale(MORALE_COMBAT_LOSS); getAttackPreview() returns morale field. src/systems/enemyAI.js: imported changeMorale/MORALE_TILE_LOST; _counterattack() success branch calls changeMorale(MORALE_TILE_LOST). ui/militaryPanel.js: imported getMoraleLabel/getMoraleEffect; subscribed MORALE_CHANGED; injected _moraleSection() between formation and spells; _moraleSection() renders purple-accented card with gauge bar (color transitions red→amber→green), tier badge (5 CSS modifiers), morale/100 counter, combat effect line, and modifier pills (wars/alliances/season/desertion-risk). src/main.js: imported initMorale/moraleTick; registerSystem(moraleTick) 11th system; initMorale() in boot+_newGame; morale:state.morale??50 in _save(); state.morale=s.morale??50 in _applySave(); save version bumped to 16. styles/main.css: .morale-section (purple left-border accent), .morale-header/.morale-title, .morale-label/--inspired/--confident/--steady/--demoralized/--broken (tier pills), .morale-bar-wrap/.morale-bar (transition), .morale-value, .morale-effect/--bonus/--penalty, .morale-mods/.morale-mod."
  },
  {
    "id": "T058",
    "title": "war score and forced peace system",
    "description": "Per-empire warScore field in diplomacy state. Each combat victory on a war-faction tile: +5 warScore. When warScore ≥20 and player has 200 gold: 'Demand Surrender' button forces the empire to neutral and resets warScore. warScore resets to 0 on declareWar(). War score progress bar shown in diplomacy panel for at-war empires. No save version bump needed (warScore lives inside state.diplomacy).",
    "priority": 58,
    "status": "done",
    "phase": 19,
    "notes": "systems/diplomacy.js: exported SURRENDER_COST=200 and WAR_SCORE_THRESHOLD=20 constants; added warScore:0 to each empire entry in initDiplomacy(); migration guard (emp.warScore===undefined → 0) for old saves; emp.warScore=0 reset in declareWar() on war start; added demandSurrender(empireId) — validates war+score+gold, deducts cost, sets neutral, resets warScore, emits DIPLOMACY_CHANGED+RESOURCE_CHANGED, logs 'surrendered'. systems/combat.js: _victory() awards +5 warScore when tile.faction matches an empire at war (emit DIPLOMACY_CHANGED so diplomacy panel re-renders). ui/diplomacyPanel.js: imported demandSurrender+SURRENDER_COST+WAR_SCORE_THRESHOLD; _empireCard() builds warScoreHtml (progress bar + label + 'Surrender available!' notice) for war-state cards; Demand Surrender button rendered when ws≥threshold (red gradient .btn--surrender, disabled when no gold); _onClick() handles 'surrender' action. styles/main.css: .dipl-war-score/.dipl-ws-label/.dipl-ws-bar-wrap/.dipl-ws-bar (red→amber gradient fill, 0.4s transition); .btn--surrender/.btn--surrender:hover (deep-red gradient). No save version bump — warScore is inside state.diplomacy already persisted."
  },
  {
    "id": "T042",
    "title": "pause button and visual pause overlay",
    "description": "Add a ⏸/▶ toggle button in the title bar alongside Save and New Game. Add a semi-transparent centre-screen 'PAUSED' overlay that appears when the game is paused. Sync the button state when Space/P keyboard shortcuts are used. Clicking the button or pressing Space/P both toggle pause and update all indicators consistently.",
    "priority": 42,
    "status": "done",
    "phase": 11,
    "notes": "index.html: added <button id='btn-pause'> in .title-bar__controls before Save; added #pause-overlay div (fixed, z-500) containing .pause-box with '⏸ PAUSED' text and .pause-hint subtext. main.js: added _togglePause() which calls stopLoop()/startLoop() then _syncPauseUI(); added _syncPauseUI() which updates btn-pause text ('⏸ Pause'/'▶ Resume'), toggles .btn--paused, and shows/hides #pause-overlay + sets aria-hidden; btn-pause click handler calls _togglePause(); Space/P keyboard case now calls _togglePause() (eliminating duplicated stop/start logic inline); _syncPauseUI() called from _newGame() to reset overlay on new game. CSS: .pause-overlay (fixed inset, flex centre, pointer-events:none, z-500); .pause-overlay--hidden (display:none); .pause-box (gold border, backdrop-filter, pause-pulse keyframe 2.4s scale+opacity); .pause-hint (dim subtext); .btn--paused (gold border+text)."
  },
  {
    "id": "T059",
    "title": "population system — citizen growth, housing cap, production bonus",
    "priority": "high",
    "status": "done",
    "spec": "Add a population mechanic: state.population={count,cap}. New House building increases pop cap by 100. Pop grows when food plentiful, declines when starving. Pop contributes passive gold income and food consumption to rates. Milestone toasts at 100/250/500/1000/2000 citizens. Show pop counter in HUD.",
    "notes": "Added systems/population.js (initPopulation, populationTick, getPopulation, getPopCap). New House building in buildings.js (+100 pop cap). Population contributes +0.003 gold/s and -0.005 food/s per citizen in recalcRates(). Milestone toasts at 100/250/500/1000/2000/5000. Population badge in title bar (population-badge). POPULATION_CHANGED event added. Save version bumped to 17."
  },
  {
    "id": "T060",
    "title": "espionage system — spy missions against rival empires",
    "priority": "high",
    "status": "done",
    "spec": "Add Espionage tech (requires tradeRoutes). State: state.espionage={cooldownUntil,log}. Three missions: Gold Heist (steal gold), Sabotage (boost morale + message), Intel Gather (reveal enemy territory). 60s cooldown between missions. 60% base success, failure triggers counter-raid. UI section in Diplomacy panel.",
    "notes": "Added systems/espionage.js (initEspionage, launchMission, canLaunchMission, espionageCooldownSecs). Espionage tech in techs.js (requires tradeRoutes, 300g/80iron/100food). Three missions: gold_heist (steal 150-350g), sabotage (+8 morale / counter-raid), intel (reveal enemy tile count). 60s cooldown. Espionage section in diplomacyPanel.js with live cooldown countdown. ESPIONAGE_EVENT added to events.js."
  },
  {
    "id": "T061",
    "title": "dynamic timed challenges",
    "description": "Generate one active challenge every 3 minutes with a 2-minute completion window. Five challenge types: territory (capture N tiles), gold (accumulate N gold), combat (win N battles), population (reach N citizens), mana (gather N mana). Resource reward on success. Expired challenges replace with a new one after 30s. Challenge state shown as a card at top of Quests tab with countdown timer and reward preview. Completed challenges listed below (last 5). CHALLENGE_UPDATED event. state.challenges saved/loaded. Save version 18.",
    "priority": 61,
    "status": "done",
    "phase": 20,
    "notes": "Created src/systems/challenges.js: 5 TEMPLATES (territory/gold/combat/population/mana), each with metric()/target(cur)/label(sv,tgt)/desc(sv,tgt)/reward(tgt) functions; initChallenges() seeds state.challenges={active,completed,nextGenTick}; challengeTick() checks completion (metric()>=target) and expiry, then generates next on schedule; getChallengeSecsLeft() for countdown. CHALLENGE_UPDATED event added to events.js. state.js: challenges:null added to static object + reset in initState(). ui/questPanel.js: full rewrite to add _challengeSection() at top (active challenge card with icon/label/countdown timer/desc/reward/progress bar, or waiting card showing next-gen countdown; completed list shows last 5 entries); TICK handler refreshes countdown every second; CHALLENGE_UPDATED/POPULATION_CHANGED/RESOURCE_CHANGED events trigger re-render. main.js: imported initChallenges+challengeTick; registerSystem(challengeTick) as 13th system; initChallenges() called in boot+_newGame; challenges:state.challenges in _save(); state.challenges=s.challenges??null in _applySave(). CSS: .challenge-section (card wrapper), .challenge-header, .challenge-card/--active/--waiting, .challenge-card__top/.icon/.label/.timer/--urgent, .challenge-card__desc/.reward, .challenge-progress with progress-bar__fill--challenge (blue gradient), .challenge-completed/__header/__entry/__reward; mobile: flex-wrap timer."
  },
  {
    "id": "T062",
    "title": "empire archetype selection",
    "description": "Three archetypes chosen in New Game Wizard (added as a third section below difficulty). none/conqueror/merchant/arcane. Conqueror: +25% unit attack, -10% building costs. Merchant: +50% trade route income, +1.5 gold/s base. Arcane: x2 mana well production, -25% spell mana costs. state.archetype persists across new games. Applied in resources.js, combat.js, spells.js, actions.js. Archetype badge in Empire Summary header. Save version 19.",
    "priority": 62,
    "status": "done",
    "phase": 20,
    "notes": "Created src/data/archetypes.js: ARCHETYPES object (none/conqueror/merchant/arcane) each with id/icon/name/desc/bonusLines; ARCHETYPE_ORDER array. ui/newGameModal.js: added third 'Empire Archetype' ng-field section with 4 .btn--arch buttons (ARCHETYPE_ORDER); click handler toggles .btn--arch-active and updates #ng-arch-desc + #ng-arch-bonuses (gold bullet lines); showNewGameWizard() now accepts currentArchetype param and pre-selects it; _submit() extracts archetype from active arch button and passes in onConfirm callback. core/state.js: added archetype:'none' field (not reset in initState — persists); challenges:null field + initState reset. core/actions.js: scaledCost() applies archMult=0.9 when state.archetype==='conqueror' (−10%). systems/resources.js: +1.5 gold/s when merchant in baseline; merchantMult=1.5 applied to trade route income loop; _buildingProdMultiplier() applies ×2 for manaWell when arcane. systems/combat.js: attackPower*=1.25 when conqueror, in both getAttackPreview() and attackTile() (after morale, before hero). systems/spells.js: manaCost computed as floor(def.manaCost*0.75) when arcane, error message uses computed cost. ui/summaryPanel.js: imported ARCHETYPES; _empireHeader() renders .summary-empire-badge--arch when archetype!=none. main.js: imported showNewGameWizard already — updated call to pass state.archetype; _newGame() applies opts.archetype to state.archetype before initState; _save() adds archetype+challenges fields (version 19); _applySave() restores both. CSS: .arch-buttons flex row, .btn--arch/.btn--arch-active (gold border+tint), .ng-arch-desc/.ng-arch-bonuses/.ng-arch-bonus-line (green); .summary-empire-badge--arch (gold pill); mobile: arch buttons column."
  },
  {
    "id": "T063",
    "title": "trade caravan system",
    "description": "Random merchant caravans appear on revealed player/neutral tiles every 3-5 minutes (720-1200 ticks). Caravan stays 90s (360 ticks). Offers 3 random barter trades from a pool of 8 (sell resources for gold, buy resources with gold, or mixed bundles). Caravan tile shows 🛒 icon on map canvas. Map stats bar shows live countdown. Click caravan tile to open trade picker. CARAVAN_UPDATED event. state.caravans saved/loaded. Save version 20.",
    "priority": 63,
    "status": "done",
    "phase": 21,
    "notes": "Created src/systems/caravans.js: SPAWN_MIN_TICKS=720/SPAWN_MAX_TICKS=1200/DURATION_TICKS=360; CARAVAN_OFFERS pool of 8 barter trades (sell food/wood/stone/iron/mana for gold, buy food/wood, buy iron+mana bundle); initCaravans() seeds state.caravans={active:null,nextSpawnTick}; caravanTick() expires active caravan when tick≥expiresAt and spawns new one when cooldown expires; _spawnCaravan() finds eligible revealed non-hostile non-capital tiles, picks random spot, shuffles 3 offers from pool, logs 'windfall' toast; acceptCaravanOffer(idx) validates resources+cooldown, deducts give/adds get (capped), logs trade; getCaravanSecsLeft() returns countdown. Added CARAVAN_UPDATED to events.js. Added caravans:null to state.js (reset in initState). ui/mapPanel.js: imported acceptCaravanOffer+getCaravanSecsLeft; added CARAVAN_TINT constant; added _caravanPickerEl+_caravanTickRef module vars; _drawTile() applies gold tint and draws 🛒 icon (overrides all others) when caravan is on tile; _showTileTip() detects caravan tile and shows caravan countdown+trade action (suppresses build hint); _onClick() checks caravan tile first; _updateStats() appends '🛒 Caravan: Ns left' when active; initMapPanel() subscribes CARAVAN_UPDATED; _createCaravanPicker()/_showCaravanPicker()/_hideCaravanPicker() create and manage the trade picker modal with 3 offer cards showing give/get resources with affordability colors, Trade buttons, live countdown setInterval. main.js: imported initCaravans+caravanTick; 14th registerSystem; called in boot+_newGame; caravans+relics persisted in _save()/_applySave(); version bumped to 20. CSS: .caravan-picker overlay, .cv-box, .cv-header, .cv-sub, .cv-countdown (gold), .cv-offers list, .cv-offer (icon+body+button row), .cv-offer__icon/body/desc/exchange, .cv-res/--ok/--bad/--gain (affordability colors), .btn--caravan-trade (gold gradient), .cv-actions/.cv-hint; mobile: flex-wrap offer row."
  },
  {
    "id": "T064",
    "title": "ancient relic discovery on tile capture",
    "description": "8% chance when capturing a tile to discover an ancient relic. 6 unique relics keyed by terrain type (grass/forest/hills/river/mountain) plus 1 universal arcane shard. Each relic grants a permanent passive rate or cap bonus integrated in recalcRates(). Each relic discovered at most once per game. Toast + story entry on discovery. New Relics section in Research panel shows discovered relics with icons/bonuses; locked relics shown as '???' with terrain hint. RELIC_DISCOVERED event. state.relics saved/loaded. Save version 20.",
    "priority": 64,
    "status": "done",
    "phase": 21,
    "notes": "Created src/data/relics.js: 6 RELICS definitions (terra_idol/grass +1.5 food/s; lumber_talisman/forest +1.5 wood/s; mason_rune/hills +1.5 stone/s; river_compass/river +1 gold/s +200 gold cap; iron_crown/mountain +1.5 iron/s; arcane_shard/any +1 mana/s +150 mana cap); TERRAIN_RELIC lookup; RELIC_DROP_CHANCE=0.08; ARCANE_SHARD_DROP_CHANCE=0.04; RELIC_ORDER array. Added RELIC_DISCOVERED to events.js. Added relics:null to state.js (reset in initState). systems/combat.js: imported RELICS+TERRAIN_RELIC+drop chances; added _tryDiscoverRelic(tile,x,y) call in _victory() after morale change; _tryDiscoverRelic() rolls terrain-matched relic (8%) then independent arcane_shard roll (4%), skips capital and already-discovered relics; _grantRelic() records tick in state.relics.discovered[id], logs windfall message, calls recalcRates()+emits RELIC_DISCOVERED+RESOURCE_CHANGED. systems/resources.js: imported RELICS; added relic bonus loop in recalcRates() just before Object.assign — applies def.bonus.rates and def.bonus.caps for all discovered relics. ui/researchPanel.js: imported RELICS+RELIC_ORDER+TERRAIN_RELIC; added on(RELIC_DISCOVERED,renderResearchPanel); added _relicsSection() at bottom of renderResearchPanel() building a .relics-grid of 6 relic-card elements (--found: gold border, icon, name, desc, bonus pills; --locked: greyed, ❓ icon, terrain hint); relics-header shows count/total. systems/story.js: imported RELICS; wired on(RELIC_DISCOVERED,_onRelicDiscovered); _onRelicDiscovered adds milestoneId=relic_${id} story entry with windfall type. ui/summaryPanel.js: imported RELIC_ORDER; added 🏺 Relics row (X/6) to _progressionCard(); on(RELIC_DISCOVERED,_render). CSS: .relics-section/.relics-header/.relics-count/.relics-intro/.relics-grid (auto-fill 200px); .relic-card/--found (gold border+tint)/--locked (55% opacity); .relic-icon/.relic-body/.relic-name/.relic-desc/.relic-bonus (green)/.relic-hint; mobile: single column."
  },
  {
    "id": "T065",
    "title": "empire policies system",
    "description": "Three governance policies the player can activate (one at a time): Taxation (+25% gold, -15% food), Agrarian (+30% food, +25% pop cap, -15% gold), Martial Law (-30% training time, -8% all rates). 60s cooldown between switches. Policy selector UI in Research panel. Policy multipliers applied in recalcRates(). Morale hit on activation of harsh policies.",
    "priority": 65,
    "status": "done",
    "phase": 14,
    "notes": "Created src/data/policies.js with 3 policies: Taxation (+25% gold, -15% food, -3 morale), Agrarian (+30% food, +25% pop cap, -15% gold), Martial Law (-30% training time, -8% all rates, -5 morale). Added POLICY_CHANGED event to events.js. Added state.policy/state.policyChangedAt fields (reset in initState). Added setPolicy() to actions.js with 60s cooldown enforcement and morale hit on activation. Added trainMult (0.70) application in trainUnit() for martial_law. Applied policy rate multipliers in resources.js recalcRates() (per-resource effects + allRatesMult on positive rates). Agrarian pop cap in population.js _recalcCap(). Added _policySection() to researchPanel.js (policy header, intro, optional cooldown countdown, 3 policy cards with active badge, Activate/Deactivate buttons; re-renders on POLICY_CHANGED/MORALE_CHANGED). Added policy modifiers (policyModifiers[]) to getBreakdown() return value; rendered as .hud-tt__modifier--policy in hud.js. Save/load updated in main.js (version 21, policy + policyChangedAt persisted). CSS: .policy-section, .policy-grid, .policy-card, .policy-card--active, .policy-badge, .policy-icon/.name/.desc/.effects, .btn--policy-activate/deactivate, .hud-tt__modifier--policy."
  },
  {
    "id": "T066",
    "title": "tile fortification",
    "description": "Build defensive fortifications on player-owned map tiles. Costs 40 stone + 25 iron per tile. Adds +15 defense. Shown as a fortify option in the tile improvement picker (separate from the terrain improvement). Fortified tiles shown with a palisade indicator on the canvas. Tooltip shows fortified status. Each tile can only be fortified once.",
    "priority": 66,
    "status": "done",
    "phase": 14,
    "notes": "Added fortifyTile(x, y) to actions.js: costs 40 stone + 25 iron, sets tile.fortified=true, tile.defense+=15, emits MAP_CHANGED. Updated _showImprovementPicker() in mapPanel.js: added fortifySection (shows 'Fortified' badge or 'Fortify' button with cost) to all 3 modal states (already-improved, no-improvement, can-build); renamed modal header from 'Build Improvement' to 'Tile Actions'. Added data-fortify click handler in _createImprovementPicker(). Canvas: draw blue '▲' glyph in tile top-right corner for fortified player tiles in _drawTile(). Tile tooltip: shows '🏰' next to defense value for fortified tiles; shows 'Click → Fortify (+15 def)' hint for unfortified player tiles. enemyAI.js: clears tile.fortified+restores defense when enemy captures tile; fortified tiles get -30% enemy counterattack win chance (tile.fortified check after formation and fortification tech). CSS: .imp-fortify-row, .imp-fortify-label, .btn--fortify (blue), .imp-fortified (blue badge). Imported fortifyTile in mapPanel.js."
  },
  {
    "id": "T067",
    "title": "tribute and ceasefire system",
    "description": "Pay gold to pause war raids; demand tribute from losing enemies.",
    "priority": 67,
    "status": "done",
    "phase": 7,
    "notes": "diplomacy.js: Added TRIBUTE_COST=150, TRIBUTE_DEMAND=60, CEASEFIRE_TICKS=120*4=480, DEMAND_WARSCORE_MIN=10. payTribute(empireId): costs 150 gold, sets emp.ceasefireTick=tick+480, supresses raids. demandTribute(empireId): requires warScore>=10, grants 60 gold + sets ceasefireTick (AI pays, no raids for 30s). _warRaid() checks ceasefireTick before raiding. Migration in initDiplomacy() adds ceasefireTick:0 to older saves. diplomacyPanel.js: Imported new functions/constants. TICK throttle re-renders on active ceasefires. War card shows ceasefire badge (⏳ Xsecs) when active, or Pay Tribute + Demand Tribute buttons otherwise (Demand Tribute only visible at warScore>=10). _onClick handles payTribute/demandTribute cases. CSS: .btn--pay-tribute (dim border, blue hover), .btn--demand-tribute (gold border), .dipl-ceasefire-badge (blue pill countdown)."
  },
  {
    "id": "T068",
    "title": "garrison system",
    "description": "Station units on owned tiles for defense bonuses against enemy AI attacks.",
    "priority": 68,
    "status": "done",
    "phase": 7,
    "notes": "state.js: Added garrisons:null field, reset in initState(). events.js: GARRISON_CHANGED event. actions.js: garrisonUnit(x,y,unitId,count=1) validates ownership/non-capital/army availability/food cost (30/unit)/cap (10 total); removes from state.units, stores in state.garrisons[key]; food deducted. withdrawGarrison(x,y) returns units to army. destroyGarrison(x,y) silently destroys on enemy capture (no refund). getTotalGarrisoned() and GARRISON_MAX_TOTAL exported. resources.js: Garrison upkeep loop iterates state.garrisons values to maintain resource drain. enemyAI.js: Imports destroyGarrison; adds tileGarrison defense bonus to playerDefense before winChance calc; calls destroyGarrison on capture. mapPanel.js: Imports UNITS, garrisonUnit, withdrawGarrison, getTotalGarrisoned, GARRISON_MAX_TOTAL. GARRISON_CHANGED listener triggers re-render. _drawTile draws yellow shield+count in bottom-left of garrisoned tiles. Tile tooltip shows garrison info (unit type, count, +def). _garrisonSection() builds picker HTML (existing garrison: unit badge + Withdraw; no garrison: unit type buttons). All 3 picker states include garrisonSection. main.js: garrisons in save/load (save version 21→22). CSS: .imp-garrison container (gold-tinted border), .imp-garrison__header/current/limit/units, .btn--garrison-unit (gold hover), .btn--garrison-withdraw (red hover)."
  },
  {
    "id": "T069",
    "title": "alternative victory conditions (Diplomatic & Economic)",
    "description": "Add two additional win paths alongside the existing Conquest Victory. Diplomatic Victory: all 3 AI empires allied simultaneously. Economic Victory: 50,000 lifetime gold earned + Economics tech researched. Each path shows distinct messaging in the game-over overlay. Victory conditions checklist in Empire Summary expanded to show all three paths with live progress.",
    "priority": 69,
    "status": "done",
    "phase": 22,
    "notes": "systems/victory.js: Added DIPLOMATIC_VICTORY (all 3 empires allied) and ECONOMIC_VICTORY (50k gold earned + economics tech) checks ahead of conquest check; _trigger() now accepts victoryType param and stores it in state.gameOver; exported WIN_ECONOMIC_GOLD constant. ui/gameOverPanel.js: VICTORY_TYPES lookup dict (conquest/diplomatic/economic); renders .go-victory-type badge below title showing e.g. \"🤝 Diplomatic Victory\"; uses victory-type icon. ui/summaryPanel.js: imported WIN_ECONOMIC_GOLD; expanded _progressionCard() to show 3 separate victory-path sections each with a header and progress rows; diplomatic shows allied count/3; economic shows Economics tech status + gold progress bar. styles/main.css: .go-victory-type (gold pill badge). No save version bump needed — victoryType lives in state.gameOver which is not persisted."
  },
  {
    "id": "T070",
    "title": "hero skills system",
    "description": "Champion hero gains a skill choice every 5 combat victories (max 3 skills). 10-skill pool: Battle-Hardened (+20 atk), War Drums (+25% combat mult), Logistics (+10% rates), Treasury Guard (+0.8 gold/s), Quartermaster (+0.6 food/s), Arcane Attunement (+0.5 mana/s), Swift Training (-20% train time), Veteran Knowledge (-20% research time), Iron Will (+8 morale/win), War Profiteer (+30% loot). Skill chooser card in military panel. Skills wired into combat.js, resources.js, research.js, actions.js. HERO_LEVEL_UP event. Save version 23.",
    "priority": 70,
    "status": "done",
    "phase": 22,
    "notes": "data/hero.js: Added HERO_SKILLS (10 skills: battle_hardened/war_drums/logistics/treasury_guard/quartermaster/arcane_attunement/swift_training/veteran_knowledge/iron_will/war_profiteer), HERO_SKILL_WIN_INTERVAL=5, HERO_MAX_SKILLS=3, heroSkillBonus() pure helper. core/events.js: HERO_LEVEL_UP added. core/actions.js: recruitHero() adds skills/combatWins/pendingSkillOffer to hero state; trainUnit() applies swift_training -20% mult; added chooseHeroSkill(id). systems/combat.js: getAttackPreview() + attackTile() apply attackBonus + combatMult from hero skills; _victory() applies lootMult to loot + moraleBonus bonus from iron_will; added _trackHeroCombatWin() — increments combatWins, generates pendingSkillOffer every 5 wins up to max 3, emits HERO_LEVEL_UP. systems/resources.js: hero skill resourceRate bonuses + ratesMult applied after hero upkeep in recalcRates(). systems/research.js: veteran_knowledge researchMult applied to totalTicks. ui/militaryPanel.js: imports updated; HERO_LEVEL_UP subscribed; _heroSkillsSection() renders either skill-chooser (offer pending) or active-skills list + progress line; inject into _heroActiveSection(); _handleClick intercepts [data-choose-skill]. main.js: save version 22→23; hero migration guard adds skills/combatWins/pendingSkillOffer on old saves. styles/main.css: .hero-skills-section/--offer, .hero-skills-title, .hero-skills-chooser, .hero-skill-option/__icon/__body/__name/__desc, .btn--skill-choose, .hero-skill-active/__icon/__body/__name/__desc, .hero-skills-hint/.hero-skills-progress."
  },
  {
    "id": "T071",
    "title": "tech mastery bonus system",
    "description": "Four tech mastery groups (Military/Economic/Arcane/Agrarian). Researching all techs in a group unlocks a permanent empire-wide bonus. Military: +40 flat attack, -20% unit upkeep. Economic: +3 gold/s, +30% trade route income. Arcane: +500 mana cap, +1.5 mana/s. Agrarian: -15% building costs, +0.5 food/wood/stone per second. MASTERY_UNLOCKED event. Mastery progress bars and unlocked badges in Research panel. state.masteries persists in save (version 24).",
    "priority": 71,
    "status": "done",
    "phase": 23,
    "notes": "data/techs.js: Added MASTERY_GROUPS export (4 entries: military/economic/arcane/agrarian) each with id/name/icon/desc/techs array/bonusLabel. core/state.js: Added masteries:{} field + reset in initState(); added politicalEvents:null field. core/events.js: Added MASTERY_UNLOCKED + POLITICAL_EVENT constants. systems/research.js: Imported MASTERY_GROUPS; added _checkMasteries(techId) — iterates groups, skips already-unlocked, checks if all group techs done after new tech completes, records tick in state.masteries[group.id], calls recalcRates(), logs, emits MASTERY_UNLOCKED; _checkMasteries() called in researchTick() after TECH_CHANGED emit. systems/resources.js: Trade route loop — added economicMasteryMult=1.3 when economic mastery unlocked; upkeepMult=0.80 applied to unit upkeep (and garrison upkeep) when military mastery unlocked; mastery flat bonus block (economic +3 gold, arcane +1.5 mana +500 mana cap, agrarian +0.5 food/wood/stone) applied just before Object.assign. systems/combat.js: Applied state.masteries?.military +40 attackPower in both getAttackPreview() and attackTile() after conqueror archetype; getAttackPreview() returns militaryMastery flag. core/actions.js: scaledCost() applies masteryMult=0.85 when agrarian mastery unlocked. ui/researchPanel.js: Imported MASTERY_GROUPS; subscribed MASTERY_UNLOCKED→renderResearchPanel; added _masteriesSection() with 4 mastery-cards — unlocked (gold border, MASTERED badge, all tech icons lit, bonus text); locked (progress bar + dim tech icons, pct label); injected between tech-grid and _policySection(). main.js: Imported initPoliticalEvents+politicalEventTick; save version 23→24; masteries+politicalEvents persisted in _save()/_applySave(); MASTERY_UNLOCKED→_updateScoreBadge. CSS: .mastery-section, .mastery-grid (auto-fill 220px), .mastery-card/--unlocked (gold border+tint), .mastery-card__header/icon/name, .mastery-badge (gold pill), .mastery-progress-label, .mastery-techs/.mastery-tech/--done (dim vs bright), .mastery-bar-wrap/.mastery-bar (gold fill), .mastery-bonus/--active/--locked."
  },
  {
    "id": "T072",
    "title": "political events system",
    "description": "Random political crises fire every 5-10 minutes. 8 event types (harvest_festival/tax_dispute/desertion_crisis/merchant_delegation/plague_rumour/foreign_gift/mineral_vein/religious_revival). Each event shows title, description, and two choices with resource/morale tradeoffs. Player resolves via Quest panel. Auto-expires to choice B after 2 minutes. History log shows last 4 decisions. POLITICAL_EVENT event. state.politicalEvents saved/loaded. Save version 24.",
    "priority": 72,
    "status": "done",
    "phase": 23,
    "notes": "Created src/systems/politicalEvents.js: SPAWN_MIN=1200/SPAWN_MAX=2400 ticks (5-10 min); EXPIRE_TICKS=480 (2 min); 8 EVENTS (harvest_festival/tax_dispute/desertion_crisis/merchant_delegation/plague_rumour/foreign_gift/mineral_vein/religious_revival) each with id/icon/title/desc/choiceA/choiceB (label/effect/cost/gain/morale). initPoliticalEvents() idempotent guard on state.politicalEvents; politicalEventTick() auto-expires via _applyChoice(ev,'b',true) and spawns via _spawnEvent() on schedule; _spawnEvent() picks random event, sets state.politicalEvents.pending with expiresAt, logs message, emits POLITICAL_EVENT; resolvePoliticalEvent(choice) validates, deducts cost, applies gains+morale, logs entry, resets pending+schedules next; getPoliticalEventSecsLeft() for countdown. core/state.js: politicalEvents:null + reset in initState(). core/events.js: POLITICAL_EVENT constant. ui/questPanel.js: Added resolvePoliticalEvent/getPoliticalEventSecsLeft imports; subscribed POLITICAL_EVENT→render; TICK handler refreshes when pending event (alongside challenge); click delegate for [data-pol-choice] buttons calls resolvePoliticalEvent + shows btn--shake on error; _politicalEventSection() renders section header, pending-event card (title/desc/two choice cards with affordability-gated buttons + countdown timer urgent at <30s), or waiting state; history log shows last 4 entries; injected at top of render() before _challengeSection(). main.js: registerSystem(politicalEventTick) as 15th system; initPoliticalEvents() in boot+_newGame; politicalEvents persisted in _save()/_applySave(). CSS: .pol-event-section (accent border), .pol-event-header, .pol-event-timer/--urgent, .pol-event-card, .pol-event-title/.desc, .pol-event-choices (flex wrap), .pol-event-choice (card), .pol-event-choice__label/effect, .btn--pol-choice (amber), .pol-event-log/__header/__entry, .pol-event-waiting."
  },
  {
    "id": "T073",
    "title": "terrain combat modifiers",
    "description": "Per-terrain attack/defense multipliers in combat resolution. Mountain: ATK ×0.85, DEF ×1.25. Hills: DEF ×1.15. Forest: ATK ×1.10, DEF ×1.05. River: ATK ×0.95, DEF ×1.10. Grass/capital: no modifier. Shown in combat preview modal and tile hover tooltip.",
    "priority": 73,
    "status": "done",
    "phase": 23,
    "notes": "systems/combat.js: Added TERRAIN_COMBAT_MODS constant (mountain/hills/forest/river/grass/capital) and _terrainMod() helper. In getAttackPreview(): attackPower *= terrainMod.attackMult; effectiveDefense = tile.defense * terrainMod.defMult; winChance uses effectiveDefense; returns effectiveDefense+terrainMod fields. In attackTile(): same terrain attack mult applied before siege/mana-bolt override; effectiveDefense computed after siege sets defense=0 (so 0×anything=0, siege still guaranteed); effectiveDefense passed to _victory()/_defeat() for accurate history/log. ui/mapPanel.js: Added _TERRAIN_MOD_LABELS constant (human-readable descriptions) and _terrainModHtml() helper; _showCombatPreview() shows effective defense (with base in parentheses when different) + .cp-terrain-mod notice badge above siege/formation notices; _showTileTip() shows terrain combat modifier hint row for non-player tiles. styles/main.css: .cp-terrain-mod modifier class (blue tint, lighter border than gold siege notices)."
  },
  {
    "id": "T074",
    "title": "age council boons (one-time permanent bonus chosen on age advancement)",
    "description": "When advancing to Bronze/Iron/Medieval Age, a council modal offers 3 random boons from an age-specific pool of 5. Player picks 1 permanent bonus. Boon types: rate/cap bonuses, combat attack bonus, pop cap, counterattack penalty. Modal is dismissible (skip = no reward).",
    "priority": 74,
    "status": "done",
    "phase": 23,
    "notes": "Created src/data/ageBoons.js: AGE_BOON_POOLS keyed by age index (1/2/3); 5 boons per age — Bronze: fertile_lands (+1.5 food/s), logging_mastery (+1.5 wood/s), stone_quarrying (+1.5 stone/s), bronze_weapons (+10% combat), settlers_spirit (+100 pop cap); Iron: iron_discipline (+15% combat), merchant_networks (+2 gold/s), iron_reserves (+1.5 iron/s +200 iron cap), advanced_farming (+2.5 food/s), ley_lines (+1 mana/s); Medieval: grand_tactics (+25% combat), royal_granaries (+3 food/s), imperial_mint (+3 gold/s), castle_walls (−20% enemy counterattack), arcane_mastery (+2 mana/s); BOONS flat lookup. Created src/ui/councilModal.js: showCouncilModal(age, offer, onChoose) creates #council-modal; renders 3 boon cards (button, icon, name, desc); click→onChoose(boonId)+hide; Escape/Skip button dismiss without reward. core/state.js: added councilBoons:[] to static object + initState() reset. core/events.js: COUNCIL_BOON_CHOSEN added. core/actions.js: imported BOONS; added chooseCouncilBoon(boonId) — validates not duplicate, pushes to state.councilBoons, calls recalcRates(), emits COUNCIL_BOON_CHOSEN+RESOURCE_CHANGED, logs message. systems/combat.js: imported BOONS; added _councilBoonCombatMult() (sums effect.combatAttack fractions → 1+bonus multiplier); applied in both getAttackPreview() and attackTile() after conqueror archetype. systems/resources.js: imported BOONS; added boon rateBonus+capBonus loop after relic bonuses in recalcRates(). systems/enemyAI.js: imported BOONS; castle_walls check (×0.80 on winChance) after aegis spell. systems/population.js: imported BOONS; settlers_spirit check in _recalcCap(); subscribed COUNCIL_BOON_CHOSEN→_recalcCap. main.js: imported showCouncilModal/AGE_BOON_POOLS/chooseCouncilBoon; expanded AGE_CHANGED listener to pick 3 random boons from pool and call showCouncilModal (only for age>0); councilBoons saved/loaded; save version 23→24. styles/main.css: .council-modal/.council-modal--hidden/.council-box/.council-header/.council-sub/.council-boons/.council-boon/--icon/--body/--name/--desc/.council-skip; mobile: flex-wrap."
  },
  {
    "id": "T075",
    "title": "mercenary contracts system",
    "description": "Random mercenary offers appear in the Military panel every 5-8 minutes (one at a time, 60s window). Offers show a unit type, cost (2.5x base gold), hire button. Hiring is instant (no training queue), no upkeep. MERCENARY_CHANGED event. state.mercenaries saved/loaded. Save version 25.",
    "priority": 75,
    "status": "done",
    "phase": 24,
    "notes": "Created src/systems/mercenaries.js: SPAWN_MIN_TICKS=1200/SPAWN_MAX_TICKS=1920/OFFER_DURATION=240 ticks; _pickUnit() filters UNIT_ORDER by age/tech/building requirements; _mercCost() returns 2.5× base gold cost (min 120); initMercenaries() seeds state.mercenaries={current,nextOfferTick}; mercenaryTick() expires stale offer (logs 'moved on' info msg) and spawns new one (logs 'windfall' toast); hireMercenary() validates gold, instantly adds to state.units, calls recalcRates(), emits UNIT_CHANGED+RESOURCE_CHANGED+MERCENARY_CHANGED; mercenarySecsLeft() countdown helper. Added MERCENARY_CHANGED to events.js. Added mercenaries:null to state.js + initState() reset. main.js: imported initMercenaries/mercenaryTick; registerSystem(mercenaryTick) 16th system; initMercenaries() in boot+_newGame; mercenaries in _save()/_applySave(); save version 24→25. militaryPanel.js: imported hireMercenary/mercenarySecsLeft; subscribed MERCENARY_CHANGED→render; TICK handler extended with hasMercOffer check (refreshes every 16 ticks while offer active); _mercenarySection() injected first in _render() — renders gold-accent card with unit icon/name/ATK+DEF stats, seconds countdown (red pulse <20s), gold Hire button; hire-merc action in _handleClick() (btn--shake on error). CSS: .merc-offer (gold left-border, pop-in animation), .merc-offer__header/.title/.timer/--urgent, .merc-offer__body/.icon/.info/.name/.stats, .btn--hire-merc (gold gradient), .merc-offer__sub."
  },
  {
    "id": "T076",
    "title": "alliance resource gifts from AI empires",
    "description": "Allied empires periodically (every 5-7 minutes) send resource gifts based on their specialty: Iron Horde sends iron+food, Mage Council sends mana+gold, Sea Wolves send wood+food. Gift amounts scale with game tick. Logged as 'windfall' + diplomatic history 'gift' entry. Gift timing shown in empire card when allied. No save version bump needed (nextGiftTick lives inside state.diplomacy).",
    "priority": 76,
    "status": "done",
    "phase": 24,
    "notes": "Added ALLIANCE_GIFT event to events.js. In diplomacy.js: added GIFT_MIN_TICKS=1200/GIFT_MAX_TICKS=1680 ticks constants; EMPIRE_GIFTS lookup (ironHorde→iron+food, mageCouncil→mana+gold, seaWolves→wood+food) with tick-scaled amounts; _giftInterval() helper; nextGiftTick field added to each empire in initDiplomacy(); migration guard (nextGiftTick===undefined → _giftInterval()) for old saves; _allianceGift(emp) applies resource amounts (capped), builds parts summary, _logDiplomacy type='gift', addMessage windfall, emits RESOURCE_CHANGED+ALLIANCE_GIFT, schedules next nextGiftTick; called from diplomacyTick() when allied+tick≥nextGiftTick. diplomacyPanel.js: added 'gift':'🎁' to HIST_ICON; added ALLIANCE_GIFT subscription→_render(); TICK handler now also re-renders when hasAllied (gift countdown ticks down); _giftTimingHtml(emp) renders .dipl-alliance-gift row with bold time-remaining ('Next gift in Xm Xs') or green 'arriving soon…' text; injected into tradeHtml when allied. CSS: .dipl-alliance-gift (border-top, dim text), .dipl-alliance-gift__ready (green bold), .dipl-hist-entry--gift (gold left border). No save version bump — nextGiftTick is nested inside state.diplomacy which is already persisted."
  },
  {
    "id": "T077",
    "title": "tech synergy bonuses (6 cross-tech permanent bonuses)",
    "description": "When two compatible technologies are both researched, a synergy bonus permanently activates. 6 synergies: veteran_legion (warcraft+tactics, +20% combat attack), runic_forging (alchemy+steel, +2 iron/s), trade_empire (navigation+economics, +0.8 gold/s per trade route), sacred_harvest (divine_favor+agriculture, +50% farm food), fortress_doctrine (fortification+tactics, −25% enemy counterattack success), naval_engineering (engineering+navigation, +2 wood/s +300 wood cap). Synergies computed dynamically (no extra state). SYNERGY_UNLOCKED event + log message on unlock. Synergy cards in Research panel showing locked/unlocked states.",
    "priority": 77,
    "status": "done",
    "phase": 25,
    "notes": "Added SYNERGIES constant (6 entries) + SYNERGY_ORDER to data/techs.js. Added SYNERGY_UNLOCKED to events.js. systems/research.js: added _checkSynergies(techId) called in researchTick() after _checkMasteries — iterates SYNERGIES, checks if new tech is in the pair, checks if both are done, logs '✨ Synergy unlocked: name! effect' + emits SYNERGY_UNLOCKED. systems/resources.js: added _synergy() helper; runic_forging +2 iron/s in baseline; naval_engineering +2 wood/s +300 wood cap; trade_empire +0.8 gold/s per open trade route inside trade route loop; sacred_harvest ×1.5 in _buildingProdMultiplier for farm. systems/combat.js: added _synergy() helper; veteran_legion ×1.20 in getAttackPreview() and attackTile() after military mastery; returns veteranLegion flag in preview. systems/enemyAI.js: fortress_doctrine ×0.75 on winChance after castle_walls boon. ui/researchPanel.js: imported SYNERGIES+SYNERGY_ORDER; subscribed SYNERGY_UNLOCKED→render; added _synergiesSection() rendering .synergy-grid with per-synergy cards — active (purple border+badge, green tech icons) vs locked (dimmed); injected between mastery section and policy section. CSS: .synergy-section/.synergy-section__header/count/intro, .synergy-grid (auto-fill 220px), .synergy-card/--active/--locked, .synergy-card__header/.synergy-icon/.synergy-name/.synergy-badge (purple pill), .synergy-techs/.synergy-tech/--done/.synergy-plus, .synergy-effect/--active/--locked."
  },
  {
    "id": "T078",
    "title": "weather system — periodic climate events affecting resource rates",
    "description": "Weather events fire every 4-6 minutes, lasting 60-90 seconds. 5 weather types: Clear Skies (+10% all positive rates, 60s), Heavy Rain (+25% food, -10% wood, 75s), Scorching Heat (-15% food, +15% gold, 60s), Windstorm (+25% wood, -10% stone/iron, 60s), Snowstorm (-20% all positive rates, -5 morale, 90s). Weather applied in recalcRates() after seasons. Weather badge in title bar with icon+name. WEATHER_CHANGED event. state.weather saved/loaded. Save version 26.",
    "priority": 78,
    "status": "done",
    "phase": 25,
    "notes": "Created src/data/weather.js: WEATHER_TYPES array (5 entries: clear_skies/heavy_rain/scorching_heat/windstorm/snowstorm) each with id/icon/name/desc/weight/duration/modifiers/moraleDelta?/logType. Created src/systems/weather.js: GRACE_TICKS=480 (no weather first 2min); _pickWeather() weighted random; initWeather() idempotent struct guard; weatherTick() expires active (logs 'passed', recalcRates, WEATHER_CHANGED); spawns new when cooldown+grace clear (_spawnWeather: changeMorale if moraleDelta, addMessage, recalcRates, WEATHER_CHANGED); getCurrentWeather()/getWeatherSecsLeft() helpers. Added WEATHER_CHANGED to events.js. Added weather:null to state.js static object + initState() reset. resources.js: weather multipliers applied after season modifiers — allRates (positive rates) then per-resource keys. main.js: imported all weather exports; registerSystem(weatherTick) 17th system; initWeather() in boot+_newGame; weather:state.weather in _save(); state.weather=s.weather??null in _applySave(); save version bumped to 26; _updateWeatherBadge() function (shows/hides weather-badge span with icon+name+tooltip+countdown); WEATHER_CHANGED subscription + 4-tick TICK refresh for countdown. index.html: <span id='weather-badge'> with style='display:none' between population-badge and score-badge. CSS: .weather-badge (pill shape, var(--bg-card) background, cursor:help, white-space:nowrap); hidden on ≤600px. Tech synergy section CSS also added: .synergy-* classes."
  },
  {
    "id": "T079",
    "title": "barbarian grand siege events",
    "description": "When 3+ barbarian camps coexist and a 12-minute cooldown has elapsed, the horde organises a Grand Siege on the capital. A 30-second warning fires first (message + HUD badge). Player power (units+hero+techs) vs combined camp defenses (55% threshold): repel → all camps destroyed + gold/food/iron loot + morale +15; struck → gold/food penalty + morale -20. #siege-badge pulses red during warning. BARBARIAN_SIEGE event. Save-compatible via migration guards in initBarbarians().",
    "priority": 79,
    "status": "done",
    "phase": 26,
    "notes": "Rewrote src/systems/barbarianCamps.js: added SIEGE_THRESHOLD=3, SIEGE_WARN_TICKS=120 (30s), SIEGE_INTERVAL=2880 (12min), SIEGE_WIN_RATIO=0.55, SIEGE_REPEL_LOOT={gold:200,food:100,iron:50}; imported UNITS + changeMorale; initBarbarians() now adds nextSiegeTick+siegeWarning to state.barbarians with migration guards; barbarianTick() calls _siegeCheck(campCount) after spawn logic; _siegeCheck() sets siegeWarning on threshold met (warning message + BARBARIAN_SIEGE{type:warning} + MAP_CHANGED) and fires _launchSiege() when tick>=siegeWarning; _launchSiege() computes _playerPower() (units*attack+hero30+techs) vs _barbarianPower() (sum barbarian tile defenses) — repelled: clears all camps+awards loot+changeMorale(+15)+RESOURCE_CHANGED; struck: deducts gold/food+changeMorale(-20); exported getSiegeSecsLeft(). Added BARBARIAN_SIEGE to events.js. main.js: added getSiegeSecsLeft import from barbarianCamps.js (consolidated single import); BUILDINGS import added; initPrestige()+_updateSiegeBadge()+_updatePrestigeBadge() calls in boot+_newGame; _updateSiegeBadge() shows/hides #siege-badge with countdown; registered BARBARIAN_SIEGE+TICK/4 for badge refresh. index.html: <span id=siege-badge style=display:none>. CSS: @keyframes siege-pulse (opacity 1→0.55→1 1s), .siege-badge (red border/bg #fc8181 text, animation, hidden ≤600px)."
  },
  {
    "id": "T080",
    "title": "empire prestige system — permanent milestone bonuses",
    "description": "Cumulative prestige score earned through: age advance (+100×age), wonders (+200), battle victories (+5), alliances (+50), quests (+30), tech mastery (+100), synergy (+75). Five milestones unlock permanent production bonuses: 500→+1 gold/s; 1000→+1 food/s +1 wood/s; 2000→+200 all caps; 3500→+2 gold/s +1 iron/s +1 mana/s; 5000→all positive rates ×1.15. state.prestige={score,milestones[]}. #prestige-badge shows ✨ score in title bar. PRESTIGE_CHANGED event. Save version 27.",
    "priority": 80,
    "status": "done",
    "phase": 26,
    "notes": "Created src/systems/prestige.js: PRESTIGE_MILESTONES array (5 entries with threshold/name/icon/bonusDesc); initPrestige() idempotent struct guard; awardPrestige(amount,reason) adds score+_checkMilestones+emits PRESTIGE_CHANGED+addMessage; getPrestigeScore() helper; _checkMilestones() iterates milestones pushing thresholds reached + achievement message. Added PRESTIGE_CHANGED to events.js. Added prestige:null to state.js static object + initState() reset. resources.js: T080 prestige milestone bonuses block at end of recalcRates() before Object.assign — checks pm.includes(500→+1gold), pm.includes(1000→+1food+1wood), pm.includes(2000→+200 all caps), pm.includes(3500→+2gold+1iron+1mana), pm.includes(5000→all positive rates*1.15). main.js: imported initPrestige/awardPrestige/getPrestigeScore from prestige.js; BUILDINGS import added; initPrestige() in boot+_newGame; prestige event listeners registered once in boot: AGE_CHANGED(+100*age), MASTERY_UNLOCKED(+100), SYNERGY_UNLOCKED(+75), QUEST_COMPLETED(+30), MAP_CHANGED+outcome=win(+5), BUILDING_CHANGED+wonder+count==1(+200), DIPLOMACY_CHANGED+allied(+50); _updatePrestigeBadge()+PRESTIGE_CHANGED subscription; _nextPrestigeMilestone() inline helper; prestige:state.prestige in _save(); state.prestige=s.prestige in _applySave(); save version bumped 26→27. index.html: <span id=prestige-badge class=prestige-badge> after score-badge. CSS: .prestige-badge (purple #b794f4 pill badge), .prestige-badge:hover, hidden ≤600px."
  },
  {
    "id": "T081",
    "title": "player-initiated diplomatic gift sending",
    "description": "Allow the player to send gold gifts to neutral or allied empires to improve relations. Small (50g, 30% alliance chance for neutral empires) and Large (150g, 70% alliance chance). Per-empire 30-second cooldown. Gift section shown in each empire card. No save version bump needed (playerGiftCooldownUntil lives in state.diplomacy.empires entries).",
    "priority": 81,
    "status": "done",
    "phase": 27,
    "notes": "systems/diplomacy.js: Added GIFT_SMALL_COST=50/GIFT_LARGE_COST=150/GIFT_SMALL_ALLY_CHANCE=0.30/GIFT_LARGE_ALLY_CHANCE=0.70/PLAYER_GIFT_COOLDOWN_TICKS=120*4=480 constants. Added playerGiftCooldownUntil:0 to initial empire entry in initDiplomacy(); migration guard in initDiplomacy() for older saves. sendGift(empireId, size) validates relations (war=rejected), cooldown, gold; deducts cost; sets per-empire cooldown; neutral+roll<chance → immediate alliance (DIPLOMACY_CHANGED{allied}); neutral+roll>=chance → goodwill logged; allied → appreciation logged. ui/diplomacyPanel.js: imported sendGift+GIFT_* constants; added _giftRow(emp) helper rendering .dipl-gift-row with Small/Large buttons (gold-tinted .btn--gift) when not on cooldown, or cooldown countdown badge; injected into _empireCard() between warScoreHtml and actions div; TICK throttle now also checks hasGiftCooldown (re-renders while any gift cooldown is active); sendGift case added to _onClick(). styles/main.css: .dipl-gift-row (flex wrap), .dipl-gift-label (dim), .dipl-gift-cd (yellow), .btn--gift (amber border/bg/hover)."
  },
  {
    "id": "T082",
    "title": "hero injury and recovery system",
    "description": "When the player loses a battle and the hero is recruited, 20% chance the hero is injured. Injured heroes contribute no combat bonuses and cannot use abilities for a 5-minute (1200 ticks) recovery period. Military panel shows a progress bar during recovery. Auto-recovers when tick reaches recoveryUntil. No save version bump needed (hero.injured / hero.recoveryUntil are new optional hero fields).",
    "priority": 82,
    "status": "done",
    "phase": 27,
    "notes": "systems/combat.js: Added HERO_INJURY_CHANCE=0.20, HERO_RECOVERY_TICKS=1200 constants. _heroInjured() helper: returns false if not injured; if injured + tick>=recoveryUntil auto-clears injury, logs recovery message, emits HERO_CHANGED; else returns true. getAttackPreview(): hero bonus block now guarded by !_heroInjured(); siegeActive check also respects injury; preview returns heroInjured field. attackTile(): hero bonus block guarded by !_heroInjured(); Siege Master check guarded by !state.hero.injured. _defeat(): after changeMorale, if hero recruited + not already injured + random()<HERO_INJURY_CHANCE: sets hero.injured=true, hero.recoveryUntil=tick+1200, emits HERO_CHANGED, logs \"🩹 Champion was injured\" hero-type message. ui/militaryPanel.js: _heroActiveSection() checks h.injured+now<recoveryUntil first; renders .hero-card--injured recovery card with red-tinted border, .hero-injured-msg warning, .hero-recovery-wrap with gradient progress bar (fill=pct complete) and time countdown; falls through to normal hero UI when healthy. TICK handler: added h.injured to hasHeroActivity check so panel refreshes every 16 ticks during recovery. styles/main.css: .hero-card--injured (red border+bg tint), .hero-card__badge--injured (red pill), .hero-injured-msg (red 12px), .hero-recovery-wrap/.hero-recovery-bar-outer/.hero-recovery-bar-fill (red→green gradient), .hero-recovery-time (dim right-aligned)."
  },
  {
    "id": "T083",
    "title": "empire decree system — player-activated strategic abilities with cooldowns",
    "description": "5 decrees in Military tab: Conscription (5 infantry+3 archers, 200g+30f, 7.5min CD), Emergency Levy (+200g+100f, -5 morale, 10min CD), Harvest Edict (+40% food+wood 120s, 75g, 12min CD), War Banner (+40% atk 3 battles, 100g+20i, 7.5min CD), Scholars Edict (-2min research, 150g+50m, 15min CD). state.decrees. DECREE_USED event. Save version 28.",
    "priority": 83,
    "status": "done",
    "phase": 28,
    "notes": "Created src/data/decrees.js: DECREES array (5 entries: conscription/emergency_levy/harvest_edict/war_banner/scholars_edict) each with id/icon/name/desc/cost/cooldownTicks/durationTicks?/charges?. Created src/systems/decrees.js: initDecrees() idempotent struct guard; decreesTick() expires harvestEdictExpires+recalcRates+message+DECREE_USED; canUseDecree(id) checks cooldown+resources+special conditions; useDecree(id) deducts cost+sets cooldown+applies effect via _applyConscription/_applyEmergencyLevy/_applyHarvestEdict/_applyWarBanner/_applyScholarsEdict; getDecreeSecsLeft()/isHarvestEdictActive()/getWarBannerCharges() helpers. Added DECREE_USED to events.js. Added decrees:null to state.js static + initState() reset. resources.js: harvest_edict check before Object.assign (food+wood *1.40 when active). combat.js: war_banner check in both getAttackPreview and attackTile (attackPower*1.40); consume charge each attack + message when exhausted; warBannerCharges in preview return. main.js: imported initDecrees+decreesTick; registerSystem(decreesTick) 19th system; initDecrees() in boot+_newGame; decrees:state.decrees in _save(); state.decrees=s.decrees??null in _applySave(); save version bumped 27→28. militaryPanel.js: imported useDecree+canUseDecree+getDecreeSecsLeft+isHarvestEdictActive+getWarBannerCharges from decrees.js + DECREES from data/decrees.js; added DECREE_USED subscription+tick hasDecreeCooldown check; _decreesSection() renders .decrees-section with 5 .decree-card entries showing status/cost/countdown+Enact button; _handleClick use-decree case calls useDecree(id); _escHtml helper added. CSS: .decrees-section (gold border-left), .decrees-title/.decrees-desc, .decree-grid (auto-fill 175px), .decree-card/.decree-card--cd, .decree-card__header/.decree-card__icon/.decree-card__name, .decree-costs/.decree-cost--ok/bad, .decree-status--ready/active/cd/locked, .btn--decree (amber gradient)."
  },
  {
    "id": "T084",
    "title": "10 new achievements — late-game milestones",
    "description": "Add 10 achievements: siege_repelled (repel grand siege), all_relics (all 6 relics), prestige_1000 (1000 prestige), elite_unit (unit reaches elite rank), weathered (survive snowstorm with morale>0), treasury (all 6 resources >=80% cap), speed_builder (Bronze Age in 8min), veteran_army (3+ veteran/elite units), mercenary_lord (hire 3 mercenaries), decree_master (use all 5 decrees). Add totalHired tracking to mercenaries. TOTAL_ACHIEVEMENTS 15→25.",
    "priority": 84,
    "status": "done",
    "phase": 28,
    "notes": "achievements.js: added 10 ACHIEVEMENTS entries (siege_repelled/all_relics/prestige_1000/elite_unit/weathered/treasury/speed_builder/veteran_army/mercenary_lord/decree_master). Added _lastWeatherType and _decreesUsed module vars; reset in initAchievements(). New event listeners: BARBARIAN_SIEGE(repelled→_checkAndUnlock siege_repelled), RELIC_DISCOVERED(all_relics), PRESTIGE_CHANGED(prestige_1000), WEATHER_CHANGED(track lastWeatherType; on clear if was snowstorm+morale>0→weathered), DECREE_USED(activated→add to _decreesUsed set+decree_master). Extended existing listeners: UNIT_CHANGED+veteran_army+mercenary_lord; MAP_CHANGED+veteran_army+elite_unit; AGE_CHANGED+speed_builder; RESOURCE_CHANGED+treasury. _predicate: siege_repelled/weathered return true (event-gated); all_relics checks RELIC_ORDER all discovered; prestige_1000 checks score>=1000; elite_unit checks any unitRank==elite; treasury checks all 6 resources>=80% cap; speed_builder checks age>=1 && tick<=1920; veteran_army counts ranked units>=3; mercenary_lord checks totalHired>=3; decree_master checks all DECREES ids in _decreesUsed set. mercenaries.js: added totalHired:0 to initial state; migration guard in initMercenaries(); incremented totalHired in hireMercenary(). summaryPanel.js: TOTAL_ACHIEVEMENTS 15→25. Imports added: RELIC_ORDER from data/relics.js, DECREES from data/decrees.js."
  },
  {
    "id": "T085",
    "title": "delivery contracts system",
    "description": "Every 8-10 minutes, 2 random delivery contracts appear in the Market tab. Each contract specifies resources to deliver in exchange for a gold reward. Player accepts a contract (resources deducted immediately), waits 60s processing time, then collects the gold reward. One active contract at a time. Shows offer cards with resource requirements and reward, active contract progress bar, and countdown to next refresh. 8 contract templates across all resource types. Completing contracts earns prestige (+20) and increments totalTrades. CONTRACTS_CHANGED event. state.contracts saved/loaded.",
    "priority": 85,
    "status": "done",
    "phase": 10,
    "notes": "Created src/systems/contracts.js: CONTRACT_POOL of 8 templates (grain_export/timber_supply/stone_contract/iron_trade/mana_export/mixed_supply/arms_contract/arcane_deal); initContracts() idempotent init with migration guards; contractsTick() handles offer spawning (every 8-10 min / 1920-2400 ticks when Market built and no active contract) and contract completion; acceptContract(idx) validates resources, deducts them, starts 60s processing countdown; cancelContract() forfeits resources; contractsRefreshSecs()/contractProgress()/contractSecsLeft() helpers. _completeContract() awards reward resources, totalCompleted++, awardPrestige(20), increments market.totalTrades. Updated src/ui/marketPanel.js: added CONTRACTS_CHANGED subscription; TICK handler refreshes when active; _contractsSection() renders offer cards (with affordability check, red deliver/green reward), active progress bar with countdown, and \"waiting\" state with refresh timer; _handleClick handles accept-contract and cancel-contract data-actions. Added CONTRACTS_CHANGED + HERO_EXPEDITION events to events.js. state.contracts added to state.js initState(). main.js: contractsTick registered, initContracts() called in boot+_newGame, contracts saved/loaded (save version 29). CSS: .contracts-section/.contracts-header/.contract-offers grid/.contract-offer cards (red deliver, green reward), .contract-active progress bar (green gradient), .btn--contract-accept (green), .btn--contract-cancel (red), .contract-waiting, mobile single-column."
  },
  {
    "id": "T086",
    "title": "hero training expedition",
    "description": "When the Champion hero is recruited, healthy (not injured), and not on cooldown, the player can send them on a 2-3 minute training expedition. During the expedition the hero is unavailable for combat (no attack bonus, no abilities). On return: hero gains 2 combatWins (triggering skill level-up if threshold reached) and has a 25% chance of 100-200 gold bonus loot. Military panel shows expedition progress bar and Recall button. heroSystem.js handles expedition tick logic. HERO_CHANGED event emitted on departure and return. state.hero.expedition = {active, endsAt} field added. Save version 29.",
    "priority": 86,
    "status": "done",
    "phase": 10,
    "notes": "Created src/systems/heroSystem.js: sendOnExpedition() validates hero healthy+not on expedition, randomises 2-3 min duration (480-720 ticks), stores {active, startedAt, endsAt, totalTicks} in state.hero.expedition, logs departure message, emits HERO_CHANGED+HERO_EXPEDITION; recallExpedition() clears expedition with no reward; heroTick() checks completion — awards EXPEDITION_WIN_CREDIT=2 combatWins (checking for skill level-up milestone via HERO_SKILL_WIN_INTERVAL), 25% chance of 100-200 gold bonus, logs return message, emits HERO_CHANGED+HERO_EXPEDITION; expeditionProgress() uses startedAt+totalTicks for accurate fraction; expeditionSecsLeft() for countdown. Updated src/systems/combat.js: added _heroOnExpedition() helper; all three hero bonus blocks (getAttackPreview ×2 + attackTile) now guard with !_heroOnExpedition() alongside !_heroInjured(); Siege Master check also guards expedition. Updated src/ui/militaryPanel.js: imported heroSystem exports; _heroActiveSection() checks expedition.active first — renders orange-tinted expedition card with progress bar, countdown, Recall button, and skills section; added \"Send on Expedition\" button to healthy hero card; _handleClick handles expedition-send and expedition-recall actions; TICK handler adds h.expedition?.active to hasHeroActivity check for countdown refresh. main.js: heroTick registered as tick system; hero.expedition migration guard in _applySave. CSS: .hero-card--expedition (orange border+tint), .hero-card__badge--expedition (orange pill), .hero-expedition-msg, .hero-expedition-bar-outer/fill (orange gradient), .hero-expedition-time, .btn--expedition-recall (red), .btn--expedition-send (orange)."
  },
  {
    "id": "T087",
    "title": "wandering merchant events — periodic merchant with 3 random item offers",
    "priority": 1,
    "status": "done",
    "phase": "Phase 9 — Content & Events",
    "description": "Every 8-12 minutes a wandering merchant visits and offers 3 random items (resources, units, research boosts, map reveals, etc.) for gold. Player can buy one offer before merchant departs in 90s.",
    "notes": "Created src/systems/merchant.js: ITEM_POOL of 11 item types (resource/barter/research/morale/warbanner/reveal/unit/spell). initMerchant() idempotent init; merchantTick() spawns offer every 8-12 min (VISIT_MIN/MAX_TICKS=1920-2880), expires after 90s (STAY_TICKS=360); buyMerchantItem(idx) validates affordability (gold for most, food+wood barter for gold_stash), deducts cost, applies effect, closes offer (one purchase per visit); merchantSecsLeft()/merchantNextVisitSecs()/canAffordItem() helpers. Added MERCHANT_CHANGED to events.js. Added merchant:null to state.js static + initState(). ui/marketPanel.js: imported merchant functions; added MERCHANT_CHANGED subscription + TICK refresh when offer active; _merchantSection() renders .merchant-section--active with 3 item cards (icon/title/cost/desc/Purchase btn) + countdown timer when active, or next-visit countdown when absent; handles merchant-buy data-action in _handleClick. main.js: imported initMerchant+merchantTick; registerSystem(merchantTick); initMerchant() in boot+_newGame; merchant saved/loaded (save version 29→30). CSS: .merchant-section (purple border-left #9f7aea), --active pulse animation, .merchant-items grid (auto-fill 160px), .merchant-item/--poor cards, .merchant-item__header/icon/title/cost/desc, .btn--merchant-buy (purple hover), .merchant-waiting, mobile single-column."
  },
  {
    "id": "T088",
    "title": "border skirmishes — AI vs AI conflict events with player exploitation opportunities",
    "priority": 2,
    "status": "done",
    "phase": "Phase 9 — Content & Events",
    "description": "Two AI empires occasionally enter a border skirmish lasting 3-5 minutes. During skirmish both empires are weakened (player gets +20% win chance attacking them). Player can Mediate the conflict (requires 2 alliances) for gold + prestige.",
    "notes": "Modified src/systems/diplomacy.js: Added SKIRMISH constants (SKIRMISH_INTERVAL_MIN/MAX=15-20min, SKIRMISH_DURATION_MIN/MAX=3-5min), exported SKIRMISH_ATTACK_BONUS=0.20, MEDIATE_MIN_ALLIANCES=2, MEDIATE_GOLD_REWARD=150, MEDIATE_PRESTIGE=50. Updated initDiplomacy() to add skirmish:null and nextSkirmishTick to state.diplomacy; migration guards for older saves. Added _skirmishTick() called from diplomacyTick(): on expiry emits BORDER_SKIRMISH{mediated/resolved}, logs messages, schedules next skirmish; on spawn picks 2 random non-war empires, creates skirmish object, logs arrival message. Added exported API: isSkirmishActive(), getSkirmish(), skirmishSecsLeft(), mediateSkirmish() (validates 2 alliances, sets endsAt=now for immediate resolution), isEmpireInSkirmish(empireId). Added _skirmishInterval() helper. Added BORDER_SKIRMISH to events.js. src/systems/combat.js: imported isEmpireInSkirmish+SKIRMISH_ATTACK_BONUS from diplomacy.js; getAttackPreview: skirmish bonus check before return (winChance+0.20 when tile.owner in skirmish, returns skirmishBonus field); attackTile: same check with addMessage about +20% advantage. ui/diplomacyPanel.js: imported new exports; added BORDER_SKIRMISH listener+isSkirmishActive check in TICK handler; _skirmishBanner() renders .skirmish-banner with header/desc/timer/mediate button; _empireCard() shows dipl-badge--skirmish badge on skirmishing empires; added mediateSkirmish case to _onClick(). main.js: imported MEDIATE_PRESTIGE from diplomacy.js; added BORDER_SKIRMISH listener calling awardPrestige(MEDIATE_PRESTIGE). CSS: .skirmish-banner (orange border-left pulse animation), .skirmish-banner__header/title/timer/desc/actions, .btn--mediate (green border), .skirmish-mediated, .dipl-badge--skirmish (orange pill badge)."
  },
  {
    "id": "T089",
    "title": "special map landmarks — 5 unique legendary sites with permanent bonuses",
    "priority": 89,
    "status": "done",
    "phase": 29,
    "description": "Five legendary landmark sites (Ancient Temple, Golden Fields, Ancient Quarry, Dragon's Hoard, Titan Forge) are placed on the map during generation at distance 4–12 from capital. Each reveals a ★ marker on the canvas once fog is cleared. Capturing the tile grants a permanent rate/cap bonus (mana, food, stone, gold, iron). Landmarks listed in Research panel. LANDMARK_CAPTURED event. +150 prestige on capture. state.landmarks saved/loaded. Save version 31.",
    "notes": "Created src/data/landmarks.js: 5 LANDMARK definitions with id/icon/name/desc/mapLabel/defenseBonus/bonus(rates+caps); LANDMARK_ORDER array. systems/map.js: imported LANDMARKS/LANDMARK_ORDER; added _placeLandmarks(tiles) — picks 5 neutral tiles at dist 4–12, assigns tile.landmark=id, boosts defense; called from initMap() + sets state.landmarks={captured:{}}. systems/combat.js: imported LANDMARKS; added _tryCaptureLandmark(tile,x,y) called in _victory() after relic check; records tick in state.landmarks.captured[id], calls recalcRates(), logs windfall message, emits LANDMARK_CAPTURED+RESOURCE_CHANGED. systems/resources.js: imported LANDMARKS; added landmark bonus loop in recalcRates() after relic loop — applies def.bonus.rates+def.bonus.caps for all captured landmarks. ui/mapPanel.js: imported LANDMARKS; added landmark ★ glyph drawing in _drawTile() on tile.landmark tiles not yet player-owned; added lmHtml block in _showTileTip() showing landmark name/bonus hint or 'captured' badge; subscribed LANDMARK_CAPTURED→_render. ui/researchPanel.js: imported LANDMARKS+LANDMARK_ORDER; subscribed LANDMARK_CAPTURED→render; added _landmarksSection() with relic-card styled grid showing captured (gold border, bonus pills) vs locked (★ icon, bonus hint) states; injected at end of panel. ui/summaryPanel.js: imported LANDMARK_ORDER; added 🗺️ Landmarks X/5 row in _progressionCard(); subscribed LANDMARK_CAPTURED+BUILDING_SPECIALIZED→_render. core/events.js: LANDMARK_CAPTURED+'landmarkCaptured' added. core/state.js: landmarks:null + reset in initState(); buildingSpecials:{} reset too. main.js: landmarks+buildingSpecials in _save()/_applySave(); save version bumped to 31; LANDMARK_CAPTURED listener awardPrestige(150)."
  },
  {
    "id": "T090",
    "title": "building specializations — permanent one-time upgrades for 4 core buildings",
    "priority": 90,
    "status": "done",
    "phase": 29,
    "description": "Farm, Lumber Mill, Barracks, and Quarry each have 2 specialization options. One specialization can be applied per building slot for a one-time gold cost. Specializations are permanent. Farm: Granary (+200 food cap, -10% food) or Market Garden (+30% food, +0.5 gold/s). Lumber Mill: Timber Mill (+40% wood) or Charcoal Forge (+0.5 iron/s, -20% wood, requires metalworking). Barracks: Training Grounds (-25% training time) or Armory (+15% combat, requires metalworking). Quarry: Deep Quarry (+40% stone) or Iron Quarry (+30% stone, +0.5 iron/s, requires metalworking). BUILDING_SPECIALIZED event. state.buildingSpecials saved/loaded.",
    "notes": "Created src/data/buildingSpecials.js: SPECIALIZATIONS object (8 entries across 4 buildings), SPECIALS_BY_BUILDING lookup, ELIGIBLE_BUILDINGS list. core/events.js: BUILDING_SPECIALIZED event added. core/state.js: buildingSpecials:{} field + reset in initState(). core/actions.js: imported SPECIALIZATIONS; added specializeBuilding(buildingId, specId) — validates building exists, no existing spec, tech/age requirements, affordability; deducts cost, sets state.buildingSpecials[id], calls recalcRates(), emits BUILDING_SPECIALIZED+BUILDING_CHANGED+RESOURCE_CHANGED; Training Grounds -25% applied in trainUnit() via state.buildingSpecials.barracks==='training_grounds' check. systems/resources.js: imported SPECIALIZATIONS; updated building loop to apply specDef.prodMult per resource when specialization exists; added rateBonus+capBonus from specDef after per-resource loop. systems/combat.js: added Armory +15% in both getAttackPreview() and attackTile() after council boon mult. ui/buildingPanel.js: imported specializeBuilding+SPECIALIZATIONS+SPECIALS_BY_BUILDING+ELIGIBLE_BUILDINGS; subscribed BUILDING_SPECIALIZED→render; added _specializationsSection() at bottom of panel showing eligible owned buildings — active-spec card (gold border, badge) or two option cards with cost/desc/Specialize button (gated by affordability+tech reqs); delegated specialize action in panel.onclick. CSS: .spec-section (amber border-left), .spec-section__header/intro, .spec-card/--active, .spec-card__header/desc/title, .spec-badge, .spec-options flex column, .spec-option, .spec-option__header/icon/name/cost, .spec-cost--ok/bad, .spec-option__desc, .btn--specialize (amber gradient)."
  },
  {
    "id": "T091",
    "title": "faction alliance bonuses — passive empire-specific perks when allied",
    "priority": 91,
    "status": "done",
    "phase": 30,
    "description": "When allied with each AI empire, gain unique passive bonuses. Iron Horde: +20% combat attack power. Mage Council: +1 mana/s, +30% faster research. Sea Wolves: +1 gold/s, +40% trade route income. Effects wired into resources.js (rates), combat.js (attack), research.js (research speed). Alliance bonus label shown in diplomacy panel empire card when allied. No save version bump needed — bonuses computed dynamically from existing state.diplomacy.",
    "notes": "data/empires.js: Added allianceBonus object to each empire — ironHorde {combat:0.20, label:'...'}, mageCouncil {manaRate:1.0, researchSpeed:0.70, label:'...'}, seaWolves {goldRate:1.0, tradeIncomeMult:1.40, label:'...'}. systems/resources.js: Trade route loop now applies allyTradeMult=EMPIRES[emp.id]?.allianceBonus?.tradeIncomeMult??1.0 to Sea Wolves trade gift income; added flat alliance rate bonus loop after trade route block (mana+=1.0 for mageCouncil, gold+=1.0 for seaWolves). systems/combat.js: Added Iron Horde allied check (attackPower*=1.20) in both getAttackPreview() and attackTile() after Armory specialization, before War Banner. systems/research.js: Added Mage Council allied check in startResearch() — totalTicks*=0.70 for 30% faster research. ui/diplomacyPanel.js: Added allianceBonusHtml local var in _empireCard(); renders .dipl-alliance-bonus row with empire's label when allied; injected between tradeHtml and warScoreHtml. styles/main.css: Added .dipl-alliance-bonus (border-top, dim text) and .dipl-alliance-bonus strong (gold accent)."
  },
  {
    "id": "T092",
    "title": "seasonal special events — one unique mid-season event per season cycle",
    "priority": 92,
    "phase": 30,
    "status": "done",
    "description": "A special event fires once per season at the halfway point (tick 180 of 360). Spring Festival: +60 food, +40 wood, +2 morale. Grand Tournament (Summer): +1 XP to all trained unit types (with possible promotions), +4 morale. Harvest Moon (Autumn): +80 food, +60 gold, +40 stone. Winter Solstice: +8 morale, +30 mana. All events shown as windfall toasts. SEASONAL_EVENT event added to events.js. seasonalEventFired boolean in state.season (migration guard in initSeasons). No save version bump needed.",
    "notes": "core/events.js: Added SEASONAL_EVENT:'seasonalEvent'. systems/seasons.js: Added SEASON_MID=180 constant; imported changeMorale from morale.js and UNITS from data/units.js. initSeasons() initialises seasonalEventFired:false and has migration guard for old saves. seasonTick() checks tick===SEASON_MID && !seasonalEventFired → fires _fireSeasonalEvent(seasonIndex), sets seasonalEventFired=true; resets to false on season change. _fireSeasonalEvent() switch: Spring→+60food+40wood+2morale+windfall toast; Summer→_grantTournamentXP()+4morale+windfall toast; Autumn→+80food+60gold+40stone+windfall toast; Winter→+8morale+30mana+windfall toast; all emit SEASONAL_EVENT+RESOURCE_CHANGED. _addCapped() helper caps resource at state.caps. _grantTournamentXP() iterates state.units, grants +1 XP per unit type, promotes to veteran (3 XP) or elite (6 XP) with achievement messages, emits UNIT_CHANGED if any promoted. No save version bump — seasonalEventFired is nested inside state.season which is already persisted; migration guard handles old saves."
  },
  {
    "id": "T093",
    "title": "enemy faction capitals — capture for permanent peace and prestige",
    "description": "During map generation, designate one enemy settlement per faction as its capital (the tile farthest from the player's capital). Capitals get +40% defense. Canvas: 👑 crown icon replaces ⚔️ sword on capital tiles. Tile tooltip shows 'Faction Capital — Capture for peace!'. Capturing a capital: forces that faction to neutral relations, grants +10 morale, emits FACTION_CAPITAL_CAPTURED, adds special message. main.js listens and awards +150 prestige. tile.isFactionCapital field persisted automatically via state.map.tiles. No save version bump needed.",
    "priority": 93,
    "phase": 31,
    "status": "done",
    "notes": "core/events.js: Added FACTION_CAPITAL_CAPTURED:'factionCapitalCaptured'. systems/map.js: Added _designateFactionCapitals(tiles) called from initMap() after _placeEnemies; iterates all 3 factions (ironHorde/mageCouncil/seaWolves), finds the enemy settlement farthest from CAPITAL via Math.hypot, sets tile.isFactionCapital=factionId and boosts defense by ×1.4. systems/combat.js: Imported EMPIRES from data/empires.js; _victory() saves wasFactionCapital=tile.isFactionCapital at start (before owner mutation), clears tile.isFactionCapital=null; after _tryCaptureLandmark(), checks wasFactionCapital — forces diplomacy empire to neutral+resets warScore, calls changeMorale(10), emits FACTION_CAPITAL_CAPTURED+DIPLOMACY_CHANGED, adds surrender message; main victory message branch shows '👑 Capital captured!' text. ui/mapPanel.js: _drawTile() uses crown icon '👑' instead of '⚔️' for enemy tiles with isFactionCapital; _showTileTip() shows '👑 Faction Capital' in capitalLabel and ownerHtml, adds capitalHtml div with gold '👑 Capture for peace + prestige!' row. main.js: on(FACTION_CAPITAL_CAPTURED) calls awardPrestige(150). No save version bump — tile.isFactionCapital lives in state.map.tiles which is already persisted."
  },
  {
    "id": "T094",
    "title": "resource overflow bank — convert capped surplus to gold at 25% efficiency",
    "description": "When any non-gold resource is at 99%+ of its cap with a positive production rate, the surplus that would be wasted each tick is instead converted to gold at 25% efficiency. Tracked module-level in resources.js (no state persistence needed). HUD cells show a gold-border 'full' glow when at cap. HUD tooltip shows overflow→gold conversion rate for full resources, and shows overflow income sources in the gold tooltip. No new state fields or save version bump.",
    "priority": 94,
    "phase": 31,
    "status": "done",
    "notes": "systems/resources.js: Added module-level _overflowActive={food,wood,stone,iron,mana} tracking object. resourceTick(): inside the RESOURCE_KEYS loop, for non-gold resources with positive delta, computes overflow=max(0, (current+delta)-cap); if >0 accumulates overflowGold+=overflow*0.25 and sets _overflowActive[res]=true; after the loop, applies overflowGold to state.resources.gold (capped at gold cap). getBreakdown(): for gold resId, appends overflow source lines ({label:'Food overflow', value:rate*0.25}) for each non-gold resource at 99%+ cap with positive rate; for non-gold resId, computes overflowGoldRate=rate*0.25 if at cap+positive rate, returns it as new field. ui/hud.js: _showTooltip() builds overflowRow from breakdown.overflowGoldRate (gold text, '💰 Overflow → gold: +X.XX/s'); injected into tooltip HTML. renderHUD(): toggles .hud__resource--full class when r.id!=='gold' && val>=cap*0.99 && rate>0. styles/main.css: .hud-tt__modifier--overflow (gold #ffd700 text); .hud__resource--full (gold border-color+subtle box-shadow). No save version bump — overflow is fully computed from existing state on every tick."
  },
  {
    "id": "T095",
    "title": "tile improvement level 2 upgrades",
    "description": "All 5 tile improvements (Farm/Sawmill/Quarry/Mine/Dock) can be upgraded to Level 2 once the player reaches Iron Age. Level 2 doubles production. Requires Iron Age + higher resource cost. Level 2 badge ('2') drawn on canvas in gold. Upgrade option shown in tile picker modal. tile.improvementLevel field persists automatically in state.map.tiles. No save version bump needed.",
    "priority": 95,
    "status": "done",
    "phase": 32,
    "notes": "data/improvements.js: Added level2 sub-object to each of the 5 improvement definitions with name/icon/desc/upgradeCost/production (double the level-1 rates). Advanced Farm (+4 food/s, 80wood+50stone+60gold), Advanced Sawmill (+4 wood/s, 80stone+80gold), Advanced Quarry (+2 stone/s, 80gold+60wood), Advanced Mine (+2 iron/s, 100gold+50wood), Advanced Dock (+2 gold/s+2 food/s, 80wood+60stone+60gold). core/actions.js: Added upgradeTileImprovement(x,y) — validates player ownership, existing level-1 improvement, Iron Age (age>=2), affordability; deducts upgradeCost, sets tile.improvementLevel=2, calls recalcRates(), emits MAP_CHANGED+RESOURCE_CHANGED, logs build message. systems/map.js: Updated territoryRateBonus() — when tile.improvementLevel===2 uses impDef.level2.production instead of impDef.production. ui/mapPanel.js: Imported upgradeTileImprovement; added 'T095: Upgrade improvement button' case in picker click handler (data-imp-upgrade attr); _showImprovementPicker() — when tile has level-1 improvement, shows .imp-upgrade-section card with name/desc/costs/Upgrade button (disabled if not Iron Age or can't afford), or .imp-upgrade-locked note if pre-Iron Age; updated tile tooltip impHtml to show Lv.2 badge or '(upgradeable)' hint; _drawTile() draws bold gold '2' in bottom-right corner of level-2 improved tiles. styles/main.css: .imp-upgrade-section (amber border+bg), .imp-upgrade-label/.imp-upgrade-desc, .imp-upgrade-locked, .imp-lv2-badge (gold pill), .btn--imp-upgrade (amber gradient)."
  },
  {
    "id": "T096",
    "title": "citizen role assignments",
    "description": "Players assign population groups to 4 roles: Scholars (-5% research time each), Merchants (+0.3 gold/s each), Workers (+4% production each), Soldiers (-5% training time each). Max total slots = floor(population / 100). Roles persist across new games. +/- buttons in Empire Summary card. state.citizenRoles. CITIZEN_ROLES_CHANGED event. Save version 32.",
    "priority": 96,
    "status": "done",
    "phase": 32,
    "notes": "core/events.js: Added CITIZEN_ROLES_CHANGED:'citizenRolesChanged'. core/state.js: Added citizenRoles:null (persists across new games, not in initState). core/actions.js: Added CITIZEN_ROLES constant (4 roles with icon/name/desc), CITIZEN_ROLE_ORDER array, adjustCitizenRole(role, delta) — validates role, initialises citizenRoles if needed, computes maxSlots=floor(pop/100), enforces bounds [0..max], calls recalcRates()+emits CITIZEN_ROLES_CHANGED+RESOURCE_CHANGED. systems/resources.js: After population income block, applies citizen role bonuses — merchants: rates.gold+=slots*0.3; workers: workerMult=1+slots*0.04 applied to all positive rates. systems/research.js: startResearch() applies scholarMult=max(0.50, 1-slots*0.05) to totalTicks when scholars>0. core/actions.js trainUnit(): applies soldierMult=max(0.50, 1-slots*0.05) after Training Grounds specialization. ui/summaryPanel.js: Imported CITIZEN_ROLES+CITIZEN_ROLE_ORDER+adjustCitizenRole; subscribed POPULATION_CHANGED+CITIZEN_ROLES_CHANGED→_render; added panel.onclick delegation for [data-citizen-role] buttons with btn--shake error feedback; added _citizenRolesCard() rendering header (slots counter), hint text, and 4 role rows (icon/name/count/+- buttons/desc) using grid layout; injected before _progressionCard() in _render(). main.js: citizenRoles in _save()/_applySave(); save version bumped 31→32. styles/main.css: .sum-citizen-header/title/slots/--full/hint, .sum-citizen-row (5-col grid), .sum-citizen-icon/name/count/btns/desc, .sum-citizen-btn (square icon-button), mobile hides desc column."
  },
  {
    "id": "T097",
    "title": "territorial expansion milestones",
    "description": "One-time resource + prestige rewards when player-owned tile count reaches 10 / 25 / 50 / 75 / 100. Tracked in state.expansionMilestones (persisted). Checked on every MAP_CHANGED event. No save version bump needed.",
    "priority": 97,
    "phase": 33,
    "status": "done",
    "notes": "main.js: Added EXPANSION_MILESTONES array (5 entries: thresholds 10/25/50/75/100 with resource rewards and prestige values). Added _checkExpansionMilestones() — counts player tiles, iterates milestones, awards resources (capped) + prestige, logs windfall message, marks threshold as awarded in state.expansionMilestones. Registered on(Events.MAP_CHANGED, _checkExpansionMilestones) in boot(). Added expansionMilestones to _save() (serialised as-is) and _applySave() (default {}). No save version bump — backwards-compatible default {}."
  },
  {
    "id": "T098",
    "title": "rally troops command",
    "description": "New military command in the Military panel: Rally (costs 50 gold + 25 mana) grants +1 XP to every trained unit type (may trigger veteran/elite rank promotions) and +5 morale. 5-minute cooldown. state.rallyState persists cooldown across saves.",
    "priority": 98,
    "phase": 33,
    "status": "done",
    "notes": "core/actions.js: Added RALLY_COST={gold:50,mana:25}, RALLY_COOLDOWN_TICKS=1200, RALLY_VETERAN_XP=3, RALLY_ELITE_XP=6 constants. Added rallyTroops() — validates hasUnits, checks cooldown (secsLeft in reason), checks canAfford, deducts cost, grants +1 XP to all trained unit types with rank-up promotion messages, boosts state.morale by +5 directly (avoiding circular import with morale.js), sets cooldownUntil, emits UNIT_CHANGED+MORALE_CHANGED+RESOURCE_CHANGED. ui/militaryPanel.js: Added rallyTroops to import; added _rallySection() rendering a card with cost display (color-coded by affordability), status line (ready/cooldown countdown/locked), and Rally button with data-action='rally'; inserted _rallySection() in _render() between _moraleSection() and _spellsSection(); added hasRallyCooldown check to tick handler so countdown refreshes; added 'rally' branch in _handleClick() with btn--shake error feedback. main.js: Added rallyState+expansionMilestones to _save() and _applySave(). styles/main.css: .rally-section, .rally-header, .rally-title, .rally-costs, .rally-desc, .rally-status (--ready/--cd/--locked), .btn--rally (purple gradient)."
  },
  {
    "id": "T099",
    "title": "region dominance bonuses",
    "description": "When player controls 5+ tiles of a terrain type, gain a passive bonus: Grass→+1.5 food/s, Forest→+1.5 wood/s, Hills→+1.5 stone/s, River→+1 gold/s+1 food/s, Mountain→+1.5 iron/s. At 10+ tiles the bonus doubles. Computed dynamically from map each recalcRates(). Shown in Empire Summary as a 'Terrain Control' card with per-terrain row (count, threshold bar, bonus). No new state or save version bump needed.",
    "priority": 99,
    "status": "done",
    "phase": 34,
    "notes": "systems/map.js: Added getTerrainControl() export — counts player-owned tiles per terrain type (grass/forest/hills/river/mountain). systems/resources.js: Imported getTerrainControl; added _regionBonus() helper (×1 at 5+ tiles, ×2 at 10+ tiles); applied bonuses in recalcRates() after territory block (grass→food+1.5/3, forest→wood+1.5/3, hills→stone+1.5/3, river→gold+1/2+food+1/2, mountain→iron+1.5/3). Added region breakdown entries in getBreakdown() for HUD tooltips. ui/summaryPanel.js: Imported getTerrainControl; added _terrainControlCard() with 5 terrain rows each showing icon/name/bar-filled-to-threshold/count/bonus (green if active, dim if locked); injected after _territoryCard() in _render(). styles/main.css: .sum-terrain-row (5-col grid), .sum-terrain--active (green left-border), .sum-terrain__icon/name/bar-wrap/bar/count/bonus/bonus--locked, .sum-terrain__hint; mobile col collapse."
  },
  {
    "id": "T100",
    "title": "capital development plan",
    "description": "One-time permanent strategic upgrade for the player capital chosen in the Building panel. Four plans: Fortress (300g+150s+100i, +20% combat attack, -20% enemy counterattack), Commerce (300g+150w, +2 gold/s, +500 gold cap), Academy (200g+100s+100m, -25% research time, +500 mana cap), Arcane Tower (200m+150s+100i, +1.5 mana/s, -25% spell mana cost). state.capitalPlan (reset on new game). CAPITAL_PLAN_CHOSEN event. Apply in resources.js, research.js, spells.js, combat.js, enemyAI.js. Save version 33.",
    "priority": 100,
    "status": "done",
    "phase": 34,
    "notes": "Created src/data/capitalPlans.js: 4 plan definitions (fortress/commerce/academy/arcane_tower) each with id/icon/name/desc/cost/bonusDesc/requiresAge. core/events.js: CAPITAL_PLAN_CHOSEN event added. core/state.js: capitalPlan:null field added to static object + initState() reset. core/actions.js: imported CAPITAL_PLANS; added chooseCapitalPlan(planId) — validates plan exists, no existing plan, age requirement, affordability; deducts cost, sets state.capitalPlan, calls recalcRates(), emits CAPITAL_PLAN_CHOSEN+RESOURCE_CHANGED, logs message. systems/resources.js: commerce plan +2 gold/s +500 gold cap; academy plan +500 mana cap; arcane_tower plan +1.5 mana/s — all applied before Object.assign. systems/research.js: academy plan ×0.75 totalTicks in startResearch(). systems/spells.js: arcane_tower plan ×0.75 mana cost (stacks multiplicatively with arcane archetype). systems/combat.js: fortress plan ×1.20 attackPower in both getAttackPreview() and attackTile(), applied after War Banner decree. systems/enemyAI.js: fortress plan ×0.80 winChance in _counterattack(). ui/buildingPanel.js: imported chooseCapitalPlan+CAPITAL_PLANS+CAPITAL_PLAN_ORDER; added CAPITAL_PLAN_CHOSEN listener; added choose-plan action in panel.onclick; added _capitalPlansSection() — if plan chosen: shows gold-bordered active-plan card with icon/name/bonus list; else: shows plan grid with 4 .cap-plan-card entries (header icon/name/cost, desc, bonus list, age-lock notice if needed, Establish Plan button); builds fail gracefully with btn--shake. ui/summaryPanel.js: added CAPITAL_PLAN_CHOSEN listener; added capital plan row in _statsCard(); re-renders on plan selection. main.js: capitalPlan in _save()/_applySave(); save version bumped 32→33; CAPITAL_PLAN_CHOSEN→_updateScoreBadge. styles/main.css: .cap-plan-section (gold border-left), .cap-plan-section__header/intro, .cap-plan-grid (auto-fill 200px), .cap-plan-card/--locked, .cap-plan-card__header/name/cost, .cap-plan-cost--ok/bad, .cap-plan-card__desc, .cap-plan__bonus/lock, .btn--cap-plan (gold gradient), .cap-plan-chosen (active card, gold border+tint), .cap-plan-name/.cap-plan-bonuses."
  },
  {
    "id": "T101",
    "title": "conquest streak system",
    "description": "Consecutive combat victories build a streak counter. Tiers: 3+ wins → Momentum (+10% attack), 6+ wins → Battle Fury (+20% attack), 10+ wins → Unstoppable (+35% attack, doubled loot). Streak resets on any loss. Streak badge in title bar when active. Streak tier shown in combat preview modal. state.combatStreak persisted. STREAK_CHANGED event. No save version bump.",
    "priority": 101,
    "status": "done",
    "phase": 35,
    "notes": "STREAK_CHANGED event added to events.js. state.combatStreak={count,lastWinTick} added to static state + initState() reset. combat.js: added _streakTier()/_streakMult()/_streakLootMult() helpers; getAttackPreview() applies streak mult after skirmish, returns streakCount+streakTier; attackTile() applies streak mult before roll; _victory() increments streak, logs tier-milestone messages (at 3/6/10 wins), emits STREAK_CHANGED; _defeat() resets streak to 0, logs break msg when was ≥3, emits STREAK_CHANGED; _victory() loot uses lootMult×streakLoot so Unstoppable doubles all loot. index.html: #streak-badge added between siege-badge and score-badge. main.js: imported; _updateStreakBadge() shows badge with tier label (🔥/⚡/💥) and modifier tooltip; subscribed STREAK_CHANGED in boot; called in _newGame(); combatStreak saved/loaded (backwards-compatible default {count:0,lastWinTick:0}). mapPanel.js: _showCombatPreview() renders .cp-streak-notice showing tier label+bonus when streakTier>0, or pending hint at 2+ wins. CSS: .streak-badge/--t0/--t1/--t2/--t3 (color-coded pills), --t3 has streak-glow keyframe animation; .cp-streak-notice (amber tinted notice in modal); hidden on ≤600px."
  },
  {
    "id": "T102",
    "title": "alliance military aid",
    "description": "When allied with an AI empire, request temporary troop reinforcements once per 15 minutes (costs 80 gold). Aid units vary by empire: Iron Horde→3 soldiers+1 knight, Mage Council→2 mages, Sea Wolves→3 archers. Aid troops contribute to attack power for up to 5 battles then depart. Active aid shown in Military panel army section and combat preview. Diplomacy panel shows Request Aid button per allied empire with cooldown. MILITARY_AID_CHANGED event. state.militaryAid saved/loaded. No save version bump.",
    "priority": 102,
    "status": "done",
    "phase": 35,
    "notes": "Created src/systems/militaryAid.js: AID_COST=80g, AID_BATTLES=5, AID_COOLDOWN_TICKS=3600 (15min); initMilitaryAid() idempotent guard; canRequestAid(empireId) validates alliance/gold/cooldown/no-duplicate; requestMilitaryAid(empireId) deducts gold, creates active entry with unit counts, sets per-empire cooldown, logs windfall message, emits MILITARY_AID_CHANGED; consumeAidBattle() decrements battlesLeft, clears active at 0 and logs departure; getActiveAid()/getAidCooldownSecs() helpers. data/empires.js: added aidUnits array to all 3 empires (ironHorde→3 soldiers+1 knight, mageCouncil→2 mages, seaWolves→3 archers). events.js: MILITARY_AID_CHANGED added. state.js: militaryAid:null added to static object + initState() reset. combat.js: imported getActiveAid+consumeAidBattle; getAttackPreview() sums aid unit attack after skirmish, returns aidActive/aidEmpireId/aidBattlesLeft fields; attackTile() adds aid attack before streak mult; replaced if/else return with const result = ... then consumeAidBattle() before return (ensures each battle consumes 1 charge win or loss). main.js: imported initMilitaryAid; called in boot+_newGame; militaryAid saved/loaded. ui/diplomacyPanel.js: imported all aid exports; subscribed MILITARY_AID_CHANGED→render; TICK handler checks hasAidActivity; _aidRow(emp) renders aid row for allied empires (active badge / request button with cooldown countdown); injected into _empireCard() between gift row and actions; request-aid case in _onClick() with btn--shake on error. ui/militaryPanel.js: imported getActiveAid; subscribed MILITARY_AID_CHANGED→render; _armySection() appends .mil-aid-row when aid active showing empire+units+battlesLeft. mapPanel.js: _showCombatPreview() renders green .cp-siege-notice for active aid showing battles remaining. CSS: .dipl-aid-row/--active, .dipl-aid-label/.dipl-aid-cd, .btn--request-aid (green border/hover), .mil-aid-row (green tinted)."
  },
  {
    "id": "T103",
    "title": "empire festivals — temporary production and combat boosts",
    "description": "Three player-declared festivals in the Research/Governance panel: Harvest Festival (80g+40 food → +60% food/wood 90s, +4 morale), Military Parade (100g+30 iron → +25% attack 3 battles, +6 morale), Grand Trade Fair (150g → +50% gold 90s, +3 morale). One active festival at a time. 8-minute cooldown after each. FESTIVAL_CHANGED event. state.festivals saved/loaded. Save version 34.",
    "priority": 103,
    "status": "done",
    "phase": 36,
    "notes": "Created src/data/festivals.js: 3 festival definitions (harvest/parade/trade_fair) with icon/name/desc/cost/durationTicks|charges/moraleDelta/effects; FESTIVAL_ORDER array; FESTIVAL_COOLDOWN_TICKS=1920 (8min). Created src/systems/festivals.js: initFestivals() idempotent guard; useFestival(type) validates no-active/not-on-cooldown/affordability, deducts cost, applies morale, builds active record, calls recalcRates(), emits FESTIVAL_CHANGED; festivalTick() expires timed festivals (harvest/trade_fair) at expiresAt, clears active, sets cooldownUntil, calls recalcRates(); parade charges consumed by combat.js; getActiveFestival()/getFestivalSecsLeft()/getFestivalCooldownSecs() helpers. core/events.js: FESTIVAL_CHANGED added. core/state.js: festivals:null field in static object + initState() reset. systems/resources.js: _festActive block after decree section — harvest applies ×1.6 food+wood, trade_fair applies ×1.5 gold. systems/combat.js: getAttackPreview() applies ×1.25 to parade active (_paradeActive), returns paradeChargesLeft; attackTile() applies ×1.25 when parade active (_paradeUp); T103 charge-consume block after war banner consume — decrements chargesLeft, clears active+sets cooldownUntil at 0, emits FESTIVAL_CHANGED. ui/researchPanel.js: imported FESTIVALS/FESTIVAL_ORDER/useFestival/helpers; subscribed FESTIVAL_CHANGED→render + throttled TICK→render while active/on-cooldown; _festivalsSection() renders active-banner (time or charges countdown), cooldown-banner, or 3 .festival-card entries with affordability-coloured costs and Declare button; panel.onclick handles data-festival attr calling useFestival(). main.js: imported initFestivals+festivalTick; registerSystem(festivalTick); initFestivals() in boot+_newGame; festivals saved/loaded; save version 33→34. styles/main.css: .festival-section/header/intro, .festival-active-banner (gold border+tint), .festival-active-icon/body/time/charges, .festival-cooldown-banner, .festival-grid (auto-fill 190px), .festival-card/--active, .festival-card__header/icon/name, .festival-badge (gold pill), .festival-card__desc/cost, .festival-cost--ok/bad, .btn--festival (gold hover), mobile single-column."
  },
  {
    "id": "T105",
    "title": "empire title system — milestone-based titles with passive bonuses",
    "description": "As the player's empire grows, they earn progressively grander titles based on territory and age milestones. Five tiers: Village Chief (start), Regional Lord (15 territory), High Lord (30 territory + Bronze Age), King (50 territory + Iron Age), Emperor (80 territory + Medieval Age). Each title grants cumulative production and combat bonuses. Title badge shown in title bar. TITLE_EARNED event. State: state.titleHistory (reset per game). Story entry on each new title. Bonuses applied in resources.js and combat.js.",
    "priority": 105,
    "status": "done",
    "phase": 37,
    "notes": "Created src/data/titles.js: TITLES array (5 entries: village_chief/regional_lord/high_lord/king/emperor) with icon/requires{territory,age}/bonusDesc/bonus{gold,ratesMult,combatMult}; getCurrentTitle(state) returns highest earned title. core/events.js: TITLE_EARNED added. core/state.js: titleHistory:[] field + reset in initState(). systems/resources.js: imported getCurrentTitle; applied title bonus.gold flat rate and bonus.ratesMult (×(1+mult)) to all positive rates at end of recalcRates(). systems/combat.js: imported getCurrentTitle; applied (1+combatMult) to attackPower in both getAttackPreview() and attackTile() after fortress capital plan. main.js: imported TITLES+getCurrentTitle; _lastTitleLevel module var; _checkTitle() compares current title level to _lastTitleLevel, earns skipped titles in order (addMessage achievement log + TITLE_EARNED emit + recalcRates); _updateTitleBadge() updates #title-badge span; subscribed MAP_CHANGED+AGE_CHANGED→_checkTitle; _lastTitleLevel reset to 0 in _newGame; titleHistory saved/loaded (no version bump — backwards-compatible []). systems/story.js: subscribed TITLE_EARNED+RUIN_EXCAVATED→story entries. index.html: #title-badge span added between streak-badge and score-badge. CSS: .title-badge (green-tinted pill badge, cursor:help), hidden on ≤600px."
  },
  {
    "id": "T106",
    "title": "ancient ruins excavation — map sites with random capture rewards",
    "description": "Four ancient ruin sites placed during map generation on neutral tiles at distance 5-10 from capital. Canvas: 🏛 icon with dark teal tint on revealed ruin tiles. Tile tooltip shows 'Ancient Ruins — Capture to excavate!'. When the player captures a ruin tile via combat, an excavation roll fires (35% resource cache, 30% ancient wisdom, 20% cursed trap, 15% lost artifact). Resource cache: +100 gold +50 mana +50 iron. Ancient wisdom: -120s from current research, or +1 XP to all units. Cursed trap: -8 morale, -80 gold. Lost artifact: permanent +0.8 gold/s +100 gold cap (via ruin bonus loop in resources.js). Ruins section in Research panel. +80 prestige on excavation. RUIN_EXCAVATED event. state.ruins saved/loaded. No save version bump.",
    "priority": 106,
    "status": "done",
    "phase": 37,
    "notes": "Created src/data/ruins.js: RUIN_COUNT=4, RUIN_OUTCOMES array (4 outcomes with id/weight/icon/name/desc/apply); rollRuinOutcome() weighted random selection. systems/map.js: imported RUIN_COUNT; added _placeRuins(tiles) — finds neutral tiles at dist 5-10 without landmarks, places hasRuin='ruin_N' + defense+8; called in initMap() after _placeLandmarks; state.ruins={excavated:{}} set. core/events.js: RUIN_EXCAVATED added. core/state.js: ruins:null field in static object + initState() reset. systems/combat.js: imported rollRuinOutcome; added _tryExcavateRuin(tile,x,y) — checks tile.hasRuin, rolls outcome, calls outcome.apply(state), records in state.ruins.excavated[ruinId], calls recalcRates(), logs windfall message, emits RUIN_EXCAVATED; called in _victory() after clearResourceNode. systems/resources.js: ruins bonus loop — lost_artifact outcome adds +0.8 gold/s +100 gold cap per excavated artifact. ui/mapPanel.js: RUIN_TINT constant (dark teal); _drawTile() applies teal tint + 🏛 glyph for revealed unexcavated ruin tiles; _showTileTip() shows ruinHtml (teal colored 'Ancient Ruins' or 'excavated' badge); RUIN_EXCAVATED subscription re-renders; index.html: no change. ui/researchPanel.js: imported RUIN_COUNT+RUIN_OUTCOMES; RUIN_EXCAVATED subscription; _ruinsSection() renders 🏛️ Ancient Ruins section with 4 relic-card style entries (found shows outcome icon/name/bonus, locked shows '🏛️ Capture to excavate'); appended in panel.innerHTML. ui/summaryPanel.js: imported RUIN_COUNT; RUIN_EXCAVATED subscription; added '🏛️ Ruins X/4' row in _progressionCard(). systems/story.js: RUIN_EXCAVATED subscription fires _onRuinExcavated() story milestone. main.js: RUIN_EXCAVATED listener calls awardPrestige(80); ruins saved/loaded (backwards-compatible null default)."
  },
  {
    "id": "T104",
    "title": "map resource node events — temporary capture-for-bonus spots",
    "description": "Every 5–8 minutes, 1–2 glowing resource nodes appear on revealed neutral tiles. Each node has a terrain-matching resource reward (scaled with tick). Nodes expire after 4 minutes if uncaptured. Canvas: amber ✦ glyph on node tiles. Map stats bar shows live count. Tile tooltip shows resource reward and countdown. Capturing the node grants the resources and removes it. RESOURCE_NODE_CHANGED event. state.resourceNodes saved/loaded. No save version bump.",
    "priority": 104,
    "status": "done",
    "phase": 36,
    "notes": "Created src/systems/resourceNodes.js: TERRAIN_NODE lookup (grass→gold/forest→wood/hills→stone/river→food/mountain→iron) with base reward amounts; _scaledAmount() applies +10% per 500 ticks; initResourceNodes() idempotent struct guard; resourceNodeTick() expires nodes and spawns 1–2 new nodes per wave (SPAWN_MIN=1200/MAX=1920 ticks); _spawnWave() finds revealed neutral tiles excluding caravan tile and existing nodes, picks random spots; collectResourceNode(x,y) grants resources (capped), removes node, emits RESOURCE_CHANGED+RESOURCE_NODE_CHANGED; clearResourceNode(x,y) silently removes node (called on enemy expansion or player combat capture); getNodeAt()/nodeSecsLeft()/activeNodeCount() helpers. events.js: RESOURCE_NODE_CHANGED added. state.js: resourceNodes:null in static object + initState() reset. systems/combat.js: imported clearResourceNode; _victory() calls clearResourceNode(x,y) after _tryCaptureLandmark. systems/enemyAI.js: imported clearResourceNode; _expandEnemies() calls clearResourceNode(nx,ny) before setting owner='enemy'. ui/mapPanel.js: imported collectResourceNode/getNodeAt/nodeSecsLeft/activeNodeCount; added NODE_TINT='rgba(255,170,0,0.35)' constant; initMapPanel() subscribes RESOURCE_NODE_CHANGED→_render; _drawTile() applies amber glow tint + draws '✦' glyph at tile centre for active nodes; _showTileTip() detects isNodeTile (getNodeAt) and shows gold-coloured resource/amount/countdown row + 'Click to collect' action row; _onClick() checks node first (before caravan) and calls collectResourceNode; _updateStats() appends '✦ Nodes: N' to stats bar when nodes>0. main.js: imported initResourceNodes+resourceNodeTick; registerSystem(resourceNodeTick) as 23rd system; initResourceNodes() in boot+_newGame; resourceNodes saved/loaded (no save version bump — backwards-compatible null default)."
  },
  {
    "id": "T107",
    "title": "unit arsenal upgrades — permanent per-unit attack boosts",
    "description": "Players invest gold to permanently upgrade each unit type's attack power. Each upgrade level adds +10% attack (up to 5 levels, max +50%). Cost scales as 100×level gold. Upgrade button per unit type in Military panel (only shows for trained units). Upgrade multiplier applied in both getAttackPreview() and attackTile(). UNIT_UPGRADED event. state.unitUpgrades saved/loaded. No save version bump.",
    "priority": 107,
    "status": "done",
    "phase": 38,
    "notes": "core/events.js: Added UNIT_UPGRADED:'unitUpgraded'. core/state.js: Added unitUpgrades:{} to static object + initState() reset. core/actions.js: Added UNIT_UPGRADE_MAX=5, UNIT_UPGRADE_COST_BASE=100, upgradeUnit(unitId) — validates unit present, checks level<max, deducts 100×(level+1) gold, increments state.unitUpgrades[unitId], emits UNIT_UPGRADED+UNIT_CHANGED+RESOURCE_CHANGED, logs build message. systems/combat.js: Applied upgradeMult=1+(unitUpgrades?.[id]??0)*0.10 to per-unit attack calculation in both getAttackPreview() and attackTile() unit loops. ui/militaryPanel.js: Imported upgradeUnit/UNIT_UPGRADE_MAX/UNIT_UPGRADE_COST_BASE; subscribed UNIT_UPGRADED→_render; added _upgradeSection() — renders .upgrade-section card with title/intro and one .upgrade-row per trained unit type (icon/name/level badge/Upgrade button with cost); injected after _rallySection() in _render(); added 'upgrade-unit' action handler in _handleClick() with btn--shake error feedback. main.js: unitUpgrades saved/loaded (backwards-compatible default {}). styles/main.css: .upgrade-section (blue left border), .upgrade-title/.upgrade-intro, .upgrade-row (4-col grid), .upgrade-icon/.upgrade-name/.upgrade-level/--max, .btn--upgrade (blue border), .upgrade-btn--max (gold)."
  },
  {
    "id": "T108",
    "title": "map exploration milestones — fog-of-war reveal rewards",
    "description": "One-time resource + prestige rewards when 50/75/90% of map tiles are revealed. 50%: +100 gold, +80 prestige. 75%: +150 gold +50 mana, +120 prestige. 90%: +200 gold +100 mana, +150 prestige + permanent +0.8 gold/s. Exploration % shown in Empire Summary progression card with progress bar. EXPLORATION_MILESTONE event. state.explorationMilestones saved/loaded. No save version bump.",
    "priority": 108,
    "status": "done",
    "phase": 38,
    "notes": "core/events.js: Added EXPLORATION_MILESTONE:'explorationMilestone'. core/state.js: Added explorationMilestones:{} to static object + initState() reset. systems/resources.js: Added state.explorationMilestones?.[90] check before Object.assign — applies +0.8 gold/s permanent bonus when 90% milestone reached. main.js: Added EXPLORATION_MILESTONE_DEFS array (3 milestones at 50/75/90%); added _checkExplorationMilestones() — counts revealed/total tiles, computes exploredPct, awards resources+prestige for each unearned milestone, calls recalcRates() for 90% milestone, logs windfall messages, emits RESOURCE_CHANGED+EXPLORATION_MILESTONE; registered on(Events.MAP_CHANGED, _checkExplorationMilestones) in boot(); unitUpgrades+explorationMilestones added to _save()/_applySave() (backwards-compatible defaults). ui/summaryPanel.js: Subscribed EXPLORATION_MILESTONE→_render; _progressionCard() now computes revealed % from state.map and renders exploration progress row (% label + next milestone hint) + progress bar below Ruins row. No save version bump — backwards-compatible {} default."
  },
  {
    "id": "T109",
    "title": "warlord duel events — champion challenges from enemy war empires",
    "description": "While the player is at war with an enemy empire and has a recruited, non-injured hero not on expedition, enemy warlords periodically issue champion duel challenges (4–7 min cooldown, 45 s window). Accept: win chance 30% + 15% per hero skill (max 85%); +10% Conqueror bonus. Win: +150–300 gold, +100 prestige, morale +10, enemy raid deferred 120 s. Lose: hero injured 5 min, morale −12, −50 gold. Decline: morale −5. Notification banner at top of Military panel with Accept/Decline buttons and countdown timer. DUEL_CHANGED event. state.duels saved/loaded. No save version bump.",
    "priority": 109,
    "status": "done",
    "phase": 39,
    "notes": "Created src/systems/duels.js: WARLORD_NAMES map (ironHorde/mageCouncil/seaWolves); initDuels() idempotent null guard; duelTick() — expires stale pending challenge, tries to spawn if conditions met (hero recruited+healthy+not on expedition, at least one war empire); acceptDuel() — computes win chance, resolves combat (gold loot, prestige, morale, hero injury, enemy raid deferral), emits DUEL_CHANGED; declineDuel() — morale -5, emits DUEL_CHANGED; getDuelSecsLeft() helper. core/events.js: Added DUEL_CHANGED:'duelChanged'. core/state.js: Added duels:null to static object + initState() reset. src/ui/militaryPanel.js: Imported acceptDuel/declineDuel/getDuelSecsLeft; subscribed DUEL_CHANGED→_render; added hasDuelPending to TICK throttle check; added _duelSection() rendering pulsing red banner with warlordName/secsLeft/urgent blink/Accept+Decline buttons; injected before _mercenarySection() in _render(); added 'duel-accept' and 'duel-decline' action handlers in _handleClick(). main.js: Imported initDuels+duelTick; registerSystem(duelTick); initDuels() in boot+_newGame; duels saved/loaded (backwards-compatible null default). styles/main.css: .duel-banner (red tint, pulsing border animation), .duel-banner__header/icon/title/timer, --urgent blink animation, .duel-banner__sub/hint, .duel-banner__actions, .btn--duel-accept (red), .btn--duel-decline (muted)."
  },
  {
    "id": "T110",
    "title": "pioneer expeditions — colonists settle distant uninhabited lands",
    "description": "Players can dispatch a pioneering party to claim distant neutral territory. Cost: 120 food + 80 wood. Duration: 2.5 minutes (600 ticks). On completion: captures 2–3 neutral tiles near a randomly-chosen distant point (dist 8–16 from capital), reveals surrounding fog, awards +60 prestige. Maximum 3 expeditions per game. Progress bar shown in Military panel during expedition. PIONEER_CHANGED event. state.pioneers saved/loaded. No save version bump.",
    "priority": 110,
    "status": "done",
    "phase": 39,
    "notes": "Created src/systems/pioneerExpeditions.js: PIONEER_COST={food:120,wood:80}, PIONEER_MAX=3, EXPEDITION_TICKS=600; _pickTarget() scans map for neutral tiles at dist [8,16] from capital; initPioneers() idempotent null guard; sendPioneerExpedition() — validates no active expedition, sent<MAX, affordability, picks target, deducts cost, sets state.pioneers.active={endsAt,cx,cy}; pioneerTick() — on expiry captures up to 3 neutral tiles using CAPTURE_OFFSETS, calls revealAround(), recalcRates(), awardPrestige(60), emits PIONEER_CHANGED+MAP_CHANGED; getPioneerProgress()/getPioneerSecsLeft() helpers. core/events.js: Added PIONEER_CHANGED:'pioneerChanged'. core/state.js: Added pioneers:null to static object + initState() reset. src/ui/militaryPanel.js: Imported sendPioneerExpedition/getPioneerProgress/getPioneerSecsLeft/PIONEER_COST/PIONEER_MAX; subscribed PIONEER_CHANGED→_render; added hasPioneerActive to TICK throttle check; added _pioneerSection() — shows active progress bar with secsLeft OR idle Dispatch button with cost/remaining count OR exhausted message; injected after _combatHistorySection() in _render(); added 'pioneer-send' action handler in _handleClick(). main.js: Imported initPioneers+pioneerTick; registerSystem(pioneerTick); initPioneers() in boot+_newGame; pioneers saved/loaded (backwards-compatible null default). styles/main.css: .pioneer-section (green left border), .pioneer-header/intro, .pioneer-active/label/hint, .pioneer-progress-wrap/bar (animated green progress), .pioneer-idle, .pioneer-cost/--bad, .btn--pioneer (green), .pioneer-meta, .pioneer-exhausted."
  },
  {
    "id": "T111",
    "title": "map natural disasters — tile improvement damage events",
    "description": "Every 8–10 minutes a natural disaster strikes player-owned tiles, destroying tile improvements (and optionally fortifications) based on terrain type. Four disaster types: Wildfire (forest, destroys sawmill), Earthquake (mountain/hills, destroys improvement+fortification), Flood (river, destroys dock), Locust Swarm (grass, destroys farm). If no eligible player tiles exist the event silently skips. NATURAL_DISASTER event. state.naturalDisasters saved/loaded. No save version bump.",
    "priority": 111,
    "status": "done",
    "phase": 40,
    "notes": "Created src/systems/naturalDisasters.js: SPAWN_MIN=1920/MAX=2400 ticks (8–10 min); DISASTER_TYPES array (4 entries: wildfire/earthquake/flood/locust_swarm) each with terrain targets, affectsImprovement, affectsFortification; initNaturalDisasters() idempotent null guard with migration; naturalDisasterTick() fires on schedule, picks random type, finds player tiles of matching terrain with improvements/fortifications (candidates), shuffles, takes up to MAX_TARGETS=2, clears tile.improvement+tile.improvementLevel (improvement) or tile.fortified+defense-=15 (fortification), calls recalcRates(), emits MAP_CHANGED+RESOURCE_CHANGED+NATURAL_DISASTER, logs disaster-type message with affected coords. core/events.js: Added NATURAL_DISASTER:'naturalDisaster'. core/state.js: Added naturalDisasters:null to static object + initState() reset. main.js: Imported initNaturalDisasters+naturalDisasterTick; registerSystem(naturalDisasterTick); initNaturalDisasters() called in boot+_newGame; naturalDisasters saved/loaded (backwards-compatible null default). No new CSS needed — uses existing 'disaster' toast type."
  },
  {
    "id": "T112",
    "title": "hero legendary quest chain — 3-phase permanent reward progression",
    "description": "When the Champion hero reaches 10+ combat victories, a 3-phase Legendary Quest unlocks. Phase 1 (Battle Master, 5 wins): +20 permanent flat attack power. Phase 2 (War Strategist, 3 more wins): hero ability cooldowns halved. Phase 3 (Supreme Commander, 5 more wins): hero abilities have zero cooldown + +200 prestige. Progress shown in Military panel with a phase progress bar. HERO_QUEST_CHANGED event. Fields stored on state.hero (legendaryQuest, legendaryAttack, cdReduction, supremeCommander). No save version bump.",
    "priority": 112,
    "status": "done",
    "phase": 40,
    "notes": "core/events.js: Added HERO_QUEST_CHANGED:'heroQuestChanged'. core/actions.js: recruitHero() adds legendaryQuest:null/legendaryAttack:0/cdReduction:false/supremeCommander:false to hero state; useHeroAbility() now computes cdTicks — supremeCommander sets 0, cdReduction sets floor(cd/2), else full cd. systems/combat.js: Both getAttackPreview() and attackTile() hero bonus blocks add state.hero.legendaryAttack after hero skills; _trackHeroCombatWin() calls new _handleLegendaryQuest(h) — initialises legendaryQuest at 10+ wins with unlock message, then on each win checks winsThisPhase >= PHASE_WINS_REQUIRED[phase]; on phase completion applies reward (legendaryAttack+=20 / cdReduction=true / supremeCommander=true), advances phase, emits HERO_QUEST_CHANGED; _LEGENDARY_PHASE_WINS=[5,3,5] constant array. ui/militaryPanel.js: Added HERO_QUEST_CHANGED listener→_render; added _heroLegendarySection() function — shows teaser when 5≤wins<10, shows phase card with title/desc/progress bar/wins meta when active, shows completion card with earned bonuses when phase=3; injected into all 3 hero card views (active/expedition/injured). main.js: on(HERO_QUEST_CHANGED, d→if phase===3 awardPrestige(200)); migration guard in _applySave() adds legendaryAttack/cdReduction/supremeCommander defaults for older saves. styles/main.css: .hero-legendary-section (gold border-left accent), --teaser (dim), --complete (gold tint), .hero-legendary-title (gold), .hero-legendary-desc/.meta (dim 11px), .hero-legendary-progress-wrap/.bar (gold gradient animated), .hero-legendary-rewards/.reward (gold)."
  },
  {
    "id": "T113",
    "title": "spy network upgrades — 3 levels improving espionage success and cooldowns",
    "description": "Players can upgrade their spy network through 3 levels. Level 1 \"Agent Network\" (150 gold): +15% success rate, cooldown -15s. Level 2 \"Intelligence Bureau\" (300 gold): +25% success rate, cooldown -30s, failed gold_heist refunds 50% cost. Level 3 \"Shadow Ministry\" (600 gold): +35% success rate, cooldown -45s, gold heist loot +100 bonus. Requires espionage tech. State: state.espionage.networkLevel (0-3). Action: upgradeSpyNetwork() in espionage.js. Upgrade section shown below espionage missions in diplomacy panel. No new event needed — reuse ESPIONAGE_EVENT with type upgrade. No save version bump.",
    "priority": 113,
    "status": "done",
    "phase": 41,
    "notes": "systems/espionage.js: Added NETWORK_LEVELS array (4 entries: level 0-3 with successBonus/cooldownRedSecs/heistBonus/counterspy); upgradeSpyNetwork() validates tech+gold, increments networkLevel, deducts cost, emits ESPIONAGE_EVENT with type upgrade; getNetworkLevel()/getNextNetworkLevel() helpers; effectiveCooldownTicks() computes ESPIONAGE_COOLDOWN minus network cooldownRed; _successChance() now adds networkLevel.successBonus (max cap raised 0.90→0.95); _resolve() for gold_heist uses netDef.heistBonus on success, netDef.counterspy halves penalty on failure; sabotage failure counter-raid halved by counterspy passive. ui/diplomacyPanel.js: imported upgradeSpyNetwork/getNetworkLevel/getNextNetworkLevel/NETWORK_LEVELS; _spyNetworkSection() renders upgrade widget (name, 4 pip indicators, bonuses summary, upgrade button with cost); injected into _espionageSection() return; data-action spy-upgrade handler calls upgradeSpyNetwork(). styles/main.css: .spy-network-section (dark bg, rounded border), .spy-net-row/name/pips, .spy-net-pip/--filled (blue dot), .spy-net-bonuses (dim), .spy-net-upgrade/.spy-net-maxed, .btn--spy-upgrade (blue accent)."
  },
  {
    "id": "T114",
    "title": "alliance favor — accumulate diplomatic capital for powerful one-time requests",
    "description": "Allied empires accumulate favor points over time (1 point per 60 ticks = 15 seconds, max 50). Players can spend favor on three requests: \"Gold Loan\" (25 favor) → +300 gold; \"Supply Convoy\" (35 favor) → +100 food + 50 wood + 50 stone; \"War Party\" (45 favor) → +10 morale + +80 prestige. Favor resets to 0 when relations change away from allied. State: emp.favor (number) on each diplomacy empire entry. Action: requestAllianceFavor(empireId, requestType) in diplomacy.js. ALLIANCE_FAVOR_CHANGED event. Shown in each allied empire card in diplomacy panel. No save version bump.",
    "priority": 114,
    "status": "done",
    "phase": 41,
    "notes": "core/events.js: Added ALLIANCE_FAVOR_CHANGED event. systems/diplomacy.js: Added FAVOR_MAX=50, FAVOR_ACCRUAL_TICKS=60*TPS, FAVOR_REQUESTS object (gold_loan/supply_convoy/war_party with label/cost/desc); initDiplomacy() migration adds emp.favor=0/emp.nextFavorTick per empire; diplomacyTick() accumulates +1 favor per FAVOR_ACCRUAL_TICKS for allied empires (cap FAVOR_MAX), emits ALLIANCE_FAVOR_CHANGED; declareWar() resets emp.favor=0; requestAllianceFavor(empireId, requestType) validates allied+cost, deducts favor, delivers reward (300 gold / +100food+50wood+50stone / +10 morale+80 prestige via dynamic import), emits ALLIANCE_FAVOR_CHANGED+DIPLOMACY_CHANGED, logs to history. ui/diplomacyPanel.js: imported requestAllianceFavor/FAVOR_MAX/FAVOR_REQUESTS; added on(ALLIANCE_FAVOR_CHANGED) listener; _favorRow(emp) renders favor progress bar + 3 request buttons per allied empire; injected into _empireCard() between _aidRow and actions; data-action alliance-favor handler calls requestAllianceFavor(). styles/main.css: .dipl-favor-row (bordered top section), .dipl-favor-header/label, .dipl-favor-bar-wrap/.bar (purple gradient progress), .dipl-favor-requests, .btn--favor (purple accent)."
  },
  {
    "id": "T115",
    "title": "seasonal market commodities",
    "description": "Each season, 1–2 resources trade at premium on the Market: Spring=food (×2 sell / ×0.5 buy), Summer=wood, Autumn=stone+iron, Winter=mana. Seasonal badge on market rows, highlight note at panel top. SEASON_CHANGED re-renders market panel. No state or save version changes needed.",
    "priority": 115,
    "status": "done",
    "phase": 42,
    "notes": "systems/market.js: Added SEASONAL_COMMODITIES constant (4 season→resource-array mapping), SEASONAL_SELL_MULT=2.0, SEASONAL_BUY_MULT=0.5, getSeasonalCommodities() export; updated buyPrice() to multiply by SEASONAL_BUY_MULT and sellPrice() by SEASONAL_SELL_MULT when resource is in current season's commodity list. ui/marketPanel.js: imported getSeasonalCommodities; added on(SEASON_CHANGED,_render) listener; _render() computes seasonal list, builds seasonalNote banner showing resource names when non-empty; _row() now accepts seasonal[] param, adds .market-row--seasonal class and 🌟 Seasonal badge when applicable. styles/main.css: .market-seasonal-note (purple tint banner), .market-seasonal-badge (purple pill), .market-row--seasonal faint tint, .market-row--seasonal .market-price--sell green bold, .market-row--seasonal .market-price--buy red bold."
  },
  {
    "id": "T116",
    "title": "research inspiration events",
    "description": "Random flash-of-inspiration events fire every 3–5 minutes when research is active. Three types: Breakthrough (−25% remaining time on current research), Mana Surge (+40 mana), Workshop Boost (−20% time on next research). Player sees a notification card in the Research panel with Accept/Dismiss buttons and 90s countdown. RESEARCH_INSPIRATION event. state.researchInspiration saved/loaded. No save version bump.",
    "priority": 116,
    "status": "done",
    "phase": 42,
    "notes": "Created src/systems/researchInspiration.js: INSPIRATION_TYPES array (3 entries); initInspiration() idempotent null guard with nextCheckTick migration; inspirationTick() expires stale pending event or spawns new one when researchQueue non-empty + cooldown elapsed (3–5 min random interval); acceptInspiration() applies effect (breakthrough: cuts entry.remaining by 25%, mana_surge: +40 mana+RESOURCE_CHANGED, workshop_boost: sets workshopDiscount flag); dismissInspiration() clears pending and reschedules; getInspirationSecsLeft() countdown helper. core/events.js: RESEARCH_INSPIRATION event added. core/state.js: researchInspiration:null added to static object + initState() reset. systems/research.js: workshopDiscount applied after academy capital plan — totalTicks*=0.80, flag cleared, message logged. ui/researchPanel.js: imported acceptInspiration/dismissInspiration/getInspirationSecsLeft/INSPIRATION_TYPES; subscribed RESEARCH_INSPIRATION→render; TICK throttle now also checks state.researchInspiration?.pending; _inspirationCard() renders pending-event card (icon/title/timer/desc/effectDesc/Accept+Dismiss buttons) or workshop-discount active banner; injected in panel.innerHTML between queue section and tech grid; panel.onclick handles insp-accept and insp-dismiss data-actions. main.js: imported initInspiration+inspirationTick; registerSystem(inspirationTick); initInspiration() in boot+_newGame; researchInspiration in _save()/_applySave(). styles/main.css: .inspiration-card (purple left-border accent, pop-in), .inspiration-header/.icon/.title/.timer (--urgent blink), .inspiration-desc, .inspiration-actions, .btn--inspiration-accept (purple gradient), .btn--inspiration-dismiss (muted border), .inspiration-discount-banner (purple tint)."
  },
  {
    "id": "T117",
    "title": "empire crisis response system — timed emergencies requiring resource payment",
    "priority": "high",
    "status": "done",
    "notes": "Created src/data/crises.js: 4 CRISIS_TYPES (plague/drought/rebellion/market_crash) each with resolveCost, penalty {resource,rateMult,durationTicks}, resolve/fail messages, prestigeReward. Created src/systems/crises.js: initCrises() idempotent; crisisTick() spawns crisis every 8-12min, expires unresolved crisis and pushes rate modifier to state.randomEvents.activeModifiers (plague→food×0.4 10min, drought→food×0.3 12min, rebellion→gold×0.4 8min, market_crash→gold×0.5 6min); canResolveCrisis()/resolveCrisis() pay cost and award prestige. core/events.js: Added CRISIS_SPAWNED/CRISIS_RESOLVED. core/state.js: Added crises:null + reset in initState(). index.html: Added crisis-banner div between title-bar and HUD. main.js: Imported initCrises/crisisTick/getActiveCrisis/resolveCrisis; registerSystem(crisisTick); initCrises() at boot+newGame; _updateCrisisBanner() DOM function with flex layout, countdown timer, resolve button; CRISIS_SPAWNED/CRISIS_RESOLVED events trigger banner refresh; 4-tick TICK throttle for countdown; delegated click handler on crisis-banner div; crises/heroLegacy saved+loaded; save version bumped 34→35. styles/main.css: .crisis-banner (red border-left, pulse animation), .crisis-banner__icon/body/title/desc/actions/timer, .btn--crisis-resolve (red gradient), mobile flex-col. Added .log-entry--danger (red border, subtle bg, bold)."
  },
  {
    "id": "T118",
    "title": "hero enshrinement system — retire max-skill champions for permanent legacy bonuses",
    "priority": "high",
    "status": "done",
    "notes": "systems/heroSystem.js: Added import recalcRates; LEGACY_RATES_BY_SKILL mapping (10 skills→rate bonuses e.g. battle_hardened→gold+0.4, war_drums→gold+0.6, logistics→food+0.3+wood+0.2, etc.); ENSHRINE_MAX=2, ENSHRINE_PRESTIGE=200 exported constants; canEnshrineHero() checks recruited+3skills+10wins+maxLimit; enshrineHero() validates, computes rates from skills, appends to state.heroLegacy.enshrined, clears state.hero, calls recalcRates(), emits HERO_ENSHRINED+HERO_CHANGED+RESOURCE_CHANGED. systems/resources.js: Added T118 hero legacy bonus loop after landmark section — iterates state.heroLegacy.enshrined and adds entry.rates to rates[]. core/events.js: Added HERO_ENSHRINED event. core/state.js: Added heroLegacy:null field + reset in initState(). ui/militaryPanel.js: Imported canEnshrineHero/enshrineHero/ENSHRINE_MAX from heroSystem; added HERO_ENSHRINED→_render subscription; added _heroEnshrineSection() (amber border panel, shows progress toward enshrinement, enshrine button when ready); added skillsNeeds() helper; added _heroLegacyPanel() (gold bordered panel showing enshrined champion cards with rate badges); updated _heroSection() to show legacy count in subtitle + _heroLegacyPanel() below recruit card; updated hero active section to include _heroEnshrineSection(); added enshrine-hero data-action click handler calling enshrineHero(). main.js: Imported ENSHRINE_PRESTIGE; HERO_ENSHRINED listener awards 200 prestige; heroLegacy saved+loaded. styles/main.css: .enshrine-section (amber left-border), .enshrine-section__header/desc, .enshrine-status/--ready/--maxed, .btn--enshrine (amber gradient), .hero-legacy-panel, .legacy-hero-card, .legacy-rate pill badges."
  },
  {
    "id": "T119",
    "title": "hero commander traits — personality modifiers chosen at recruitment",
    "description": "When recruiting the Champion hero, a trait chooser offers 3 random traits from a pool of 8. The chosen trait gives permanent bonuses/penalties active for the hero's lifetime. Traits: Iron Fist (+30 atk, -10% rates), War Scholar (-20% research, +15% combat), Merchant Heart (+0.8 gold/s), Tactician (formation bonuses doubled), Naturalist (+25% food+wood rates), Rally Master (rally free + +10 morale/win), Arcane Mind (+0.5 mana/s, -30% spell costs), Iron Will (+10 morale/win, -50% morale loss on defeat). state.hero.trait. HERO_TRAIT_CHOSEN event. No save version bump.",
    "priority": 119,
    "status": "done",
    "phase": 43,
    "notes": "data/hero.js: Added HERO_TRAITS array (8 entries: iron_fist/war_scholar/merchant_heart/tactician/naturalist/rally_master/arcane_mind/iron_will) and HERO_TRAIT_ORDER constant export. core/events.js: Added HERO_TRAIT_CHOSEN event. core/actions.js: Added _pickTraitOffer() private helper (shuffles, picks 3 random trait IDs); recruitHero() adds trait/pendingTrait/traitOffer to hero state; added chooseHeroTrait(traitId) export (validates, sets hero.trait, clears pendingTrait/traitOffer, calls recalcRates(), emits HERO_TRAIT_CHOSEN+HERO_CHANGED+RESOURCE_CHANGED); rallyTroops() checks rally_master trait for free rally (+10 morale vs +5). systems/resources.js: Added trait rate effects block inside hero block (iron_fist: all positive rates ×0.90, merchant_heart: +0.8 gold/s, naturalist: food+wood ×1.25, arcane_mind: +0.5 mana/s). systems/combat.js: _formationAttackMult() doubles delta for tactician; getAttackPreview()+attackTile() add iron_fist (+30 atk) and war_scholar (×1.15); _victory() adds +10 morale bonus for rally_master/iron_will; _defeat() halves morale loss for iron_will. systems/research.js: war_scholar cuts research time by 20%. systems/spells.js: arcane_mind reduces spell mana cost by 30% (stacks multiplicatively). main.js: Added T119 migration guards in _applySave() for hero.trait/pendingTrait/traitOffer. ui/militaryPanel.js: Fixed duplicate import (merged chooseHeroTrait into existing actions.js import); added HERO_TRAITS import from hero.js; on(HERO_TRAIT_CHOSEN) subscription; _traitChooserSection() renders 3 trait cards with Choose button; _traitBadge() shows active trait pill; _heroSection() shows chooser when pendingTrait=true; trait badge added to active/expedition/injured hero cards; choose-trait click handler calls chooseHeroTrait(). styles/main.css: .trait-chooser, .trait-option, .trait-option__icon/body/name/desc, .btn--trait-choose, .hero-trait-badge styles added with mobile breakpoints."
  },
  {
    "id": "T120",
    "title": "resource cap upgrades — treasury expansions via gold investment",
    "description": "Players can pay gold to permanently expand individual resource caps by +250 per upgrade. Cost: 150×(level+1) gold, max 5 upgrades per resource. Upgrade button per resource in a new Treasury section in the Building panel. state.capUpgrades. CAP_UPGRADED event. Save version 36.",
    "priority": 120,
    "status": "done",
    "phase": 43,
    "notes": "core/state.js: Added capUpgrades:{} to static state object and initState() reset. core/events.js: Added CAP_UPGRADED event. core/actions.js: Added CAP_UPGRADE_MAX=5, CAP_UPGRADE_BONUS=250, CAP_UPGRADE_BASE=150 constants; upgradeResourceCap(resId) validates resource, level<max, gold cost = BASE*(level+1), deducts gold, increments capUpgrades[resId], calls recalcRates(), emits CAP_UPGRADED+RESOURCE_CHANGED. systems/resources.js: Added T120 cap upgrade block before Object.assign(state.caps,caps) — iterates state.capUpgrades entries, adds level*250 to respective resource cap. ui/buildingPanel.js: Imported upgradeResourceCap/CAP_UPGRADE_MAX/CAP_UPGRADE_BONUS/CAP_UPGRADE_BASE; added on(CAP_UPGRADED) listener; _treasurySection() renders 6 resource rows (icon/name/current-cap/level/upgrade-button); injected after _capitalPlansSection(); panel.onclick handles upgrade-cap action. main.js: version bumped 35→36; capUpgrades added to _save() payload and _applySave() restore. styles/main.css: .treasury-section, .treasury-section__header/intro, .treasury-grid, .treasury-row, .treasury-row__icon/name/cap/level/maxed, .btn--treasury-upgrade styles added."
  },
  {
    "id": "T121",
    "title": "city founding — convert owned tiles into cities for permanent income bonuses",
    "description": "Players can spend 200 gold + 100 stone + 50 iron to found a city on any player-owned tile (non-capital, Bronze Age required). Each city grants +1.5 gold/s and +0.5 food/s. Max 5 cities. Cities can be razed by enemy counterattacks. UI: 'Found City' button in the tile improvement picker. tile.hasCity flag. CITY_FOUNDED event. Save version 37.",
    "priority": 121,
    "status": "done",
    "phase": 44,
    "notes": "core/events.js: Added CITY_FOUNDED event. core/actions.js: Imported COMPANIONS from hero.js; added CITY_COST={gold:200,stone:100,iron:50}, CITY_MAX=5 constants; foundCity(x,y) validates Bronze Age + player ownership + not capital + not already city + city count<5 + canAfford; deducts cost, sets tile.hasCity=true, calls recalcRates(), emits CITY_FOUNDED+MAP_CHANGED+RESOURCE_CHANGED. systems/resources.js: Added T121 city bonus block before T120 cap section — counts tiles where hasCity, adds cityCount*1.5 gold/s and cityCount*0.5 food/s. systems/enemyAI.js: In _counterattack() on enemy capture, if tile.hasCity, clears it and logs raze message. ui/mapPanel.js: Imported foundCity from actions; added on(CITY_FOUNDED) re-render; _drawTile() adds 'else if hasCity' branch before improvement icon check, draws 🏙️ icon; _showTileTip() adds cityHtml (shows city bonus if tile.hasCity); _citySection(x,y,tile) helper renders city status or build button with cost/age/max checks; _showImprovementPicker() includes _citySection in all 3 branches; _createImprovementPicker() click handler handles data-found-city. main.js: save version bumped 36→37. styles/main.css: .imp-city-section, .imp-city-header, .imp-city-desc, .imp-city-built, .btn--city-found styles added."
  },
  {
    "id": "T122",
    "title": "hero companion — passive combat ally unlocked after 15 victories",
    "description": "After 15 combat victories with the hero, a companion offer appears. Player chooses one of 3: Scout (reveals extra tiles after each win), Healer (15% chance to prevent unit casualty on defeat), Warlock (+12 mana per win). One companion per game, persists for the hero's lifetime. COMPANION_RECRUITED event. Migration guards in _applySave(). Save version 37.",
    "priority": 122,
    "status": "done",
    "phase": 44,
    "notes": "data/hero.js: Added COMPANION_UNLOCK_WINS=15; COMPANIONS object (scout/healer/warlock each with id/icon/name/desc); COMPANION_ORDER array. core/events.js: Added COMPANION_RECRUITED event. core/actions.js: Imported COMPANIONS; added chooseCompanion(type) — validates hero recruited + companionOffer + no existing companion + valid type; sets hero.companion={type}, clears hero.companionOffer, emits COMPANION_RECRUITED+HERO_CHANGED, adds message. systems/combat.js: Imported COMPANIONS+COMPANION_UNLOCK_WINS; in _trackHeroCombatWin() after legendaryQuest handling, checks combatWins>=15 + no companion + no offer: sets companionOffer=true + message; in _victory() after ruin excavation: warlock companion adds +12 mana (capped), scout companion reveals Manhattan-≤2 radius tiles; in _defeat(): checks healer companion 15% chance before _loseOneUnit(), adds hero save message. ui/militaryPanel.js: Imported COMPANIONS+COMPANION_ORDER from hero.js; imported chooseCompanion from actions; subscribed COMPANION_RECRUITED→_render; added _companionSection() — shows locked hint (wins<15), offer chooser (3 pick buttons), or active companion card; integrated into _render() after _heroSection(); added companion-pick data-action handler → chooseCompanion(). main.js: T122 migration guards in _applySave() for hero.companion=null and hero.companionOffer=false when hero is recruited. styles/main.css: .companion-section (blue left-border), .companion-section--locked/--offer variants, .companion-header, .companion-card, .companion-icon/name/desc, .companion-locked-hint, .companion-offer-desc, .companion-picks, .btn--companion-pick, .companion-pick__icon/name/desc styles added."
  },
  {
    "id": "T123",
    "title": "notification center — persistent event history drawer in the title bar",
    "description": "Add a 🔔 bell button to the title bar. Clicking it toggles a dropdown drawer listing the last 25 important game events (quests, achievements, age advances, crises, raids, disasters, windfalls, hero events, sieges, duels, relics, landmarks, ruins, title earns). Unread events show a badge count on the bell. Clear button dismisses all. Events are tracked module-level (no persistence). GAME_STARTED resets the log.",
    "priority": 123,
    "status": "done",
    "phase": 45,
    "notes": "Created src/ui/notificationCenter.js: MAX_NOTIFICATIONS=25; TRACKED_TYPES Set (25 event types); TYPE_ICON map; module-level _notifications/_unreadCount/_isOpen/_bellEl/_badgeEl/_drawerEl; initNotificationCenter() (idempotent): _buildDOM() injects #notif-bell button before #score-badge in title bar + appends #notif-drawer to body; _onMessage() filters MESSAGE events by type, prepends entry, increments unread badge or refreshes list; _open()/_close()/_toggle(); _positionDrawer() calculates drawer position below bell via getBoundingClientRect(); _renderList() builds .notif-item--{type} rows with coloured left border per type, icon, text, relative time; _updateBadge() shows/hides .notif-badge count; _reset() clears on GAME_STARTED or Clear All click; _relTime() converts tick delta to 'just now/Xs/Xm/Xh ago'; click-outside handler on document closes drawer. main.js: imported initNotificationCenter; called in boot() after initMinimap(). styles/main.css: .notif-bell (border-radius pill, hover/open highlight), .notif-badge (red pill, --hidden display:none), .notif-drawer (fixed 300px dropdown, max-height 380px, flex column, z-1500), .notif-drawer--hidden (display:none), .notif-drawer__header (flex justify-between border-bottom), .notif-clear, .notif-list (scrollable), .notif-empty, .notif-item (flex, 3px left border per type, --quest gold/--achievement brown/--age purple/--raid+duel red/--windfall+relic+landmark+ruin green/--disaster+crisis+siege+barbarian orange/--hero+companion blue/--title+prestige yellow), .notif-icon/.notif-body/.notif-text/.notif-time; hidden on ≤480px."
  },
  {
    "id": "T124",
    "title": "empire legacy traits — cross-game meta-progression via localStorage points",
    "description": "After each game ends (win or lose), award floor(score/100) legacy points saved to 'empireos-legacy' localStorage key. Players spend points between games to buy 8 permanent starting-bonus traits: Ancient Bloodline (+150g/+80f/+60w), Military Tradition (3 free soldiers), Trade Heritage (Market built), Scholar's Legacy (agriculture tech researched), Iron Constitution (+10 morale), Builder's Instinct (free Lumber Mill), Explorer's Heritage (+100 stone/+80 iron), Arcane Lineage (+80 mana). Once bought, traits apply permanently at every new game. Legacy section in Settings panel. No save version bump (all in localStorage).",
    "priority": 124,
    "status": "done",
    "phase": 45,
    "notes": "Created src/data/legacyTraits.js: LEGACY_KEY='empireos-legacy'; LEGACY_TRAITS object (8 entries: ancient_bloodline/military_tradition/trade_heritage/scholars_legacy/iron_constitution/builders_instinct/explorers_heritage/arcane_lineage) each with id/icon/name/desc/cost/apply(state) function; LEGACY_TRAIT_ORDER array; loadLegacy() reads+parses localStorage with fallback {points:0,owned:[]}; saveLegacy() writes back; awardLegacyPoints(score) computes floor(score/100), loads+adds+saves; buyLegacyTrait(traitId) validates (unknown/already-owned/insufficient), deducts points+pushes id+saves, returns {ok,reason}. main.js: imported initNotificationCenter+loadLegacy+awardLegacyPoints+LEGACY_TRAITS from their respective modules; added _applyLegacyBonuses() — loads legacy, iterates owned array, calls def.apply(state) for each; called in boot() new-game path after _applyDifficultyStart() and in _newGame() at same position; added awardLegacyPoints(calcScore()) call at end of _saveToLeaderboard() (which fires before every new game). ui/settingsPanel.js: imported loadLegacy/buyLegacyTrait/LEGACY_TRAITS/LEGACY_TRAIT_ORDER; added ${_legacySection()} injection in panel.innerHTML between leaderboard and shortcuts sections; added buy-button click delegates in bind block (btn--shake on failure); _legacySection() renders: legacy-points-bar with points balance, legacy-grid with 8 .legacy-card entries (--owned gold border+tint+'✓ Owned' badge; --locked with cost label colour-coded by affordability and disabled Buy Trait button when insufficient pts). No save version bump — all state is in the separate empireos-legacy localStorage key."
  },
  {
    "id": "T125",
    "title": "forge system — craft 6 unique items from resources for permanent passive bonuses",
    "description": "Players can forge one of each of 6 unique items at the Iron Foundry (requires metalworking tech). Each item grants a permanent passive bonus for the rest of the game. Items: Iron Helm (100 iron+80 gold, +25 flat combat attack), Ring of Prosperity (150 gold+50 mana, requires tradeRoutes, +0.8 gold/s), Arcane Tome (100 mana+80 gold, requires arcane, -20% research time), War Drums (80 iron+80 wood+60 gold, requires warcraft, -20% training time), Farmer's Almanac (150 food+60 wood+50 gold, requires agriculture, +1.5 food/s), Ironwood Shield (100 iron+80 stone, requires engineering, -20% enemy counterattack success). Forge section in Building panel. FORGE_CHANGED event. state.forge saved/loaded. No save version bump.",
    "priority": 125,
    "status": "done",
    "phase": 46,
    "notes": "Created src/data/forgeItems.js: FORGE_ITEMS (6 items: iron_helm/ring_of_prosperity/arcane_tome/war_drums/farmers_almanac/ironwood_shield each with id/icon/name/desc/cost/requires.tech/bonusLabel) + FORGE_ORDER array. core/events.js: Added FORGE_CHANGED event. core/state.js: Added state.forge=null; initState() resets state.forge=null. core/actions.js: Imported FORGE_ITEMS; in trainUnit() added War Drums -20% training time mod; added forgeItem(itemId) — validates ironFoundry>=1 + metalworking tech + item tech req + not already crafted + canAfford; deducts cost, sets state.forge.crafted[itemId]=tick, calls recalcRates()+emit(FORGE_CHANGED)+emit(RESOURCE_CHANGED)+addMessage. systems/resources.js: Added forge rate bonuses block — ring_of_prosperity adds +0.8 gold/s, farmers_almanac adds +1.5 food/s. systems/combat.js: Added iron_helm +25 flat attackPower in both getAttackPreview() and attackTile(). systems/research.js: Added arcane_tome -20% research time in startResearch() after war_scholar trait. systems/enemyAI.js: Added ironwood_shield -20% enemy counterattack winChance. ui/buildingPanel.js: Imported forgeItem from actions + FORGE_ITEMS/FORGE_ORDER; added on(FORGE_CHANGED) subscription; html += _forgeSection() in render; forge-item action handler; added _forgeSection() rendering locked/available/crafted states per item. main.js: state.forge saved/loaded; initAuction import (for T126 below). styles/main.css: .forge-section, .forge-section__header/intro, .forge-grid, .forge-item/--locked/--crafted, .forge-item__header/icon/name/bonus/crafted-badge/desc/cost/lock, .forge-cost--ok/bad, .btn--forge styles added."
  },
  {
    "id": "T126",
    "title": "resource auction house — periodic commodity bundles players bid on for gold",
    "description": "Every 8-10 minutes, an auction house event offers 2 resource bundles the player can bid on. Each bundle has 2-3 resources with a fixed gold bid price (100/200/300 gold). Auction window is 60 seconds. After the window, highest 'bid' wins the bundle (player just pays the fixed price). One bundle per auction. Auction section in Market panel with countdown timer, bundle preview, and Bid/Pass button. AUCTION_CHANGED event. state.auction saved/loaded. No save version bump.",
    "priority": 126,
    "status": "done",
    "phase": 46,
    "notes": "Created src/systems/auction.js: AUCTION_MIN/MAX=3-5min, DURATION=90s; BUNDLE_TEMPLATES (8 2-3 resource bundles); _nextAuctionTick()/_bundleValue()/_spawnAuction() helpers; initAuction() initializes state.auction={current:null,nextAuctionTick,won:0} if not already set; auctionTick() — checks expiry (awards bundle or expires silently, clears current), spawns new auction when timer fires and market exists; bidOnAuction(amount) — validates active auction + gold + not already met; deducts gold, adds to playerBid (capped at bidGoal), emits AUCTION_CHANGED+RESOURCE_CHANGED; passAuction() — refunds partial bid, clears current, emits AUCTION_CHANGED. core/events.js: Added AUCTION_CHANGED event. core/state.js: state.auction=null; initState() resets. main.js: imported initAuction+auctionTick; registerSystem(auctionTick); initAuction(); state.auction saved/loaded. ui/marketPanel.js: imported bidOnAuction+passAuction from auction.js; on(AUCTION_CHANGED) subscription; TICK handler extended to refresh on auction.current; _auctionSection() renders idle (countdown+total won) or active (bundle, bid goal, progress bar, Bid 10/50+Pass buttons, met state); html injected after _contractsSection(); click handlers for auction-bid and auction-pass. styles/main.css: .auction-section/--active, .auction-header, .auction-timer, .auction-idle, .auction-bundle, .auction-bid-info, .auction-progress-bar/fill, .auction-won-msg, .auction-actions, .btn--auction, .btn--auction-pass styles added."
  },
  {
    "id": "T127",
    "title": "resource raid action — steal resources from enemy tiles without capturing",
    "description": "New tactical action in the combat preview modal: '⚡ Raid for Resources'. Raids an adjacent enemy tile to steal resources without capturing territory. Costs 30 food. Enemy defense is halved (40% of normal). Win chance clamped [40%, 85%]. Win: steal 60% of tile loot + bonus gold (scales with tick). Loss: lose 1 unit. Global 5-minute cooldown. RAID_CHANGED event. state.raids saved/loaded.",
    "priority": 127,
    "status": "done",
    "phase": 47,
    "notes": "RAID_CHANGED event added to events.js. state.raids = {cooldownUntil, totalRaids} added to state.js + reset in initState(). systems/combat.js: imported TICKS_PER_SECOND; added RAID_FOOD_COST=30, RAID_COOLDOWN_TICKS=1200 (5min); added _initRaids() helper; added getRaidPreview(x,y) — validates adjacency+enemy+units, computes attackPower (same unit loop as getAttackPreview), enemyDef=tile.defense*0.4, winChance=clamp(0.40,0.85), loot=60% of tile.loot + bonus gold; added raidTile(x,y) — validates preview, checks cooldown+food, deducts food, rolls, win: grants resources+increments totalRaids+logs windfall, loss: _loseOneUnit()+logs combat-loss; sets cooldownUntil either way. ui/mapPanel.js: imported raidTile+getRaidPreview from combat.js; _showCombatPreview() builds raidHtml section (win%, cost, loot preview) and appends Raid button to .cp-actions; _createCombatPreview() click handler handles cp-raid-btn. main.js: raids saved/loaded (backwards-compatible null default). styles/main.css: .cp-raid-section (purple tinted card), .cp-raid-header/.info/.desc, .btn--cp-raid (purple gradient)."
  },
  {
    "id": "T128",
    "title": "seasonal building bonuses — per-building production multipliers tied to the active season",
    "description": "Each season activates passive production bonuses for specific building types: Spring (Farms +50%, Mana Wells +20%), Summer (Lumber Mills +30%, Gold Mines +20%), Autumn (Quarries +30%, Markets +25%), Winter (Iron Foundries +25%, Watchtowers +50%). Applied inside _buildingProdMultiplier() in resources.js so they stack correctly with tech/age multipliers and are reflected in HUD tooltips. Season badge tooltip updated to list which buildings are boosted.",
    "priority": 128,
    "status": "done",
    "phase": 47,
    "notes": "data/seasons.js: Added SEASON_BUILDING_BONUSES (array of 4 season objects keyed by buildingId→mult) and SEASON_BUILDING_LABELS (human-readable label per season listing the boosted buildings). systems/resources.js: Added SEASON_BUILDING_BONUSES to import from data/seasons.js; added T128 seasonal building bonus block at end of _buildingProdMultiplier() — reads SEASON_BUILDING_BONUSES[state.season?.index] and applies building-specific mult when found. getBreakdown() calls _buildingProdMultiplier() so HUD tooltips automatically show correct boosted values. main.js: Added SEASON_BUILDING_LABELS to import from data/seasons.js; updated _updateSeasonBadge() to append '🏗️ Building bonus: <labels>' line to the season badge tooltip. No save version bump — all computed dynamically from season state."
  },
  {
    "id": "T129",
    "title": "strategic map chokepoints",
    "description": "During map generation, 3 neutral river/mountain tiles at distance 3–9 from capital are designated as strategic chokepoints (tile.isChokepoint=true). When fortified by the player, chokepoints grant +40 defense (instead of +15) and enemy AI counterattacks get an extra −35% win chance. Canvas: amber ◈ glyph in top-left corner on revealed chokepoint tiles. Tile tooltip shows '◈ Strategic Chokepoint' with bonus info and updated fortify hint. tile.fortifyBonus persists the actual defense delta so enemy captures and natural disasters correctly restore the original value. No save version bump.",
    "priority": 129,
    "status": "done",
    "phase": 48,
    "notes": "systems/map.js: Added CHOKEPOINT_COUNT=3, CHOKEPOINT_TERRAINS (river/mountain), _placeChokepoints(tiles) — finds neutral river/mountain tiles at dist 3–9, shuffles, marks first 3 as tile.isChokepoint=true; called from initMap() after _placeRuins. core/actions.js: fortifyTile() now computes defBonus = tile.isChokepoint ? 40 : 15, stores tile.fortifyBonus=defBonus, applies it to tile.defense; message differs for chokepoints ('◈ Chokepoint fortified!'). systems/enemyAI.js: Added 'if (tile.isChokepoint && tile.fortified) winChance *= 0.65' after the existing tile.fortified check; fixed fortification removal to use tile.fortifyBonus ?? 15 (instead of hardcoded 15) and clears tile.fortifyBonus. systems/naturalDisasters.js: Same fortifyBonus fix for earthquake-type fortification removal. ui/mapPanel.js: _drawTile() draws amber ◈ glyph in top-left corner (blue when player-owned, amber otherwise) on all revealed chokepoint tiles; _showTileTip() builds chokepointHtml (color:#ffc070 label + dim desc line, two states: fortified/unfortified) and injects it after capitalHtml; fortify hint updated to show '+40 def — chokepoint!' vs normal '+15 def'."
  },
  {
    "id": "T130",
    "title": "seasonal unit recruitment discounts",
    "description": "Each season, one unit type trains at 20% reduced cost: Spring=soldier, Summer=archer, Autumn=knight, Winter=mage. Applied in trainUnit() using a derived effectiveCost. Season badge tooltip gains a '⚔️ Unit discount: X 20% cheaper' line. Military panel unit cards show a green '20% Off' badge on the discounted unit. No state or save version changes needed — discount computed dynamically from state.season.",
    "priority": 130,
    "status": "done",
    "phase": 48,
    "notes": "data/seasons.js: Added SEASON_UNIT_DISCOUNT export (array of 4 unit IDs: soldier/archer/knight/mage) and SEASON_UNIT_LABELS export (human-readable per-season discount descriptions). core/actions.js: Imported SEASON_UNIT_DISCOUNT from data/seasons.js; trainUnit() now computes discountUnit=SEASON_UNIT_DISCOUNT[state.season?.index??0] and effectiveCost (floors each cost component at 0.80× when matched); canAfford+deductCost use effectiveCost; message appends '(seasonal discount)' when discount applied. ui/militaryPanel.js: Imported SEASON_UNIT_DISCOUNT; _unitCard() computes seasonDiscount boolean (current season's unit === id); if true and not locked, injects .unit-card__discount badge '🟢 20% Off' into card header (before count span). src/main.js: Added SEASON_UNIT_LABELS to seasons import; _updateSeasonBadge() tooltip appended '⚔️ Unit discount: ${unitLabel}' line. styles/main.css: Added .unit-card__discount (green text, faint green bg, rounded border pill badge)."
  },
  {
    "id": "T131",
    "title": "empire proclamations — one strategic declaration per age",
    "description": "Player issues one proclamation per age (clears on age advance) from the Military panel. 4 options with trade-offs: War Economy (+25% attack, -15% all rates, cost 150g), Golden Era (+40% gold, -20% food/wood, cost 100g), Great Works (+30% research speed, -25% mana production, cost 200g+50 mana), Iron Will (-20% enemy counterattack, cost 120g+30 iron + -10 morale upfront). Active proclamation shown in styled panel; new-game proclamation resets. Save/load compatible.",
    "priority": 131,
    "status": "done",
    "phase": 49,
    "notes": "data/proclamations.js: New file with 4 PROCLAMATIONS (war_economy/golden_era/great_works/iron_will). core/events.js: Added PROCLAMATION_ISSUED. core/state.js: Added proclamation field; initState() resets to {activeId:null,ageWhenIssued:-1}. core/actions.js: issueProclamation(id) validates one-per-age, deducts cost, iron_will -10 morale, recalcRates(), emits. systems/resources.js: proclamation rate effects at end of recalcRates(). systems/research.js: great_works 1.3x tick decrement. systems/combat.js: war_economy +25% attack in preview+actual; siege_engine defense halving. systems/enemyAI.js: iron_will x0.80 counterattack. ui/militaryPanel.js: _proclamationsSection() rendered after decrees; click handler; event listener. main.js: save/load; AGE_CHANGED clears proclamation. styles/main.css: proclamation styles (purple theme)."
  },
  {
    "id": "T132",
    "title": "siege engine unit — heavy catapult max 1 halves fortified defense",
    "description": "New military unit Siege Engine (catapult). Stats: attack 40, defense 3. Cost: 400g+150s+100i. 15s train. Upkeep: 0.3 food+0.5 gold/s. Requires ironFoundry+engineering+Iron Age. Cap: max 1. When owned, fortified tile effectiveDefense halved in both combat preview and actual attack. Combat preview shows notice. Military panel shows 1-max badge.",
    "priority": 132,
    "status": "done",
    "phase": 49,
    "notes": "data/units.js: siege_engine definition added. core/actions.js: cap check in trainUnit() — rejects if already owned or in queue. systems/combat.js: effectiveDefense*=0.50 when siege_engine owned and tile.fortified; both getAttackPreview+attackTile; siegeEngineActive flag in preview return. ui/mapPanel.js: siegeEngineActive amber notice in combat preview. ui/militaryPanel.js: siege_engine in UNIT_ORDER; cap badge (yellow/red); trainDisabled includes siegeCapped. styles/main.css: unit-card__cap styles."
  },
  {
    "id": "T133",
    "title": "wonder projects — three massive late-game constructions with permanent empire bonuses",
    "description": "At Medieval Age, the player can commission one of 3 Wonders (one per game). Each costs significant resources and takes 4 minutes to build. Wonders: Grand Colosseum (500g+400s+200i+150w, +10 morale permanent + festival effects +25%), Grand Bazaar (400g+200w+100s+100m, +2.5 gold/s + market trade amounts ×1.5, requires tradeRoutes), Tower of Babel (350g+200m+200s+150i, −20% tech costs + 1.5 mana/s + 300 mana cap, requires arcane). Wonder in-progress shown as a progress bar in the Research panel Wonders section. WONDER_CHANGED event. state.wonder saved/loaded. Save version 39.",
    "priority": 133,
    "status": "done",
    "phase": 50,
    "notes": "data/wonders.js: New file with 3 WONDERS (colosseum/grand_bazaar/tower_of_babel each with id/icon/name/desc/flavorText/cost/buildTicks/requires/bonusLabel) + WONDER_ORDER array. core/events.js: Added WONDER_CHANGED event. core/state.js: Added wonder field ({buildingId,startTick,endsAt,completedId}|null); initState() resets state.wonder=null. systems/wonders.js: New file — initWonders(), wonderTick() checks endsAt, startWonder(id) validates age/tech/cost/uniqueness, deducts cost, sets buildingId+startTick+endsAt, emits WONDER_CHANGED; _completeWonder() applies colosseum morale +10 via changeMorale(), sets completedId, calls recalcRates(), emits; getWonderProgress() returns {pct,secsLeft}; getCompletedWonder()/isWonderBuilding() helpers. systems/resources.js: T133 wonder bonus block at end of recalcRates(): grand_bazaar +2.5 gold/s; tower_of_babel +1.5 mana/s + caps.mana+300. Festival multiplier block enhanced with _colosseumMult (1.25 if colosseum complete) applied to harvest+trade_fair festival rate bonuses. systems/research.js: Tower of Babel -20% research time (totalTicks*0.80) added after arcane_tome block. systems/market.js: sellResources(): earned*1.5 if grand_bazaar complete; buyResources(): finalAmount=min(room, actual*1.5) if grand_bazaar. ui/researchPanel.js: Added WONDERS/WONDER_ORDER import from data; startWonder/getWonderProgress/getCompletedWonder/isWonderBuilding import from systems/wonders; WONDER_CHANGED event listener re-renders panel; tick throttle includes isWonderBuilding(); panel.innerHTML includes _wondersSection(); onclick handler for [data-wonder]; _wondersSection() renders 3 wonder cards with locked/building/completed states + progress bar when building. main.js: initWonders+wonderTick imports; registerSystem(wonderTick); initWonders() in boot+newGame; state.wonder saved/loaded (version 39); WONDER_CHANGED listener added in boot. styles/main.css: .wonder-section/header/intro, .wonder-completed-banner, .wonder-bonus-pill, .wonder-progress-banner/header/bar/fill, .wonder-grid, .wonder-card/--completed/--building headers/icon/name/desc/flavor/bonus/cost/lock, .wonder-badge/--done/--wip, .wonder-cost/--ok/--bad, .btn--wonder (purple gradient)."
  },
  {
    "id": "T134",
    "title": "wandering scholar events — periodic knowledge offers with 60s accept window",
    "description": "Every 5–8 minutes, a wandering scholar visits and offers one of 5 teachings. A green banner (like the crisis banner) appears for 60 seconds. Player clicks Accept or Dismiss. Teachings: Arcane Revelation (+150 mana instantly), Agricultural Wisdom (+100% food+wood for 90s), Military Doctrine (next 3 units trained at 50% cost), Trade Secrets (+50% sell prices for next 3 market sells), Engineering Plans (buildings cost 40% less for 90s). SCHOLAR_CHANGED event. state.scholar saved/loaded. No save version bump.",
    "priority": 134,
    "status": "done",
    "phase": 50,
    "notes": "core/events.js: Added SCHOLAR_CHANGED event. core/state.js: Added scholar field ({active,nextScholarTick,totalAccepted,activeEffect}|null); initState() resets state.scholar=null. systems/scholars.js: New file — TEACHINGS array (5 entries: arcane_revelation/agricultural_wisdom/military_doctrine/trade_secrets/engineering_plans); initScholars() idempotent; scholarTick() expires timed activeEffect + expired active visit + spawns new scholar when timer fires; acceptTeaching() applies teaching via _applyTeaching(), resets nextScholarTick; dismissScholar() clears active, resets timer; _applyTeaching() handles all 5 cases: arcane_revelation adds mana, agricultural_wisdom sets activeEffect{type,expiresAt}, military_doctrine sets activeEffect{type,chargesLeft:3}, trade_secrets sets activeEffect{type,chargesLeft:3}, engineering_plans sets activeEffect{type,expiresAt}. systems/resources.js: Scholar agricultural_wisdom block applies rates.food*2 + rates.wood*2 when activeEffect.type==='agricultural_wisdom' && tick<expiresAt. systems/market.js: sellResources() checks activeEffect.type==='trade_secrets' && chargesLeft>0, multiplies earned*1.5, decrements chargesLeft. core/actions.js: trainUnit() checks activeEffect.type==='military_doctrine' && chargesLeft>0, applies 50% cost reduction, decrements chargesLeft; scaledCost() checks activeEffect.type==='engineering_plans', applies 0.60 multiplier when active. index.html: Added scholar-banner div after crisis-banner. main.js: initScholars+scholarTick+acceptTeaching+dismissScholar imports; registerSystem(scholarTick); initScholars() in boot+newGame; state.scholar saved/loaded (same version 39); SCHOLAR_CHANGED listener triggers _updateScholarBanner(); tick throttle updates banner; _updateScholarBanner() function renders green banner with icon/title/desc/timer/Accept+Dismiss buttons; scholar-banner click handler delegates accept/dismiss to system functions. styles/main.css: .scholar-banner (green left-border, scholar-glow animation), .scholar-banner__icon/body/title/desc/actions/timer, .btn--scholar-accept (green gradient), .btn--scholar-dismiss (outlined)."
  },
  {
    "id": "T135",
    "title": "territory bounty system — targeted tile rewards",
    "description": "Every 8–12 minutes (Bronze Age+) a bounty is posted on a specific enemy or barbarian tile adjacent to player territory. Reward: 200–400 gold + 50 food + 60 prestige (scales with tick). Bounty expires after 3 minutes if unclaimed. Claimed automatically when the player captures the bounty tile via normal combat. Map: gold ⭐ star marker on the bounty tile. Quest panel: bounty card at top with target coordinates, countdown, reward preview, and 'Navigate to target' info. BOUNTY_CHANGED event. state.bounty saved/loaded. No save version bump.",
    "priority": 135,
    "status": "done",
    "phase": 51,
    "notes": "systems/bounty.js: New file — initBounty() (idempotent), bountyTick() (spawns/expires), getActiveBounty(), getBountySecsLeft(), claimBounty(x,y). Constants: SPAWN_MIN=1920 ticks (8min), SPAWN_MAX=2880 (12min), EXPIRE_TICKS=720 (3min), requires Bronze Age (age>=1). _spawnBounty() picks enemy/barbarian tiles adjacent to player, reward scales with tick. core/events.js: BOUNTY_CHANGED event. core/state.js: state.bounty=null field + initState reset. systems/combat.js: claimBounty(x,y) called after _tryExcavateRuin in _victory(). ui/questPanel.js: _bountySection() shows card with coords/countdown/terrain/reward; urgent blink at ≤45s; countdown refresh in TICK handler. ui/mapPanel.js: gold tint + border + ⭐ glyph on bounty tile; BOUNTY_CHANGED subscription; bountyHtml in _showTileTip(). main.js: import + registerSystem(bountyTick) + initBounty() in boot+newGame; saved/loaded (version bumped to 40 in T136 session). styles/main.css: .bounty-section/.bounty-header/.bounty-waiting/.bounty-card variants + urgent blink animation."
  },
  {
    "id": "T136",
    "title": "great person system — accumulated milestone rewards",
    "description": "Earn 1 Great Person point every 10 minutes. At 3 points a random Great Person appears (Engineer / Merchant / Scholar / General). Player uses them from a new card in the Empire Summary tab. Engineer: instantly construct the cheapest affordable building (no cost). Merchant: +600 gold instantly. Scholar: instantly complete current research (or -50% time on next). General: +50 attack power for next 5 battles. Max 1 available at a time; unused persons expire after 5 minutes. GREAT_PERSON event. state.greatPersons saved/loaded. No save version bump.",
    "priority": 136,
    "status": "done",
    "phase": 51,
    "notes": "systems/greatPersons.js: New file — GREAT_PERSON_TYPES array (engineer/merchant/scholar/general), initGreatPersons() (idempotent + migration guards), greatPersonTick() (expire + point accumulation + spawn), getAvailableGreatPerson(), getGreatPersonSecsLeft(), getNextPointSecs(), useGreatPerson(), getGeneralBonus(), consumeGeneralCharge(). Constants: POINT_INTERVAL=2400 ticks (10min), POINTS_TO_SPAWN=3, EXPIRE_TICKS=1200 (5min), GENERAL_BONUS_ATK=50, GENERAL_CHARGES=5. _applyEffect(): engineer finds cheapest non-wonder affordable building and grants it free; merchant +600 gold (capped); scholar sets entry.remaining=0 or scholarDiscount flag; general sets generalCharges=5. core/events.js: GREAT_PERSON event. core/state.js: state.greatPersons=null + initState reset. systems/combat.js: getGeneralBonus()+consumeGeneralCharge() imports; General bonus added in both getAttackPreview() and attackTile() after T131 proclamation; consumeGeneralCharge() called after consumeAidBattle(). systems/research.js: Scholar discount block in startResearch() after T133 — halves totalTicks, clears flag, logs message. ui/summaryPanel.js: _greatPersonCard() shows progress pips, next-point countdown, general charges row, available person card (with urgent timer <60s), Summon button; click handler for [data-use-gp]; GREAT_PERSON event subscription. main.js: import + registerSystem(greatPersonTick) + initGreatPersons() in boot+newGame; saved/loaded (version 40). styles/main.css: .great-person-card variants, .gp-pip/--filled, .gp-points-pips, .btn--use-gp."
  },
  {
    "id": "T137",
    "title": "building auto-queue",
    "description": "Players can queue up to 3 non-wonder buildings for auto-construction. When resources become sufficient for the first item in the queue, it auto-builds and the next item becomes active. Queue section at top of Building panel with Remove buttons. Each building card shows a Queue button alongside Build. Wired via on(RESOURCE_CHANGED) in main.js. QUEUE_CHANGED event. state.buildQueue saved/loaded. No save version bump.",
    "priority": 137,
    "status": "done",
    "phase": 52,
    "notes": "core/state.js: Added buildQueue:[] to static state and state.buildQueue=[] in initState(). core/events.js: Added QUEUE_CHANGED:'queueChanged' to Events freeze. core/actions.js: BUILD_QUEUE_MAX=3 const; addToBuildQueue(buildingId) — validates non-wonder/non-unique, checks queue capacity and duplicates, pushes, emits QUEUE_CHANGED; removeFromBuildQueue(idx) — splices by index, emits QUEUE_CHANGED. ui/buildingPanel.js: imports addToBuildQueue/removeFromBuildQueue/BUILD_QUEUE_MAX; on(Events.QUEUE_CHANGED, renderBuildingPanel) added to initBuildingPanel(); renderBuildingPanel() opens with _queueSection() prepended; each non-wonder building card gets queue/cost vars + queueBtnHtml ('+ Queue' when not queued, '⏳ Queued' disabled when in queue, disabled when full); onclick delegates action=queue (calls addToBuildQueue + shake animation on fail) and action=queue-remove (calls removeFromBuildQueue by idx); _queueSection() renders .build-queue-section header with count badge, .bq-items list showing position/icon/name/scaled-cost/remove button, or .bq-empty hint when empty. main.js: _processAutoQueue() function attempts buildBuilding(queue[0]), on success splices queue[0] and emits QUEUE_CHANGED (cascade via RESOURCE_CHANGED); on(Events.RESOURCE_CHANGED, _processAutoQueue) in boot(); state.buildQueue saved and loaded. styles/main.css: .build-queue-section (amber left border, subtle bg), .bq-header/.bq-title/.bq-count (pill badge), .bq-items/.bq-item (flex rows), .bq-pos/.bq-icon/.bq-name/.bq-cost/.bq-remove, .bq-empty, .btn--queue (amber outline), .btn--queue-in (dimmed), @keyframes btn-shake."
  },
  {
    "id": "T138",
    "title": "combat odds overlay on map",
    "description": "Toggle button (🎯 Odds) in the map controls bar. When active, pre-compute win probability for all attackable tiles each render using getAttackPreview(). Draw a green/amber/red semi-transparent overlay and white win% text on each attackable tile. Green ≥70%, amber 40–69%, red <40%. Overlay clears when toggled off. No new state or events needed.",
    "priority": 138,
    "status": "done",
    "phase": 52,
    "notes": "ui/mapPanel.js: Added _oddsMode=false and _oddsCache={} module-level vars. _buildHTML() now includes a second button '#map-odds-toggle' (.btn--map-odds) next to the resource overlay toggle in .map-controls. initMapPanel() wires oddsBtn click: toggles _oddsMode, toggles .btn--map-odds-active class, calls _render(). _render(): when _oddsMode active, iterates all tiles and calls getAttackPreview(ox,oy) for each _isAttackable tile, populates _oddsCache['x,y']=winChance; clears cache when mode off. _drawTile(): after bounty section, checks _oddsCache[x+','+y]; if present, draws rgba tint (blue>=70%, amber 40-69%, red<40%) at 0.40 opacity + bold white 7px win% text centered on tile. styles/main.css: .btn--map-odds (same pattern as overlay toggle), .btn--map-odds-active (red accent for combat theme)."
  },
  {
    "id": "T139",
    "title": "barbarian bribe — pay gold to cancel incoming grand siege",
    "description": "When a Grand Siege warning is active, show a Bribe button (💰 200g) inside the siege badge. Clicking it costs 200 gold, cancels the siege warning, and resets the siege cooldown. New export bribeBarbarians() in barbarianCamps.js. Siege badge switches to innerHTML to embed the button; delegated click handler in _bindControls() in main.js. CSS for .btn--siege-bribe (amber styling).",
    "priority": 139,
    "status": "done",
    "phase": 53,
    "notes": "systems/barbarianCamps.js: Added BRIBE_COST=200 const and bribeBarbarians() — validates siegeWarning active and gold >= BRIBE_COST, deducts gold, sets siegeWarning=null and nextSiegeTick=tick+SIEGE_INTERVAL, emits RESOURCE_CHANGED+BARBARIAN_SIEGE({type:bribed}). main.js: Updated barbarianCamps import to include bribeBarbarians/BRIBE_COST. _updateSiegeBadge() now uses innerHTML to embed a .btn--siege-bribe button showing cost; disables when player cant afford. _bindControls() adds delegated click handler on siege-badge checking #btn-siege-bribe. styles/main.css: .btn--siege-bribe (amber border/color, same pattern as crisis button)."
  },
  {
    "id": "T140",
    "title": "population happiness system — mood score affects production rates",
    "description": "Add a happiness score (0-100) to state.population. Happiness drifts toward a target each tick based on food security, morale, and active crises. When ≥75 (Prosperous): +10% all positive rates. When ≤25 (Unrest): -10% all positive rates. Threshold crossings emit messages and recalcRates. HUD population badge shows a mood emoji (😊/😐/😤) and CSS color.",
    "priority": 140,
    "status": "done",
    "phase": 53,
    "notes": "systems/population.js: Added HAPPY_HIGH=75, HAPPY_LOW=25 exports; import recalcRates from resources.js; initPopulation() initialises state.population.happiness=50 and migrates old saves; added getHappiness() getter; added happinessTick() — computes target from foodPct (>=60%→+20, <25%→-25), morale (>=70→+15, <30→-15), crises (-15), smoothly drifts happiness at 0.2%/tick, calls recalcRates()+message+POPULATION_CHANGED on threshold crossings. systems/resources.js: In recalcRates(), after T134 scholar block, reads state.population.happiness and applies ×1.10 (Prosperous) or ×0.90 (Unrest) to all positive rates. main.js: Added happinessTick to population.js import; registerSystem(happinessTick) in boot(). ui/hud.js: Added getHappiness/HAPPY_HIGH/HAPPY_LOW to population.js import; _renderPopBadge() now appends mood emoji, sets population-badge--happy/unrest CSS classes, and shows happiness% in tooltip. styles/main.css: .population-badge--happy (green), .population-badge--unrest (red)."
  },
  {
    "id": "T141",
    "title": "tech milestone rewards — one-time bonuses at research thresholds",
    "description": "One-time resource + prestige rewards when total researched tech count reaches 4, 8, 12, and all 16 techs. 4 techs: +200 gold +50 mana (windfall toast). 8 techs: +400 gold +100 mana +50 prestige. 12 techs: +600 gold +200 prestige + permanent +1.5 gold/s. All 16 techs: permanent +10% all positive rates +300 prestige. Permanent bonuses read from state.techMilestones in recalcRates(). Checked on every TECH_CHANGED event. state.techMilestones saved/loaded. No save version bump.",
    "priority": 141,
    "status": "done",
    "phase": 54,
    "notes": "core/state.js: Added techMilestones:{} to static object + initState() reset; allianceMissions:null added too (for T142). core/events.js: Added ALLIANCE_MISSION event (for T142). systems/resources.js: Added T141 permanent bonus block after T140 happiness block — techMilestones[12] adds +1.5 gold/s flat; techMilestones[all] multiplies all positive rates by 1.10. main.js: Added TECH_MILESTONE_DEFS array (4 entries: thresholds 4/8/12/all with loot rewards and prestige values); TOTAL_TECHS=16 constant; _checkTechMilestones() — counts Object.keys(state.techs).length, iterates milestones, awards resources (capped) + prestige, recalcRates() for permanent bonuses, logs windfall message; registered on(Events.TECH_CHANGED, _checkTechMilestones) in boot(); techMilestones+allianceMissions added to _save() and _applySave() (backwards-compatible defaults). No save version bump — backwards-compatible {} default."
  },
  {
    "id": "T142",
    "title": "alliance missions — ally-assigned objectives with gold rewards",
    "description": "Each allied AI empire may assign the player a small mission every 8-12 minutes. Three mission types: battle_wins (win 2-4 combat battles), earn_gold (accumulate 300-800 gold above current), research_tech (complete any research). Player has 5 minutes (1200 ticks). Success: 150-250 gold + 30 prestige + diplomatic log entry (type mission). Failure is silent. One active mission per ally at a time. ALLIANCE_MISSION event. state.allianceMissions saved/loaded. No save version bump.",
    "priority": 142,
    "status": "done",
    "phase": 54,
    "notes": "Implemented: systems/allianceMissions.js (initAllianceMissions, allianceMissionTick, checkMissionProgress, trackMissionBattleWin, missionSecsLeft, missionNextSecs, MISSION_PRESTIGE). Three mission types: battle_wins, earn_gold, research_tech. 5-minute window, 8-12min cooldown. Wired into main.js (import, registerSystem, initAllianceMissions in boot+newGame, event hooks MAP_CHANGED/RESOURCE_CHANGED/TECH_CHANGED). combat.js _victory() calls trackMissionBattleWin. diplomacyPanel.js: _missionRow() renders active mission card or next-mission countdown, injected into _empireCard(), ALLIANCE_MISSION subscription, hasMission tick refresh. CSS: .dipl-mission-row, .dipl-mission-card, .dipl-mission-bar-wrap/.bar. state.allianceMissions saved/loaded (no version bump)."
  }
]